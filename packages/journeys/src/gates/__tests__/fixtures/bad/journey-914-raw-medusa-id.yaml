# Lint-gate BAD fixture — schema-level rule re-checked at lint altitude
# (check 6): a raw Medusa id literal in params must surface as a
# `raw_medusa_id` lint problem instead of an exception.
id: JOURNEY-914
title: Lint fixture — raw Medusa id in params
businessCase: Gate fixture; journeys reference entities by handle only.
persona: Cliente autenticado com uma referência de produto não estável.
channel: web
params:
  authenticated: true
  productId: prod_01HXAMPLE
acts:
  - kind: chat
    goal: Adicionar um item ao carrinho.
expects:
  - intentKind: order.item.add
    decision: EXECUTE
verify: []
status: active
source: T1a-2 lint-gate fixture
