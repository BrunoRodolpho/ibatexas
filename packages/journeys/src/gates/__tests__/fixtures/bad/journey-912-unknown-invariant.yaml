# Lint-gate BAD fixture — `verify[]` references an invariant id that does
# not resolve in the invariant registry. Lint must fail it with
# `unknown_invariant`.
id: JOURNEY-912
title: Lint fixture — unresolvable verify ref
businessCase: Gate fixture; verify refs must resolve against the invariant registry.
persona: Cliente autenticado adicionando um item ao carrinho.
channel: web
params:
  authenticated: true
acts:
  - kind: chat
    goal: Adicionar um item ao carrinho.
expects:
  - intentKind: order.item.add
    decision: EXECUTE
verify:
  - invariant: no.such.invariant
    args:
      status: pending
status: active
source: T1a-2 lint-gate fixture
