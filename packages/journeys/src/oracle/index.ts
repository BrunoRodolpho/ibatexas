// oracle/ barrel — deterministic oracle kit.
//
// PLACEHOLDER: filled by T1a-6 (ProjectionBarrier) and T1a-7
// (AuditReader + AuditTrailMatcher). Later agents edit ONLY this barrel —
// the root src/index.ts already re-exports it.

export {}
