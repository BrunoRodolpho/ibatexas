{
  "name": "@ibatexas/domain",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc --outDir dist",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "db:generate": "prisma generate && prisma generate --schema=prisma/claustrum-memory.prisma",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "db:seed:tables": "tsx src/seed-tables.ts",
    "db:seed:homepage": "tsx src/seed-homepage.ts",
    "db:seed:delivery": "tsx src/seed-delivery.ts",
    "db:seed:orders": "tsx src/seed-orders.ts"
  },
  "dependencies": {
    "@adjudicate/audit": "^4.0.0",
    "@adjudicate/core": "^1.6.0",
    "@adjudicate/primitives": "^0.4.0",
    "@ibatexas/nats-client": "workspace:*",
    "@ibatexas/pack-customer-onboarding": "workspace:*",
    "@ibatexas/pack-orders": "workspace:*",
    "@ibatexas/pack-reservations": "workspace:*",
    "@ibatexas/pack-whatsapp": "workspace:*",
    "@ibatexas/types": "workspace:*",
    "@prisma/adapter-pg": "^7.6.0",
    "@prisma/client": "^7.6.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.0",
    "prisma": "^7.6.0",
    "tsx": "^4.7.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
