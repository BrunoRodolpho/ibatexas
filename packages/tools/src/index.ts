// @ibatexas/tools
// Agent tool definitions and ToolRegistry.
// All 11 tools live here. Each tool is an isolated module — definition + handler.
// Authorization is enforced per tool via the ToolRegistry before execution.

export {};
