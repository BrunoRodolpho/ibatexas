import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Minha Lista de Desejos | IbateXas',
}

export default function WishlistLayout({ children }: { children: React.ReactNode }) {
  return children
}
