=== W7-P4 AFTER state — 4 prisma.orderNote.create sites wired to addNoteFromEnvelope ===
Branch: feat/correctness-w7-close-w6-findings

── apps/api/src/routes/cart.ts (~line 875-955, checkout note) ──────────────────
Migrated:
  - prisma.orderNote.create(...) → orderCmdSvc.addNoteFromEnvelope(envelope, state, extras)
  - Envelope kind: "order.note.add"
  - actor.principal: "user" (request.customerId) OR cart-scoped session
  - taint: "UNTRUSTED" (customer-initiated)
  - OrderState ctx: channel="web", projection-derived paymentMethod /
    paymentStatus / totalInCentavos
  - extras.author = "customer", authorId = request.customerId
  - createOrderCommandService(undefined, { auditSink: getAuditSink() })
    instantiated inline (the cart route doesn't already have a singleton).
  - W7-P4 marker comment block above the migrated block.

── apps/api/src/routes/order-actions.ts (POST /api/orders/:id/notes) ───────────
Migrated:
  - prisma.orderNote.create(...) → noteAddSvc.addNoteFromEnvelope(...)
  - Envelope kind: "order.note.add", taint: "UNTRUSTED"
  - Pre-call orderQuerySvc.getById(id) feeds OrderState; on miss returns 404.
  - Outcome decision: only EXECUTE / REWRITE proceed; REFUSE/DEFER/ESC
    surface the kernel's pt-BR message at HTTP 403.
  - Service: createOrderCommandService(server.log, { auditSink: getAuditSink() })
  - Response shape preserved by refetching the persisted note for content +
    createdAt.

── apps/api/src/routes/admin/order-actions.ts (POST /staff-notes) ──────────────
Migrated:
  - prisma.orderNote.create(...) → orderCmdSvc.addNoteFromEnvelope(...)
  - Envelope kind: "order.note.add"
  - Payload includes isInternal: true (Pack's OrderNoteAddPayload supports
    optional isInternal flag).
  - actor: staffId → "user" / "TRUSTED", null → "system" / "SYSTEM".
  - Top-level orderCmdSvc upgraded to:
      createOrderCommandService(server.log, { auditSink: getAuditSink() })
    (previously: createOrderCommandService() — no audit wiring; the Wave-6
    finding's silent-audit risk closed here).

── apps/api/src/routes/admin/payments.ts (POST /api/admin/orders/:id/notes) ────
Migrated:
  - prisma.orderNote.create(...) → orderCmdSvc.addNoteFromEnvelope(...)
  - Envelope kind: "order.note.add"
  - actor: staffId → "user" / "TRUSTED", null → "system" / "SYSTEM".
  - New orderCmdSvc declared at top of adminPaymentRoutes:
      createOrderCommandService(server.log, { auditSink: getAuditSink() })

── Untouched (out of scope) ────────────────────────────────────────────────────
- prisma.orderNote.findMany sites at order-actions.ts:715 and
  admin/payments.ts:1268 — these are reads, not mutations. Kernel
  governance applies only to mutating intents per CLAUDE.md rule #9.

── Tests ────────────────────────────────────────────────────────────────────────
New: apps/api/src/routes/__tests__/order-note-add-routing.test.ts (7 tests)
  1. cart.ts uses addNoteFromEnvelope + kind:"order.note.add" + W7-P4 marker
  2. order-actions.ts uses addNoteFromEnvelope + no prisma.orderNote.create
  3. admin/order-actions.ts uses addNoteFromEnvelope + no direct write
  4. admin/payments.ts uses addNoteFromEnvelope + no direct write
  5. Each migrated route emits envelope with UNTRUSTED (customer) or
     staffId-conditional TRUSTED/SYSTEM (admin) taint
  6. admin/order-actions.ts uses audit-wired orderCmdSvc
  7. admin/payments.ts constructs audit-wired orderCmdSvc

This is a source-level chokepoint test — mirrors apps/api/src/__tests__/
bypass-detection/* in intent. Full integration coverage is the
W7-Verifier agent's responsibility.

── Verification ────────────────────────────────────────────────────────────────
pnpm vitest run apps/api/src/routes/__tests__/order-note-add-routing.test.ts
  → 7 tests passed
pnpm vitest run apps/api/src/routes/__tests__/
  → 22 tests passed (existing customer-mutation-governance + auth-customer-
    envelope unaffected)
pnpm vitest run apps/api/src/routes/admin/__tests__/
  → 67 tests passed (force-routes-governance, kernel-kill-switch, products-
    routes all unaffected)
pnpm vitest run apps/api/src/__tests__/bypass-detection/
  → 31 tests passed (no new bypasses introduced)

── KNOWN_INTENT_KINDS exclusion ────────────────────────────────────────────────
order.note.add is in @ibatexas/pack-orders' canonical Pack intent list
(packages/pack-orders/src/types.ts:86). It's LLM-proposable on the
WhatsApp customer surface (capabilities.add_order_note maps to it). No
change is needed to KNOWN_INTENT_KINDS — the kind was already registered
when the Pack was promoted. This PR only adds NEW call sites.
