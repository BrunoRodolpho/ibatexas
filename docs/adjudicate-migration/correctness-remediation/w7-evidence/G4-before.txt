# G4 — kernel_audit_spill_bytes metric mismatch (BEFORE fix)
#
# Two demonstrations:
#
# ── 1. False-negative — test passes despite real mismatch ─────────────
#
# Sink (apps/api/src/plugins/kernel-metrics-sink.ts:340) emits
#   `kernel_audit_sink_spill_size`
# Dashboard (infra/grafana/dashboards/kernel-audit-pipeline-health.json:252)
# and alert (infra/alerts/kernel.yaml:218) reference
#   `kernel_audit_spill_bytes`
# Test (packages/cli/src/__tests__/infra-grafana-alerts.test.ts:65)
# allowlist STATICALLY includes `kernel_audit_spill_bytes` — so the
# mismatch passes silently (the dashboard's metric name is in the
# hardcoded allow set even though the sink doesn't emit it).
#
# Command (run from /Users/thaisrodolpho/projects/ibatexas/packages/cli):
#   pnpm vitest run src/__tests__/infra-grafana-alerts.test.ts
# Result: 42 tests pass.


 RUN  v3.2.4 /Users/thaisrodolpho/projects/ibatexas/packages/cli

 ✓ src/__tests__/infra-grafana-alerts.test.ts (42 tests) 5ms

 Test Files  1 passed (1)
      Tests  42 passed (42)
   Start at  18:02:37
   Duration  271ms (transform 26ms, setup 0ms, collect 43ms, tests 5ms, environment 0ms, prepare 46ms)



# ── 2. Manual mismatch injection: test DOES catch unknown metrics ───────
#
# When the dashboard is mutated to reference a non-existent metric
# (kernel_audit_fake_metric_xyz, NOT in the allowlist), the test fails
# as expected — proving the allow-list mechanism works, but is
# powerless against the *actual* mismatch above because both the wrong
# name and the right name are in the static allowlist.
#
# Reproduction (with dashboard temporarily mutated):
#   sed -i.bak 's/kernel_audit_spill_bytes/kernel_audit_fake_metric_xyz/g' \
#     infra/grafana/dashboards/kernel-audit-pipeline-health.json
#   pnpm vitest run packages/cli/src/__tests__/infra-grafana-alerts.test.ts
# Result: 1 test fails with offender:
#   panel 3 (Persistent buffered-sink spill size (threshold 1GB)): kernel_audit_fake_metric_xyz
#
# After capturing this evidence the dashboard was restored to its original
# kernel_audit_spill_bytes (the broken state). The W7-G4 fix renames the
# three references to kernel_audit_sink_spill_size AND replaces the
# hardcoded allowlist with a programmatic derivation from the sink.
