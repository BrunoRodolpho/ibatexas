W7 — P1 Reservation tool layer migration — BEFORE state
=========================================================

Date: 2026-05-23
Branch: feat/correctness-w7-close-w6-findings
HEAD: 94964f0 (W6 final synthesis)
Agent: W7-Govern-Customer

Bypass description (per W6 governance-coverage scan):
  4 reservation tool files invoke `createReservationService()` and call
  the bare-argument legacy methods (svc.create / svc.modify / svc.cancel /
  svc.joinWaitlist) rather than routing through the envelope-typed entry
  points (createFromEnvelope / modifyFromEnvelope / cancelFromEnvelope /
  *FromEnvelope) that already exist on the service.

  Per CLAUDE.md rule #9 (LLM authority), every LLM-tool mutation must
  pass through the adjudicate kernel via an IntentEnvelope. The bare
  service calls bypass the kernel entirely, so any LLM-proposed
  reservation mutation never gets adjudicated nor audit-recorded.

Sites bypassing the kernel (BEFORE):
  - packages/tools/src/reservation/create-reservation.ts:17
      svc.create({ customerId, timeSlotId, partySize, specialRequests })

  - packages/tools/src/reservation/modify-reservation.ts:22
      svc.modify(parsed.reservationId, parsed.customerId, { ... })

  - packages/tools/src/reservation/cancel-reservation.ts:23, 25
      svc.getById(...)  ← read; envelope not strictly needed
      svc.cancel(parsed.reservationId, parsed.customerId)  ← write bypass

  - packages/tools/src/reservation/join-waitlist.ts:12
      svc.joinWaitlist({ customerId, timeSlotId, partySize })

Envelope methods available on packages/domain/src/services/reservation.service.ts:
  - createFromEnvelope          (line 557)
  - modifyFromEnvelope          (line 593)
  - cancelFromEnvelope          (line 629)
  - transitionFromEnvelope      (line 654; for checkin/complete/no_show — not used by these tools)

Sub-finding: NO `joinWaitlistFromEnvelope` exists on the service today.
  Pack-reservations declares `reservation.waitlist.join` as a valid intent
  kind (packages/pack-reservations/src/types.ts:55), but there is no
  service-side envelope entry point. The join-waitlist tool migration is
  therefore BLOCKED — this is surfaced in the residual risks of the final
  report.

Vitest output (before any code change, all tests passing):

  RUN  v3.2.4 /Users/thaisrodolpho/projects/ibatexas

   ✓ packages/tools/src/reservation/__tests__/join-waitlist.test.ts (5 tests) 5ms
   ✓ packages/tools/src/reservation/__tests__/check-availability.test.ts (6 tests) 6ms
   ✓ packages/tools/src/reservation/__tests__/get-my-reservations.test.ts (5 tests) 4ms
   ✓ packages/tools/src/reservation/__tests__/modify-reservation.test.ts (8 tests) 6ms
   ✓ packages/tools/src/reservation/__tests__/cancel-reservation.test.ts (7 tests) 8ms
   ✓ packages/tools/src/reservation/__tests__/create-reservation.test.ts (6 tests) 45ms
   ✓ packages/tools/src/reservation/__tests__/notifications.test.ts (6 tests) 38ms
   ✓ packages/tools/src/reservation/__tests__/utils.test.ts (10 tests) 16ms

   Test Files  8 passed (8)
        Tests  53 passed (53)

Tests pass with bare-arg service calls because the existing tests mock
`createReservationService()` to expose `.create` / `.modify` / `.cancel` /
`.joinWaitlist` directly. They do NOT exercise the envelope path. The
"after" tests will assert the envelope-routed calls.
