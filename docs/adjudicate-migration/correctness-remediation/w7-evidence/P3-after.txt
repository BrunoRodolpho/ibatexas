W7 — P3 Customer onboarding upserts — AFTER state
==================================================

Date: 2026-05-23
Branch: feat/correctness-w7-close-w6-findings
Agent: W7-Govern-Customer

Changes:
  - apps/api/src/routes/auth.ts (POST /api/auth/verify-otp at line ~390)
      • Replaced `customerSvc.upsertFromPhone(phone, name)` with a
        system-actor envelope flow:
          payload = { phoneHash, source: "otp" }
          envelope.actor = { principal: "system", sessionId: "auth-verify-otp:<hash>" }
          envelope.taint = "SYSTEM"  (clears pack's systemMinimum: "SYSTEM" floor)
          envelope.nonce = "auth-verify-otp:<hash>"  (deterministic for replay-safety)
          state.ctx.actor.principal = "system"
          state.ctx.otpFresh = true
        Routes via `customerSvc.createFromEnvelope(envelope, state, { phone, name? })`.
      • On non-EXECUTE: logs + returns 500 (the system-seed path should
        never be refused; if it is, operators must investigate).
      • Preserves the customer-response shape `{ id, phone, name, email }`
        via a follow-up `prisma.customer.findUniqueOrThrow(id)` after
        EXECUTE — the envelope executor returns only `{ id }` per the
        existing service contract.

  - apps/api/src/whatsapp/session.ts (resolveWhatsAppSession at line ~135)
      • Replaced `customerSvc.upsertFromWhatsApp(phone)` with a
        system-actor envelope flow:
          payload = { phoneHash, source: "wa-auto" }
          envelope.actor = { principal: "system", sessionId: "wa-session-resolve:<hash>" }
          envelope.taint = "SYSTEM"
          envelope.nonce = "wa-session-resolve:<hash>"
          state.ctx.actor.principal = "system"
          state.ctx.otpFresh = false  (pre-OTP, pre-auth path)
        Routes via `customerSvc.createFromEnvelope(envelope, state, { phone })`.
      • On non-EXECUTE: throws so the upstream WhatsApp handler can
        surface the error.
      • Customer-creation rate-limit (MAX_CUSTOMER_CREATES_PER_MINUTE)
        preserved — runs BEFORE envelope build, so kernel never sees
        rate-limited traffic.

  - apps/api/src/__tests__/auth-routes.test.ts
      • Updated the @ibatexas/domain mock to expose `createFromEnvelope`
        which derives its EXECUTE result from `mockUpsertFromPhone` (a
        back-compat shim so the 9 existing test sites don't need
        per-call rewrites). Also stubs `prisma.customer.findUniqueOrThrow`
        to return the same row the shim built.

  - apps/api/src/__tests__/whatsapp-session.test.ts
      • Updated the @ibatexas/domain mock to expose `createFromEnvelope`
        which derives its EXECUTE result from `mockUpsertFromWhatsApp`.

  - apps/api/src/routes/__tests__/auth-customer-envelope.test.ts  (NEW)
      • 4 dedicated tests asserting envelope shape, response preservation,
        REFUSE handling, and conditional name in extras.

  - apps/api/src/whatsapp/__tests__/session-customer-envelope.test.ts  (NEW)
      • 5 dedicated tests asserting envelope shape, response preservation,
        REFUSE handling, cache-hit short-circuit, and rate-limit-before-envelope.

Critical-path validation:
  - OTP-verify is the HIGH-BLAST customer login path. All 36 existing
    auth-routes.test.ts cases still pass (token issuance, refresh
    cookie, response shape, brute-force lockout, IP rate limit, etc.).
    Customer object returned has identical shape `{ id, phone, name, email }`.
  - WhatsApp session resolve is the WhatsApp first-contact path. All 33
    whatsapp-session.test.ts cases still pass.

Test files run (final):

  RUN  v3.2.4 /Users/thaisrodolpho/projects/ibatexas

   ✓ apps/api/src/whatsapp/__tests__/session-customer-envelope.test.ts (5 tests) 5ms
   ✓ apps/api/src/__tests__/whatsapp-session.test.ts (33 tests) 12ms
   ✓ apps/api/src/routes/__tests__/auth-customer-envelope.test.ts (4 tests) 80ms
   ✓ apps/api/src/__tests__/auth-routes.test.ts (36 tests) 139ms

   Test Files  4 passed (4)
        Tests  78 passed (78)

2 customer-onboarding upsert sites migrated to kernel-routed envelopes.
9 new envelope-routing assertions covering the shape and behavior.
TypeScript: my files compile clean. Pre-existing TS errors in
apps/api/src/routes/stripe-webhook.ts are OUT OF MY SCOPE.
