W7-O5 — Runbook Redis key references aligned to `<APP_ENV>:foo` via `rk()` convention
=====================================================================================

Finding (W6 operational drill, Drill 1, verdict PARTIAL):

  The key is `<APP_ENV>:kill-switch:global` (e.g. `development:kill-switch:global`),
  NOT `ibatexas:kill-switch:global` as the operator might infer from the project
  name. Verified in `packages/tools/src/redis/key.ts:14` — prefix is
  `process.env.APP_ENV`. Operator querying with `ibatexas:` prefix sees nothing.

Inventory of literal `ibatexas:` Redis-key references that mislead operators (grep
restricted to runbooks/governance/migration; investigation docs out-of-scope per
RULE F):

  $ grep -rn "ibatexas:" docs/adjudicate-migration/runbooks/ \
                            docs/adjudicate-migration/governance/ \
                            docs/adjudicate-migration/migration/

  docs/adjudicate-migration/governance/07-rollback-recovery.md:106
    > The Redis key shape (`ibatexas:env:kernel:killswitch`) is what
  docs/adjudicate-migration/governance/07-rollback-recovery.md:269
    > redis-cli SCAN 0 MATCH "ibatexas:prod:defer:pending:*"
  docs/adjudicate-migration/governance/07-rollback-recovery.md:273
    > redis-cli GET "ibatexas:prod:defer:pending:{sessionId}"

  Note: `migration/05-kill-switch-strategy.md` mentions `ibatexas.kernel.kill_switch.v1`
  (NATS subject) and `@ibatexas/pack-orders` (npm package id) — neither is a Redis
  key. Left untouched.

Fix:

  1. `governance/07-rollback-recovery.md` — three rewrites that replace the
     `ibatexas:env:` / `ibatexas:prod:` literals with `<APP_ENV>:` placeholders,
     and document the `rk()` convention inline so the next operator does not
     re-acquire the same misconception. The "manual resume" snippet also gets
     re-targeted to the W7-O1 `ibx kernel defer resume <sessionId>` CLI which
     supersedes the old `ibx kernel defer:resume --force` shape.

  2. `runbooks/SHADOW-ENFORCE-ROLLOUT.md` — adds an inline `rk()` convention
     note at the top, citing the helper file (`packages/tools/src/redis/key.ts`)
     and naming the three runbook companions where the convention applies. This
     is the call-out the W6 drill explicitly flagged ("Operator querying with
     `ibatexas:` prefix sees nothing").

Post-fix grep:

  $ grep -rn "ibatexas:" docs/adjudicate-migration/runbooks/ \
                            docs/adjudicate-migration/governance/ \
                            docs/adjudicate-migration/migration/
  # → no matches in the runbook/governance/migration corpus.

  (`investigation/06-runtime-config-governance.md` still contains "ibatexas:<env>:"
  in a descriptive sentence about ledger keys; investigation/ is documentation-of-
  history and out of scope for the W7-Ops file scope per the prompt.)

Per RULE G, this is a PURE DOC commit — no code is touched.
