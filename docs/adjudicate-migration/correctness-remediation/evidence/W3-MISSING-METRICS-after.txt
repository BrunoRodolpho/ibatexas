W3 — Missing Metrics + Doc Drift — AFTER snapshot
====================================================
Date: 2026-05-23
Branch: feat/correctness-w1-reproduction

D1 — 11 ghost metrics now registered in
     apps/api/src/plugins/kernel-metrics-sink.ts:
     (from `grep -nE 'name: "(kernel_[a-z_]+)"' …`)

155:        name: "kernel_decision_total",
163:        name: "kernel_refusal_total",
176:        name: "kernel_decision_duration_seconds",
186:        name: "kernel_shadow_divergence_total",
194:        name: "kernel_ledger_op_total",
202:        name: "kernel_audit_sink_failure_total",
210:        name: "kernel_defer_resume_duration_seconds",
220:        name: "kernel_intent_kind_coverage",
228:        name: "kernel_distinct_intent_kinds_observed",
236:        name: "kernel_known_intent_kinds_total",
267:        name: "kernel_audit_lag_seconds",                  ✓ W3-1
276:        name: "kernel_replay_drift_total",                 ✓ W3-2
284:        name: "kernel_kill_switch_state",                  ✓ W3-3
292:        name: "kernel_pack_install_total",                 ✓ W3-4
300:        name: "kernel_defer_pending_gauge",                ✓ W3-5
308:        name: "kernel_defer_quota_exceeded_total",         ✓ W3-6
316:        name: "kernel_defer_timeout_total",                ✓ W3-7
324:        name: "kernel_audit_redactor_failures_total",      ✓ W3-8
332:        name: "kernel_audit_sink_buffer_size",             ✓ W3-9
340:        name: "kernel_audit_sink_spill_size",              ✓ W3-10
348:        name: "kernel_intent_kind_unknown_total",          ✓ W3-11

EMISSION POINTS:

  W3-1  kernel_audit_lag_seconds
        → registered in kernel-metrics-sink.ts:267
        → observed in packages/llm-provider/src/intent-audit-wiring.ts:
          `_sink.emit` wrapper calls recordAuditLagFromIso(record.at, emitStart)
        → hook wired in apps/api/src/plugins/kernel-bootstrap.ts:153 via
          setAuditLagHook((sink, lat) => recorder.recordAuditLag(sink, lat))

  W3-2  kernel_replay_drift_total
        → registered in kernel-metrics-sink.ts:276
        → recorder method `recordReplayDrift(class)` at :718-722
        → emission point: ibx kernel replay (CLI — wired in separate
          Wave 3 task `packages/cli/src/commands/kernel.ts`; recorder
          available via createKernelMetricsRecorder)

  W3-3  kernel_kill_switch_state
        → registered in kernel-metrics-sink.ts:284
        → seeded in apps/api/src/plugins/kernel-bootstrap.ts:351-355 on bootstrap
        → 30s poll in startKillSwitchStatePoll calls
          recorder.recordKillSwitchState("global", state.active)

  W3-4  kernel_pack_install_total
        → registered in kernel-metrics-sink.ts:292
        → emitted in apps/api/src/plugins/kernel-bootstrap.ts
          installFirstPartyPacks() — one increment per pack
          (orders, reservations, whatsapp, customer-onboarding, payments)

  W3-5  kernel_defer_pending_gauge
        → registered in kernel-metrics-sink.ts:300
        → 60s poll in startDeferPendingGaugePoll runs SCAN `defer:pending:*`
          and calls recorder.recordDeferPending(count)
        → configurable via IBX_DEFER_PENDING_POLL_SECONDS

  W3-6  kernel_defer_quota_exceeded_total
        → registered in kernel-metrics-sink.ts:308
        → emitted in apps/api/src/adapters/park-deferred-intent-nx.ts:
          bumpDeferQuotaExceededMetric(args.envelope.kind) when framework
          returns `quota_exceeded`

  W3-7  kernel_defer_timeout_total
        → registered in kernel-metrics-sink.ts:316
        → emitted in apps/api/src/jobs/defer-timeout-sweeper.ts:
          bumpDeferTimeoutMetric(signal) after successful
          publishNatsEvent("intent.defer.timeout") — both steady-state
          and recovery scan paths.

  W3-8  kernel_audit_redactor_failures_total
        → registered in kernel-metrics-sink.ts:324
        → emitted in packages/llm-provider/src/audit-redactor.ts:
          fail-open catch block now invokes opts.onFailure(reason) with
          classification cycle|throw|unknown.
        → hook wired in apps/api/src/plugins/kernel-bootstrap.ts:156-158
          via setAuditRedactorFailureHook

  W3-9  kernel_audit_sink_buffer_size
        → registered in kernel-metrics-sink.ts:332
        → emitted in intent-audit-wiring.ts onSpill (capacity-reason)
          via _bufferSizeHook(count)
        → hook wired via setAuditSinkBufferSizeHook

  W3-10 kernel_audit_sink_spill_size
        → registered in kernel-metrics-sink.ts:340
        → emitted in intent-audit-wiring.ts onOverflow
          via _spillSizeHook(count)
        → hook wired via setAuditSinkSpillSizeHook

  W3-11 kernel_intent_kind_unknown_total
        → registered in kernel-metrics-sink.ts:348
        → automatic emission in recordDecision (kernel-metrics-sink.ts:511-517)
          when intentKind is NOT in knownIntentKinds.

D2 — drift resolutions:

  doc `kernel_decision_latency_seconds` → kept code `kernel_decision_duration_seconds`
                                          (Prometheus `_duration_seconds` convention)
  doc `kernel_sink_failure_total`       → kept code `kernel_audit_sink_failure_total`
                                          (sink-failure is audit-only; specific > generic)
  doc `kernel_audit_sink_failures_total` (plural)
                                        → kept code singular `_failure_total`
  doc `kernel_entrypoint_coverage_ratio` → kept code `kernel_intent_kind_coverage`
                                          (measures intent-kind coverage, not entrypoint)

D3 — migration/06-observability-requirements.md reconciled:

  - Added W3 reconciliation note at top (lines 8-23)
  - Added `verified at` lines for every contract metric
  - Added a TODO(CI) marker for metric-contract test gate
  - Renamed metric 2 + drift list at end of supporting-metrics table
  - Fixed alerting-rule PromQL (kernel-latency-spike, kernel-sink-failure-burst)
  - Fixed dashboard 1 ascii diagrams + queries

D4 — runbooks/SHADOW-ENFORCE-ROLLOUT.md PromQL drift fixed in concurrent
     wave 3 commit (line 86: `class="DECISION_KIND"`, not `divergence=`).

TESTS:

  apps/api/src/plugins/__tests__/kernel-metrics-sink.test.ts
  - 35 pre-existing tests still passing
  - 25 new W3 tests added (11 metric registration assertions + 11 emission
    assertions + 1 /metrics scrape coverage test + fail-open recorder test +
    knownIntentKinds-absent test)
  - Total: 60 passing, 0 failing.

  packages/llm-provider tests: 387 passing.
  apps/api kernel-bootstrap tests: 14 passing.
