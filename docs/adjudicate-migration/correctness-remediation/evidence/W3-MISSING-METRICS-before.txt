W3 — Missing Metrics + Doc Drift — BEFORE snapshot
====================================================
Date: 2026-05-23
Branch: feat/correctness-w1-reproduction

Current kernel_* metric inventory in apps/api/src/plugins/kernel-metrics-sink.ts
(from `grep -nE "name: \"(kernel_[a-z_]+)\"" apps/api/src/plugins/kernel-metrics-sink.ts`):

155:        name: "kernel_decision_total",
163:        name: "kernel_refusal_total",
176:        name: "kernel_decision_duration_seconds",
186:        name: "kernel_shadow_divergence_total",
194:        name: "kernel_ledger_op_total",
202:        name: "kernel_audit_sink_failure_total",
210:        name: "kernel_defer_resume_duration_seconds",
220:        name: "kernel_intent_kind_coverage",
228:        name: "kernel_distinct_intent_kinds_observed",
236:        name: "kernel_known_intent_kinds_total",

GHOST METRICS (declared in migration/06-observability-requirements.md, NOT in code):

  1. kernel_audit_lag_seconds                  — histogram, audit emit→sink-ack
  2. kernel_replay_drift_total                 — counter, ibx kernel replay drift detect
  3. kernel_kill_switch_state                  — gauge, current kill-switch state
  4. kernel_pack_install_total                 — counter, per installed pack at boot
  5. kernel_defer_pending_gauge                — gauge, parked envelopes (SCARD)
  6. kernel_defer_quota_exceeded_total         — counter, quota-rejection events
  7. kernel_defer_timeout_total                — counter, sweeper-published timeouts
  8. kernel_audit_redactor_failures_total      — counter, redactor failures
  9. kernel_audit_sink_buffer_size             — gauge, persistentBufferedSink usage
 10. kernel_audit_sink_spill_size              — gauge, Redis spill key count
 11. kernel_intent_kind_unknown_total          — counter, kinds NOT in KNOWN_INTENT_KINDS

DOC DRIFTS:

  D2a. migration/06 line 50 `kernel_decision_latency_seconds`
       → code emits `kernel_decision_duration_seconds`
       Resolution: rename doc to match code (`_duration_seconds` is the
       Prometheus convention per https://prometheus.io/docs/practices/naming).

  D2b. migration/06 line 163 `kernel_sink_failure_total`
       → code emits `kernel_audit_sink_failure_total`
       Resolution: rename doc to match code.

  D2c. migration/06 line 169 `kernel_entrypoint_coverage_ratio`
       → code emits `kernel_intent_kind_coverage`
       Resolution: rename doc to match code.

  D2d. migration/06 line 174 `kernel_audit_sink_failures_total` (plural)
       → code emits `kernel_audit_sink_failure_total` (singular)
       Resolution: rename doc to match code.

  D4. runbooks/SHADOW-ENFORCE-ROLLOUT.md line 86 PromQL uses label
      `divergence="DECISION_KIND"` but code emits label `class="DECISION_KIND"`
      Resolution: fix runbook to use `class`.
