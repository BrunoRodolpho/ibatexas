

# ===== kernelAudit =====

## [CONFIRMED] IntentActor.principal is closed union "llm"|"user"|"system"; actor.sessionId is free-form string and INSIDE the intentHash pre-image {version,kind,payload,nonce,actor,taint}
- evidence: packages/core/src/envelope.ts:38 `readonly principal: "llm" | "user" | "system"`; :39 `readonly sessionId: string`; intentHashInput() at envelope.ts:106-122 returns {version,kind,payload,nonce,actor,taint} — whole actor object (incl. sessionId) feeds sha256Canonical in buildEnvelope (:134-143) and deriveIntentHash (:165-167). Guard at :212-215 enforces the union.
- consequence: Plan's agent-identity-in-sessionId approach (agent:order-shepherd@3:order:{id}) is sound: free-form, hash-bound, lands in every audit row. No new principal value is possible without a kernel change.

## [CONFIRMED] IntentEnvelope has NO metadata field at all (8 fixed keys, additionalProperties:false) — v1 phrasing "metadata is outside the intentHash" is mechanically wrong
- evidence: packages/core/src/envelope.ts:175-184 EXPECTED_ENVELOPE_KEYS = exactly {version,kind,payload,createdAt,nonce,actor,taint,intentHash}; isIntentEnvelope :197-203 rejects any extra/missing key; docs/specs/intent-envelope-v2.schema.json:17 `"additionalProperties": false`. Only createdAt is described as "metadata" (envelope.ts:58).
- consequence: Plan must not stash agent provenance in a metadata field — it does not exist and extras are rejected pre-adjudication. sessionId (and later attestation) are the only identity carriers.

## [CONFIRMED] actor.attestation?: {keyId,sig} seam exists in TS but docs/specs/intent-envelope-v2.schema.json actor block is additionalProperties:false with only principal+sessionId — wire envelopes carrying attestation fail the schema today
- evidence: packages/core/src/envelope.ts:48-51 attestation?: {keyId,sig}; docs/specs/intent-envelope-v2.schema.json:41-56 actor object: required [principal,sessionId], `"additionalProperties": false` (:45), properties only principal+sessionId. Nuance: admin-sdk Zod IntentActorSchema (packages/admin-sdk/src/schemas/envelope.ts:26-31) DOES accept attestation, and core isIntentEnvelope only key-checks the top level — so kernel runtime accepts it; only the published JSON Schema rejects.
- consequence: Three validation surfaces disagree: kernel runtime and admin-sdk Zod accept attestation, published wire schema rejects it. Any signed-agent-identity work must patch docs/specs JSON Schema first or external validators will refuse valid envelopes.

## [CONFIRMED] createRedisLedger + createMemoryLedger exported from @adjudicate/audit; Ledger interface is checkLedger()/recordExecution() (NOT checkAndRecord); duplicate intentHash → REFUSE with basis REPLAY_SUPPRESSED
- evidence: packages/audit/src/index.ts:10,14 export createRedisLedger/createMemoryLedger; Ledger contract in packages/core/src/ledger.ts:48-69 has checkLedger + recordExecution (returns "acquired"|"exists"); `grep -rn checkAndRecord packages --include=*.ts` → no output. Kernel: adjudicate-and-audit.ts:381 checkLedger; :636-653 replaySuppressedRefusal → decisionRefuse with basis("ledger", BASIS_CODES.ledger.REPLAY_SUPPRESSED); basis-codes.ts:44 `REPLAY_SUPPRESSED: "replay_suppressed"`. Race path: recordExecution "exists" flips EXECUTE → REPLAY_SUPPRESSED (:216-219, :488).
- consequence: Plan's Phase-0 ledger wiring into ibatexas buildAdjudicator is correct as written; deps.ledger alone gets both the pre-check and the EXECUTE-race claim. Nonce-determinism caveat remains the adopter's job.

## [CONFIRMED] @adjudicate/approval-engine exports createApprovalEngine, createInMemoryApprovalRegistry, createRedisApprovalRegistry, ApprovalChannel; shipped channels are ONLY console-log + webhook (Slack/email doc comments only); resolve() → agent.confirm() re-adjudicates through adjudicateAndAudit with ledger
- evidence: packages/approval-engine/src/index.ts:12-38 exports all four plus createConsoleLogChannel/createWebhookChannel. channel.ts:16 is only a doc comment "(Slack/Teams/email/webhook)"; implementations are createWebhookChannel (:29) and createConsoleLogChannel (:56) only. engine.ts:108-131 resolve() → opts.agent.confirm(); adapter-core/src/loop.ts:566+ confirm() calls adjudicateAndAudit with `ledger: options.ledger` and confirmationReceipt {intentHash, at, token}.
- consequence: Plan v1 line 128 ("pluggable webhook/Slack/email channels") overstates: Slack/email channels must be budgeted as new ApprovalChannel implementations (small — interface is request/notifyResolved). Webhook channel may suffice for Slack via incoming webhooks.

## [CONFIRMED] Kernel ConfirmationReceipt is {intentHash, at, originalAt?, token?} — NO approving-principal field; approving principal lives only in ApprovalRequest.resolvedBy (registry projection, outside tamper-evident audit)
- evidence: packages/core/src/kernel/adjudicate-and-audit.ts:162-194 confirmationReceipt type = intentHash/at/originalAt?/token? only; auto-derived supersedes (:448-456) carries predecessorIntentHash/predecessorAt/reason/token — no approver identity. approval-engine/src/registry.ts:23 `resolvedBy?: { id, displayName? }`; `grep -rn resolvedBy packages/core/src packages/audit/src` → no output.
- consequence: "Who approved this agent action" is not in the canonical hash-chained AuditRecord — only in the TTL'd registry projection. Plan's supervised-mode audit story should either treat resolvedBy as best-effort or propose a kernel-side receipt extension (additive, hash-affecting — needs care).

## [CONFIRMED] DEFER-resume rebuilds successor envelope with principal "system" / taint TRUSTED, nonce = predecessor intentHash (adapter-core loop ~509-541); supersedes {predecessorIntentHash,...} carried on AuditRecord for both resume paths
- evidence: packages/adapter-core/src/loop.ts:509 predecessorIntentHash = parked envelope hash; :510-520 buildEnvelope({nonce: predecessorIntentHash, actor: {principal: "system", sessionId: parked sessionId}, taint: "TRUSTED"}); :534-541 explicit supersedes {predecessorIntentHash, predecessorAt: parkedAt, reason: "defer_resumed"}. Confirm path: loop.ts:566+ passes confirmationReceipt; kernel auto-derives supersedes reason "confirmation_resolved" (adjudicate-and-audit.ts:448-456).
- consequence: Trigger-originated agent resumes inherit replay-dedup for free (nonce = predecessor hash). Any agent provenance encoded in sessionId survives resume; principal flips to "system" on the resumed leg — plan's per-agent attribution must read sessionId, not principal, on resumed records.

## [CONFIRMED] apps/console has an /approvals page; resolving there is DISPLAY-ONLY (actual resolve runs adopter-side via createApprovalEngine.resolve → agent.confirm)
- evidence: apps/console/src/app/approvals/page.tsx:10-14: "In the reference console, resolving is DISPLAY-ONLY: it records the operator decision into the persisted registry projection. The single-use token take, parked-blob hash verify, kernel re-adjudication, and intent RESUME happen in the adopter's adapter process (createApprovalEngine.resolve → agent.confirm)." Components: src/components/approvals/{ApprovalsPanel,ApprovalsArea,ApprovalChainView,ApprovalHistoryView}.tsx, hooks/useApprovals.ts.
- consequence: Plan's Phase-3 supervised mode must run an adopter-side process hosting createApprovalEngine (with resolveStateContext into ibatexas state) — the console alone never resumes anything. The display-only gap is the real glue cost.

## [CONFIRMED] adjudicate simulate --scenarios contract {intent,state,expected}; exit codes 0/1/2, mismatch wins over error (docs/guides/testing-your-policy.md)
- evidence: Code (authoritative): packages/cli/src/lib/scenario.ts:65-68 schema {intent: IntentInputSchema, state: z.unknown(), expected?: ...}; packages/cli/src/lib/simulate-diff.ts:211-215 `computeExitCode: if (summary.changed > 0) return 2; if (summary.errors > 0) return 1; return 0;` — mismatch (2) checked before error (1). simulate.ts:206-209 process.exit(exitCode). Docs agree: testing-your-policy.md:215-217.
- consequence: Docs and code agree; plan can rely on exit-code semantics in CI gates and on the {intent,state,expected} shape as the L0 fixture format for the journey pyramid.

## [CONFIRMED] AI_CONTEXT.md still references PROJECT_STATUS_AND_NEXT_STEPS.md and docs/roadmap/ which were deleted (commit 50eec91)
- evidence: AI_CONTEXT.md:59 "roadmap/ Forward-looking roadmap notes.", :67 and :197 reference PROJECT_STATUS_AND_NEXT_STEPS.md. `ls` → "No such file or directory" for both paths. `git show --name-only --diff-filter=D 50eec91` ("docs: minimize project docs") lists PROJECT_STATUS_AND_NEXT_STEPS.md and docs/roadmap/README.md + docs/roadmap/design/*.
- consequence: Do not treat AI_CONTEXT.md's doc map as ground truth; any plan step that says "consult PROJECT_STATUS / roadmap" dead-ends. Cheap upstream fix if the plan touches adjudicate docs.

## DISCOVERY
- **@adjudicate/red-team package: deterministic adversarial envelope generators (prompt-injection, taint-escalation, tool-scope-violation), RedTeamScenario carries defense.acceptable Decision set, NON_EXECUTE_DEFENSES, and toSimulateScenario() down-projects to the simulate fixture format; surfaced as CLI `adjudicate red-team`**
  - evidence: packages/red-team/src/vectors.ts:6-17 generateAllVectors; scenario.ts:28-33 RedTeamScenario, :59-66 NON_EXECUTE_DEFENSES, :79-89 toSimulateScenario; cli/src/bin.ts:289 .command("red-team"); console has src/app/red-team page
  - consequence: Plan's agent-safety testing can seed adversarial fixtures from these generators against ibatexas packs (RedTeamPack only needs id/intents/policy) instead of hand-authoring attack scenarios; escapes feed straight into the simulate CI gate via toSimulateScenario.
- **CLI `adjudicate scenarios generate` — seeded-LCG deterministic scenario fixture emission (--count per intent kind, --seed reproducible, pipes through pack.rehydrateState), output shape exactly matches simulate --scenario input**
  - evidence: packages/cli/src/commands/scenarios-generate.ts:1-43 (header documents shape + seeded LCG); bin.ts:338-342 scenarios/generate command
  - consequence: Plan's L0 deterministic fixture layer can be bootstrapped per intent kind mechanically; journeys distill failures down into fixtures this tool can scaffold.
- **@adjudicate/drift package: statistical behavioral drift detection (createDriftDetector, createDriftHistory, totalVariationDistance) — pure observer over decision.kind/intent.kind/basis distributions, baseline vs trailing window, deterministic count-based windows; explicitly distinct from the console's operational DriftPanel**
  - evidence: packages/drift/src/index.ts:1-33; detector.ts/history.ts/distance.ts
  - consequence: Plan v1 never mentions it. Stage-0 shadow agents need exactly this: distribution drift over the journaled decisions (new-category and proportion-spike alarms) without building bespoke monitoring. Wire via AuditEventBus.
- **AuditEventBus infrastructure: createInMemoryAuditEventBus, createRedisAuditEventBus, bridgeAuditSinkToBus — fan AuditRecords to in-process or cross-process subscribers**
  - evidence: packages/audit/src/event-bus.ts:91 createInMemoryAuditEventBus, :159 bridgeAuditSinkToBus, :223 createRedisAuditEventBus
  - consequence: The plan's shadow journal / reconciliation gate can subscribe to live AuditRecords instead of polling intent_audit Postgres; also the natural attach point for @adjudicate/drift and per-agent metrics.
- **Distributed kill-switch machinery already built: startDistributedKillSwitch (Redis polling, ≤pollMs*2 convergence), startDistributedKillSwitchPubSub (Redis pub/sub, <100ms propagation, poll fallback, cold-boot resync), createRedisEmergencyStateStore, kill-switch-timeline; kernel consults RuntimeContext kill state fail-closed and emits basis kill.ACTIVE**
  - evidence: packages/audit/src/distributed-kill-switch.ts:68, kill-switch-pubsub.ts:1-55 + :183, redis-emergency-store.ts:162; kernel refusal with basis("kill", BASIS_CODES.kill.ACTIVE) in adjudicate-and-audit.ts ~290-300
  - consequence: Plan line 130 proposes a bespoke "per-agent kill switch Redis flag checked by the scope guard". Reusing this machinery (per-agent RuntimeContext or its propagation pattern) gets fail-closed semantics, audit basis, sub-100ms fan-out, and restart recovery for free.
- **NatsSink in @adjudicate/audit with a closed/open/half-open circuit breaker, explicitly designed around the IbateXas adopter's publishNatsEvent() wrapper**
  - evidence: packages/audit/src/sink-nats.ts:1-24 ("the IbateXas adopter passes a wrapper around its publishNatsEvent()")
  - consequence: Plan's JetStream transport discussion can stream AuditRecords to NATS via this first-class sink rather than custom bridging; breaker semantics already handle sustained outage visibility.
- **Pack-health conformance primitive: pure gold/silver/bronze/unrated tier roll-up composed from validatePackManifest + runConformance + verifyPackTrust, plus CLI `adjudicate pack lint|bom|verify` and conformance ai-bom.ts/config-seal.ts**
  - evidence: packages/conformance/src/pack-health.ts:1-60 (tiers, closed axis taxonomy); cli/src/bin.ts:148,157,172 (pack lint/bom/verify); conformance/src/ai-bom.ts, config-seal.ts
  - consequence: Plan's skill-manifest + AI-BOM gate (Phase 4) re-invents a scoring/gating surface that exists upstream; mapping the skill manifest onto PackManifest axes inherits health tiers and CI fail-noisy behavior.
- **approval-engine's adopter glue is smaller than plan budgets: ApprovalEngine.request() takes {token,sessionId,intentHash,intentKind,prompt,taint} from the REQUEST_CONFIRMATION turn; the only adopter-supplied seam is resolveStateContext(sessionId) fetched fresh at resolve; engine handles channel routing/fallback, registry projection, single-use-token failure marking (expired) and notifyResolved**
  - evidence: packages/approval-engine/src/engine.ts:14-15 (resolveStateContext), :24-39 (API), :49-106 (request), :108-147 (resolve incl. markResolved 'expired' on CONFIRM_REJECTED)
  - consequence: Plan's "~1 week approvals surface prerequisite" for Stage-1 supervised agents shrinks: parked-envelope↔approval-request↔resume orchestration is already the engine; remaining work is resolveStateContext into ibatexas SessionPort state plus a console-to-adopter resolve RPC.
- **@adjudicate/admin-sdk: Zod schemas for the full envelope/decision/audit vocabulary (IntentEnvelopeSchema accepts the attestation seam), audit query schemas, tRPC router (@adjudicate/admin-sdk/trpc) and Next adapter — with compile-time drift guards against kernel types**
  - evidence: packages/admin-sdk/src/index.ts:1-31; schemas/envelope.ts:18-32 (attestation accepted), :48-65 (drift guards)
  - consequence: Plan's console extensions (/agents, /coverage, /runs) and any agent-host HTTP surface should validate with these schemas rather than re-declaring zod shapes; note the admin-sdk-vs-JSON-Schema attestation divergence flagged in claims.
- **adapter-core ships a TokenUsageStore subsystem: TokenUsageSample/TokenBudgetConfig/SessionConsumption/TenantConsumption/TokenExhaustionEvent + in-memory store, i.e. per-session and per-tenant token budget accounting**
  - evidence: packages/adapter-core/src/token-usage-store.ts:63-173 (interfaces + createInMemoryTokenUsageStore options)
  - consequence: Plan composes createAgentScopeGuard "like sessionTokenBudgetGuard"; per-agent budget caps can reuse this store's consumption/exhaustion-event model keyed by the agent sessionId prefix instead of new accounting.
- **Supersession-chain walker in @adjudicate/audit reconstructs request→resolved→resumed chains (also rewrite_executed, replay, LGPD scrub links) from AuditRecord sets — one canonical implementation**
  - evidence: packages/audit/src/supersession-chain.ts:1-30
  - consequence: Plan mentions supersession-chain walking in the oracle kit (line 83) — confirm it imports this primitive rather than re-implementing the walk in ibatexas's AuditTrailMatcher.
- **examples/ contains three runnable references: commerce-reference, quickstart-anthropic, vacation-approval (vacation-approval exercises the approval flow end-to-end)**
  - evidence: ls examples → commerce-reference, quickstart-anthropic, vacation-approval
  - consequence: Phase-3 approval wiring and the Anthropic adapter loop have working reference implementations to crib from; reduces design risk in the supervised-mode glue.
- **Full CLI surface is 13 commands: analyze, dev, doctor, export (json/csv/parquet), pack init/lint/bom/verify, reap, repl, replay, red-team, scenarios generate, simulate, visualize**
  - evidence: packages/cli/src/bin.ts:3-17 imports, :32-422 command registrations
  - consequence: Plan's tooling inventory only credits simulate; export (audit→parquet), replay, and visualize are relevant to the eval-dataset and coverage-matrix phases without new tooling.


# ===== claustrum =====

## [CONFIRMED] ChannelKind is closed union "whatsapp" | "web" (packages/core/src/ports/channel.ts ~23); NO exhaustive switch on it anywhere (only a parallel inline union in cli replay.ts ~43, type-only); widening is a one-line core edit.
- evidence: claustrum packages/core/src/ports/channel.ts:23 `export type ChannelKind = "whatsapp" | "web";`. grep -n switch across packages: only handle-turn.ts:378 (acted.kind), dispatch.ts:108 (decision.kind), anthropic/provider.ts:179,257, openai/provider.ts:159 — none on ChannelKind. cli/src/commands/replay.ts:43 `readonly channel: "web" | "whatsapp"` is type-only (readTurnFile validates only `typeof parsed.channel === "string"`, lines 68-72). CAVEAT the claim misses: a SECOND parallel inline union at channel-whatsapp/src/attest.ts:30 `readonly channel: "whatsapp" | "web"` (AttestContext).
- consequence: Widening compiles with one edit at channel.ts:23 (narrow unions stay assignable to the widened one). But a new channel kind calling attestWithGatewayKey needs attest.ts:30 widened too. No runtime validation rejects unknown channel strings anywhere.

## [CONFIRMED] LOCK-KEY: Conductor session-lock key is `${input.channel}:${input.customerId}` and IGNORES OpenCapsuleInput.sessionKey (conductor.ts ~101-122); closeCapsule persists sessions keyed (customerId, channel) — so a trigger turn on a new channel kind and a live web turn for the same customer neither serialize nor share session state.
- evidence: conductor.ts:101-102 `sessionKeyOf = (channel, customerId) => \`${channel}:${customerId}\``; :122 `const lockKey = sessionKeyOf(input.channel, input.customerId)` — input.sessionKey used only for tenantResolver.resolve (:136-141) and sessionId (:148-149), never the lock. closeCapsule (conductor.ts ~245-258): `options.session.load(capsule.customerId, capsule.channel)` then `options.session.save(latest)`. session.load is (customerId, channel)-keyed (ports/session.ts:65).
- consequence: A turn opened on channel "system" (or any new kind) takes lock `system:cust` while a web turn holds `web:cust` — concurrent unserialized turns for one customer, with two disjoint channel-keyed sessions. Cross-channel triggers need either a shared lock-key strategy or explicit design acceptance.

## [CONFIRMED] Conductor.openCapsule accepts OpenCapsuleInput.actor override (principal "system" legal); default actor only when omitted; override is unvalidated.
- evidence: conductor.ts:80 `readonly actor?: Actor;` in OpenCapsuleInput; conductor.ts:151 `const actor: Actor = input.actor ?? { principal: "user", sessionId, customerId: input.customerId, role: "customer" };` — no validation of supplied actor. tools/types.ts:70 `readonly principal: "llm" | "user" | "system";` — "system" is in the union.
- consequence: A trigger/system path can mint capsules with principal "system" today without core changes; nothing in the Conductor checks who supplied the actor, so host-level gating of openCapsule callers is the only control point.

## [CONFIRMED] ChannelDriver.matchToParked contract: null = fresh utterance; resolveResume trigger-2 still auto-resumes DUE deferred envelopes channel-agnostically (handle-turn.ts ~345-352).
- evidence: ports/channel.ts:120-122 doc: "`null` (not a `ParkedMatch`) means 'fresh utterance; run the normal cognitive loop'". handle-turn.ts:295 doc "(channel-agnostic)"; :345-352 `const due = pickDueDeferred(session.deferredEnvelopes, inbound.receivedAt); if (due !== null) { const decision = await capsule.resume(due.envelope); ... }` — no receipt, no channel filter. CAVEAT: session itself is loaded per (customerId, channel) (conductor.ts:140-143), so trigger-2 only sees deferred envelopes parked on THAT channel's session; "channel-agnostic" means no reply-semantics needed, not cross-channel.
- consequence: Any inbound turn on the same channel-session wakes due deferred envelopes (re-adjudicated, no receipt). A deferred envelope parked on whatsapp will NOT resume from a web/system turn unless the SessionPort impl shares sessions across channels. Nothing wakes deferred envelopes without an inbound message.

## [CONFIRMED] CC-002 (execute-triggers-one-tool) + property test enforce EXECUTE/REWRITE ⇒ exactly one tool invocation; EXECUTE-with-empty-plan dispatches as noop "executed" with ZERO invocations (dispatch.ts ~115-123) which CC-002 would flag; REWRITE resolves the rewritten envelope through the same registry; ToolRegistry chooseImplementation supports per-tenant resolution.
- evidence: conformance/src/checks/execute-triggers-one-tool.ts:35 id "CC-002"; failure branch: `if (decisionKind === "EXECUTE" || decisionKind === "REWRITE") { ... if (delta !== 1) failures.push(...) }`. Property test core/test/properties/execute-one-tool.test.ts (200 iterations, fast-check). dispatch.ts:115-122 empty plan → `{ kind: "executed", envelope: emptyEnvelope(), toolId: "<noop>", result: undefined }` (0 invocations). handle-turn.ts:124-129: empty plan NOT short-circuited — adjudicatePlan([]) and kernel returns EXECUTE. REWRITE: dispatch.ts:148-161 `inferCapability(decision.rewritten, capsule.tools)` then `capsule.tools.resolveTool(capability, capsule)`. registry.ts:75-78 optional `chooseImplementation(candidates, ctx)`, default last-registered wins (:114-119), applied at :176.
- consequence: Per-tenant/shadow tool resolution slot exists (chooseImplementation + visibility, registry.ts:65-78) — no registry fork needed. Real tension: any adopter whose planner emits zero envelopes (smalltalk) produces EXECUTE-with-0-invocations turns, which CC-002 counts as failures — the plan's conformance runs must account for this.

## [CONFIRMED] InMemoryModelProvider complete() is a content-blind modulo-wrapping cursor over scripted completions (test-doubles/in-memory-model-provider.ts ~48-56).
- evidence: in-memory-model-provider.ts:48-56: `async complete(req) { this.seen.push(req); const next = this.completions[this.completionCursor % this.completions.length]; this.completionCursor += 1; ... return next; }` — request content never inspected; cursor wraps modulo. stream() shares the same cursor (:58-65).
- consequence: Scripted multi-turn agent tests must order completions exactly per LLM call (complete and stream advance one shared cursor); no content-keyed routing exists — if the plan needs prompt-conditional doubles it must build them.

## [CONFIRMED] PostgresAdvisorySessionLock exists in @claustrum/memory-postgres (pg_try_advisory_lock); Conductor DEFAULTS to InMemorySessionLock when options.sessionLock omitted.
- evidence: memory-postgres/src/advisory-session-lock.ts:52 `export class PostgresAdvisorySessionLock implements SessionLock`; :66 `SELECT pg_try_advisory_lock($1) AS locked`; exported at memory-postgres/src/index.ts:33. conductor.ts:95 `const sessionLock: SessionLock = options.sessionLock ?? new InMemorySessionLock();` with doc warning (conductor.ts:63-70) "correct for a SINGLE replica only ... Multi-process deployments MUST inject a distributed lock".
- consequence: Distributed serialization is opt-in; any multi-replica or out-of-process trigger worker sharing customers with the API must wire PostgresAdvisorySessionLock (or equivalent) into BOTH processes or double-adjudication occurs — and note from claim 2 the lock key is channel-scoped anyway.

## [CONFIRMED] FewShotIndex port has zero implementations and is never consumed; golden-conversation layer 2 and trace-replay layer 3 are plumbing-only (no trace store; core never calls emitLLMTrace).
- evidence: FewShotIndex defined ports/few-shot.ts:62, exported index.ts:128; grep across claustrum packages + examples + ibatexas apps: zero `implements FewShotIndex`, zero `.select(` consumers — PromptComposer takes a plain callback instead (synthesizer.ts:56, :109-111). No TraceStore impl anywhere (grep: only doc comments telemetry.ts:9-10, telemetry-bounds.ts:4; roadmap PROJECT_STATUS:52 v0.5.x). No emitLLMTrace call site in core/src or anthropic/openai providers; conformance/src/checks/prompt-manifest-in-trace.ts:26: "the FROZEN `handleTurn` does NOT call `emitLLMTrace` directly". CAVEAT on layer 2: CC-006 few-shot-regression (conformance/src/checks/few-shot-regression.ts) is a WORKING fixture-driven golden-conversation runner with 2 bundled fixtures (conformance/fixtures/few-shots/{greeting,refund-request}.json) + options.fixturesDir; layer 3: cli replay.ts replays single recorded turn files (decision-kind match), not historical LLM traces.
- consequence: Trace-replay against historical LLM traces is fully unbuilt (no store, no emission). But golden-conversation regression is NOT greenfield: CC-006 + fixturesDir already runs conversations through handleTurn and diffs envelope kinds + Decision — the plan should extend CC-006 fixtures rather than build a parallel runner.

## [BROKEN] WebChannel (in @claustrum/channel-web) matchToParked returns null unconditionally and render is a no-op.
- evidence: Half confirmed: channel-web/src/web-channel.ts:66-70 `matchToParked(...): ParkedMatch | null { ... return null; }` unconditionally. Half wrong: render is NOT a no-op in the package — web-channel.ts:44-46 `async render(response) { await this.config.sink(response); }` and constructor throws unless `typeof config.sink === "function"` (:34). The no-op lives in the ADOPTER: ibatexas claustrum-bootstrap.ts:1241-1243 wires `sink: async () => { // Replaced per-request by chat.ts via attachStream() pattern (TODO). }`.
- consequence: Web render delivery is an injection seam, not a package gap: the plan can deliver web renders by replacing ibatexas's no-op sink (per-request attachStream TODO already noted in source) without touching @claustrum/channel-web. matchToParked=null remains the structural blocker for chat-confirm resume.

## [CONFIRMED] PROJECT_STATUS_AND_NEXT_STEPS.md roadmap items relevant to trigger/system channel, multi-agent (PlannerProfile), trace store, MCP packages.
- evidence: Read in full (76 lines). See discovery entries for the substance: MCP v0.3.x planned/no packages exist; multi-agent 'partial' via PlannerProfile+PromptComposer but PlannerProfile has zero code hits; LLM-trace store v0.5.x; trigger/system channel absent from the doc entirely.
- consequence: Reported as discovery per instructions.

## DISCOVERY
- **TokenUsageStore does NOT live in claustrum — it is `createInMemoryTokenUsageStore` from @adjudicate/adapter-core, wired only in ibatexas as telemetry (ADR-135), fed from emitTurn, process-local + LRU-bounded, explicitly outside the determinism boundary.**
  - evidence: ibatexas apps/api/src/claustrum-bootstrap.ts:156-159 (import from "@adjudicate/adapter-core"), :671-682 (comment "TELEMETRY ONLY ... INDEPENDENT read of the same usage the F4 guard meters off the Redis counter"), :1000 `fastifyTelemetry(usageStore: TokenUsageStore)`. Claustrum's only token surface: LLMTrace/Completion inputTokens/outputTokens (core/src/ports/telemetry.ts:40-41,57-58). grep TokenUsageStore in claustrum: zero hits.
  - consequence: Any plan section placing token accounting in claustrum is mis-located; budget/usage work extends @adjudicate/adapter-core's interface (Redis-backed impl is a documented follow-up in the bootstrap comment) and ibatexas's fastifyTelemetry.
- **Conformance custom-check extension point already exists: ConformanceOptions.checks (plus sampling and seed knobs); runner falls back to DEFAULT_CHECKS of 6 checks (CC tool-capability-indirection, execute-triggers-one-tool, prompt-manifest-in-trace, refuse-renders-user-text, memory-recent-actions-via-api, few-shot-regression).**
  - evidence: conformance/src/types.ts:93 `readonly checks?: ReadonlyArray<ConformanceCheck>;`, :74 sampling, :81 seed; runner.ts:47-48 `options.checks ?? DEFAULT_CHECKS`; default-checks.ts:20-27.
  - consequence: Adopter-specific invariants (e.g. trigger-channel or shadow-mode checks) plug in as ConformanceCheck objects passed via options.checks — no fork or upstream change to @claustrum/conformance needed.
- **No scheduler/cron/trigger primitive exists anywhere in claustrum. Deferred-envelope resume (trigger-2) fires ONLY when an inbound message arrives on the same (customerId, channel) session; nothing scans deferredEnvelopes out-of-band. The park/defer machinery itself is complete: SessionPort.parkPendingConfirmation/parkDeferred/unpark, ParkedEnvelope/DeferredEnvelope types, DEFAULT_DEFER_MS.**
  - evidence: grep -il "trigger|scheduler|cron" core/src → only handle-turn.ts and ports/channel.ts (the resume doc). ports/session.ts:35-98 (ParkedEnvelope:35, DeferredEnvelope:43, parkDeferred:86, unpark:98). handle-turn.ts:345-352 pickDueDeferred.
  - consequence: A trigger/system channel or out-of-band waker is genuinely greenfield, but it should drive the EXISTING parkDeferred/pickDueDeferred substrate (synthesize an inbound turn) rather than invent new parking state.
- **PROJECT_STATUS roadmap positions (claim 10): MCP is v0.3.x planned — "@claustrum/mcp-client wraps any MCP server as a ToolPack" + "@claustrum/mcp-server exposes claustrum's capability graph" (lines 41-44); NEITHER package exists (packages/ = anthropic, channel-web, channel-whatsapp, cli, conformance, core, eslint-config, grounding-pgvector, memory-postgres, openai). Multi-agent is "partial: Agents as PlannerProfile + PromptComposer configs over the same cognitive loop" (line 21) with "Multi-agent broker over IPC" at v0.6.x (line 59). LLM-trace store with separate retention is v0.5.x (line 52). A trigger/system channel appears NOWHERE in the document.**
  - evidence: PROJECT_STATUS_AND_NEXT_STEPS.md:20-21,41-44,52,56-60; ls packages/ output quoted above.
  - consequence: Plan's trigger-channel idea has no upstream roadmap slot — it is a new proposal to claustrum, not an acceleration of planned work. MCP and trace-store sections can cite the roadmap rungs for sequencing.
- **PlannerProfile exists only as roadmap prose — zero hits in any .ts file across claustrum and ibatexas. PromptComposer is real (createPromptComposer, prompting/synthesizer.ts) and its fewShots hook is a plain async callback, not the FewShotIndex port.**
  - evidence: grep -rn PlannerProfile packages → no source hits (only PROJECT_STATUS:21). core/src/index.ts:159-160 exports createPromptComposer/PromptComposer; synthesizer.ts:56 `readonly fewShots?: (...)`, :109-111 invocation.
  - consequence: If the plan says "agents as PlannerProfile configs", it is referencing a type that must be designed from scratch; the existing composition axis is (PlannerPort impl + PromptComposer options) per Conductor.
- **Doc drift in port roster: PROJECT_STATUS:14 cites packages/core/src/ports/STATUS.md as the "authoritative roster" — that file does not exist. CLAUDE.md Rule 4 claims "14 conceptual ports across 15 port files"; the ports dir holds exactly 15 .ts files (adjudicator, channel, explainer, few-shot, grounding, handoff, memory, model-provider, planner, resolver, responder, session, session-lock, telemetry, tenant).**
  - evidence: ls packages/core/src/ports/ (15 files, no STATUS.md); PROJECT_STATUS_AND_NEXT_STEPS.md:14; CLAUDE.md Rule 4.
  - consequence: Do not cite STATUS.md in the plan; treat core/src/index.ts exports as the roster. Port-freeze claims sourced from docs need source verification.
- **WebChannelStub (core test-double) ships an injectable matchToParkedImpl override, defaulting to null — a ready seam for exercising web resume paths in tests without modifying @claustrum/channel-web.**
  - evidence: core/src/test-doubles/web-channel-stub.ts:28-31 `public matchToParkedImpl: ((channelEvent, session) => ParkedMatch | null) | null = null;`, :72-76 matchToParked delegates to it.
  - consequence: Plan tests for confirmation-resume on web can use this stub immediately; the production gap remains WebChannel.matchToParked + ibatexas's no-op sink (claustrum-bootstrap.ts:1241-1243, attachStream TODO).
- **advisoryLockId(key) helper hashes arbitrary string lock keys to 63-bit bigints for pg advisory locks, and is exported publicly from @claustrum/memory-postgres alongside the lock class.**
  - evidence: memory-postgres/src/advisory-session-lock.ts:44-51 advisoryLockId; index.ts:32-37 exports { PostgresAdvisorySessionLock, advisoryLockId, AdvisoryLockPool, AdvisoryLockClient }.
  - consequence: Any plan component needing cross-process mutual exclusion on a custom key (e.g. per-customer trigger dedup, cross-channel serialization) can reuse advisoryLockId + the same pool instead of inventing a Redis lock.
- **CC-006 few-shot fixtures ship in-package (conformance/fixtures/few-shots/greeting.json, refund-request.json) and the check accepts options.fixturesDir for adopter fixture sets; fixture schema = {id, scenario, conversation[], expectedEnvelopeKinds[], expectedDecisionKind, tags?} — essentially a mini golden-conversation format that already drives handleTurn end-to-end.**
  - evidence: find packages/conformance -path *fixtures* → 2 JSON files; few-shot-regression.ts:1-60 (fixture interface + defaultFixturesDir).
  - consequence: The plan's journey/golden-conversation layer overlaps this format; either extend CC-006's fixture schema or explicitly justify the richer journey YAML as a superset, citing this as the existing baseline.


# ===== bootstrapPlanner =====

## [CONFIRMED] buildAdjudicator({ sink: getAuditSink() }) never passes deps.ledger; TODO(Stage 3) comment; AdjudicatorBridgeDeps.ledger optional and forwarded only when present
- evidence: apps/api/src/claustrum-bootstrap.ts:1161 '// TODO(Stage 3): wire createRedisLedger({ client: redis, keyFor: rk })'; :1165 'const adjudicator = buildAdjudicator({ sink: getAuditSink() });'; :198 'readonly ledger?: Ledger;'; :418 '...(deps.ledger ? { ledger: deps.ledger } : {})' inside adjudicateAndAudit call (safeAuditedAdjudicate :381-432)
- consequence: Cross-turn replay-suppression is OFF in the live conductor path. Wiring createRedisLedger is a one-call in-repo change; the bridge already plumbs it. Phase 0 ledger-wiring prerequisite stands.

## [CONFIRMED] Audit read-paths replayEnvelopesByCustomerId / streamAuditByIntentHashPrefix / getOutcomes are empty TODO(loop-closure) stubs; verifyAuditRecord is real
- evidence: claustrum-bootstrap.ts:311-315 returns [] with '// TODO(loop-closure)'; :317-319 empty async generator; :320-322 returns []; :324-332 verifyAuditRecord calls kernelVerifyAuditRecord (imported :92 from @adjudicate/core) and maps {verified}→{ok}, fail-safe on missing hash
- consequence: Any plan step that reads back audit history through the Adjudicator port (memory recall, outcomes) gets empty results today; coverage matrices must read intent_audit/Postgres directly, not these port methods. Tamper-verification is usable now.

## [CONFIRMED] AnthropicProvider constructed inline (~1143) inside zero-parameter bootstrapClaustrum(); no DI seam; optional modelProvider override is purely in-repo
- evidence: claustrum-bootstrap.ts:1102 'export async function bootstrapClaustrum(): Promise<Conductor>' (no params); :1137-1151 'new Anthropic({...})' + 'const modelProvider = new AnthropicProvider({...})' inline; modelProvider feeds planner :1252-1254, responder :1258, grounding :1203. grep shows no override param anywhere
- consequence: Scripted-completion regression tests require adding an options param to bootstrapClaustrum (or a module-level setter) — ibatexas-side only, no @claustrum upstream change. Singleton guard at :1103 (if (_conductor) return) also needs a test reset hook.

## [CONFIRMED] Planner mints nonce: randomUUID() per express_intent; buildEnvelope accepts caller-supplied nonce (adopter-side deriveNonce seam); payload is in-hash and LLM-generated so deterministic nonce alone does not dedup re-planned redeliveries
- evidence: apps/api/src/claustrum/ibatexas-planner.ts:261-267 buildEnvelope({... payload: payload ?? {}, ... nonce: randomUUID()}) where payload comes from LLM tool call.input (:252). adjudicate/packages/core/src/envelope.ts:106-122 intentHashInput = {version,kind,payload,nonce,actor,taint}; :130-143 buildEnvelope hashes caller-supplied input.nonce; :61 'Adopter-supplied idempotency key. Part of the intentHash'
- consequence: Deterministic trigger nonces are a planner-deps change in ibatexas only — but because LLM-generated payload bytes are hashed, a re-planned redelivery can still differ. Host-level trigger dedup (BullMQ jobId / cooldown keys) remains a required second layer, as plan v1 line 58 states.

## [CONFIRMED] ANTHROPIC_MODEL controls BOTH planner (~1254) and responder (~813); fallback literal claude-opus-4-5-20250101; .env.example sets ANTHROPIC_MODEL=claude-sonnet-4-6
- evidence: claustrum-bootstrap.ts:813 and :1254 both read 'process.env.ANTHROPIC_MODEL ?? "claude-opus-4-5-20250101"' (exact literal, both sites). Root .env.example:8 'ANTHROPIC_MODEL=claude-sonnet-4-6           # LLM model for agent responses' (only the repo-root .env.example; apps/api has none)
- consequence: Planner and responder models cannot be pinned independently without a code change; any harness model-pinning design needs either two env vars (in-repo edit) or accepts shared pinning.

## [CONFIRMED] HandoffPort wired as noopHandoff (~648-661, 1260) — logger.warn only
- evidence: claustrum-bootstrap.ts:648-662 noopHandoff(): queue() does logger.warn('handoff queued (noop — Slack/PagerDuty not wired)') with '// TODO: wire Slack/PagerDuty'; :1260 'handoff: noopHandoff(),' in createConductor
- consequence: Journeys exercising escalation/handoff (e.g. JOURNEY-003 blocked_by handoff-port-noop) can only assert the warn log, not a queued artifact. blocked_by tagging in the plan is accurate.

## [CONFIRMED] sessionTokenBudgetGuard + confirmOnAutoResolveGuard composed adopter-level in compose-policy-packs.ts (~40-53, 92-118)
- evidence: apps/api/src/claustrum/compose-policy-packs.ts:40-53 sessionTokenBudgetGuard (createTokenBudgetGuard, action REFUSE, budget AGENT_SESSION_TOKEN_BUDGET default 100000 :35-38); :70-84 confirmOnAutoResolveGuard (createConfirmGuard over kinds order.cancel/payment.pix.regenerate/reservation.cancel :65-69); :92-94 IBATEXAS_ADOPTER_BUSINESS_GUARDS; :102-118 buildIbatexasPolicyPacks prepends to every pack's business phase
- consequence: Adopter guards run before every pack's own business guards; policy-manifest exporter consumes the same builder so manifests cannot drift. Token-budget and confirm-on-autoresolve refusals will appear in journey ledgers and must be modeled in expects.

## [CONFIRMED] TokenUsageStore / llm:tokens budget accounting — v1 says in-memory; find actual implementation
- evidence: TokenUsageStore: claustrum-bootstrap.ts:157-159 imports createInMemoryTokenUsageStore from @adjudicate/adapter-core; :682-689 constructed; impl is Map-backed LRU in-memory (adjudicate/packages/adapter-core/src/token-usage-store.ts:232-245). BUT it is telemetry-only (:671-677 'TELEMETRY ONLY... INDEPENDENT read'). The F4 BUDGET accounting is Redis-backed: write claustrum-bootstrap.ts:1019-1032 redis.multi().incrBy(sessionTokenKey,total).expire(...); read resolve-and-assemble.ts:36 rk(`llm:tokens:${channel}:${customerId}`), :44-56 readSessionTokensConsumed (fail-open to 0), :476 injected into state.ctx.sessionTokensConsumed
- consequence: v1's 'in-memory' is true only for the dashboard TokenUsageStore. Budget enforcement survives restarts and is shared across replicas (Redis). Harness can reset/read budgets via sessionTokenKey (exported); note the read is fail-OPEN to 0 on Redis errors.

## [CONFIRMED] buildSystemEnvelope uses deterministic nonce = eventId, principal 'system', taint SYSTEM
- evidence: apps/api/src/subscribers/__shared__/system-actor-envelope.ts:86-100: buildEnvelope({kind, payload, actor:{principal:"system", sessionId:`${args.sourceSubject}:${args.eventId}`}, taint:"SYSTEM", nonce: args.eventId})
- consequence: An existing in-repo precedent for deterministic trigger nonces — the trigger-bridge deriveNonce design should reuse this helper/convention (sourceSubject:eventId) rather than invent a new scheme.

## [CONFIRMED] CLAUDE.md rule #9 claims execution ledger is 'always-on and fail-closed' (contradicted by claim 1); CLAUDE.md references docs/PROJECT_STATE.md and docs/backlog/TODO-BACKLOG.md — verify existence
- evidence: CLAUDE.md:44 'The execution ledger (`@adjudicate/audit` + Redis) is always-on and fail-closed' — contradicted by claustrum-bootstrap.ts:1161-1165 (no ledger injected, TODO Stage 3). CLAUDE.md:7 and :9 reference both docs; 'ls' output: 'docs/PROJECT_STATE.md: No such file or directory', 'docs/backlog/TODO-BACKLOG.md: No such file or directory'; docs/ contains only agents architecture cli compliance ops security setup. Commit 1f1f459 'docs: minimize project docs' removed them
- consequence: CLAUDE.md is stale on the ledger invariant and on two doc links. Plan should not cite CLAUDE.md as ground truth for ledger status; a doc-fix task (rule #9 wording + dead links) is cheap and prevents future agents repeating v1's error.

## DISCOVERY
- **ibatexas injects NO SessionLock into createConductor — the Conductor falls back to the in-process InMemorySessionLock default. PostgresAdvisorySessionLock exists and is exported by the INSTALLED @claustrum/memory-postgres@0.1.0, and installed @claustrum/core@0.2.0 ConductorOptions already accepts optional sessionLock/sessionLockTimeoutMs.**
  - evidence: grep 'sessionLock' over apps/api/src → zero hits; claustrum-bootstrap.ts:1248-1271 createConductor call has no sessionLock key. claustrum/packages/core/src/conductor.ts:95 'options.sessionLock ?? new InMemorySessionLock()' (default timeout 10s :96); ports/session-lock.ts:14-18 'production implementation MUST be distributed'. apps/api/node_modules/@claustrum/core/dist/conductor.d.ts:60-62 (sessionLock?, sessionLockTimeoutMs?); apps/api/node_modules/@claustrum/memory-postgres/dist/index.d.ts:13 exports PostgresAdvisorySessionLock + advisoryLockId
  - consequence: Plan v1 leans on 'existing session locks' for per-entity serialization (line 125) — today that only holds within ONE api replica. Phase 3 trigger workers / multi-replica need PostgresAdvisorySessionLock injected (one-line change, deps already installed; pgPool already exists at bootstrap :1132).
- **Deep health endpoint GET /health already exists, returning {status, version (from package.json), timestamp, checks{redis,postgres,nats,typesense}, dlq?, outbox?} with 503 on critical failure — but no test-mode/fingerprint field today.**
  - evidence: apps/api/src/routes/health.ts:119-165 (route), :12 version from package.json, :145-152 response body, :163 'criticalFail ? 503 : 200'
  - consequence: The plan's environment-handshake (line 102) has a natural home: add a test-mode fingerprint field to this existing route gated on the test composition. No new endpoint needed; harness asserts the field plus hostname denylist.
- **SessionPort is fully implemented and Redis-backed (not a stub): redisSessionStore with load/save/parkPendingConfirmation/parkDeferred/unpark, JSON blobs under rk('claustrum:session:'+sessionId), guest vs customer TTLs, intentHash-deduped park lists, RMW serialized by the Conductor lock.**
  - evidence: claustrum-bootstrap.ts:881-993; key fn :859-861; TTL selection :896-898; park dedupe :957-961; comment :990-992 notes SessionPort.isStale removed upstream (claustrum commit 21c5393)
  - consequence: Confirmation-resume journeys have durable parked state to assert against (Redis key inspectable by harness). But the RMW race-freedom assumption explicitly depends on the per-session lock — which is in-memory only (discovery 1), compounding the multi-replica risk.
- **WebChannel.sink in the bootstrap is a no-op placeholder with an open TODO ('Replaced per-request by chat.ts via attachStream() pattern (TODO)'); real web delivery is a separate two-endpoint SSE design in routes/chat.ts (POST turn fire-and-forget + GET /api/chat/stream/:sessionId with reply.hijack(), buffered-chunk replay, CORS allowlist, 15s heartbeat).**
  - evidence: claustrum-bootstrap.ts:1235-1246 (no-op sink + TODO); apps/api/src/routes/chat.ts:5,18,27,68-69,111-130,342-372
  - consequence: The plan's programmatic chat client must implement POST-then-SSE with buffered replay semantics — confirming plan line 45. Channel-level render assertions cannot go through WebChannel.sink; they must consume the SSE stream.
- **TelemetryPort.emitLLMTrace is an explicit no-op (durable LLM-trace persistence deferred); only emitTurn does work (log line + Redis token fold + in-memory usage store).**
  - evidence: claustrum-bootstrap.ts:1058-1063 'return;' with comment 'Durable LLM-trace persistence ... is a follow-up'; emitTurn :1002-1057
  - consequence: If the harness wants per-turn prompt/completion capture for debugging or accuracy scoring, there is no existing persistence seam — it would piggyback on emitLLMTrace (port already exists, implementation is the gap).
- **safeAuditedAdjudicate wraps every kernel call fail-closed: malformed PolicyBundle or any kernel/audit throw becomes a pt-BR 'policy_not_ready' REFUSE, never a crash and never an ungated execute.**
  - evidence: claustrum-bootstrap.ts:354-366 isWellFormedPolicyBundle, :368-379 policyNotReadyRefusal, :381-432 safeAuditedAdjudicate try/catch → REFUSE
  - consequence: A mis-wired test composition manifests as SECURITY/policy_not_ready refusals in the ledger, not exceptions — journey failure triage should distinguish this signature from genuine policy refusals.
- **Two fail-closed BOOT gates run inside bootstrapClaustrum before traffic: assertConfigSealOrThrow over installed packs (no-op unless CONFIG_SEAL_DIGESTS pinned) and toolRosterDrift (throws on tool/pack-intent drift); plus assertAuditPostgresReady on the pg pool.**
  - evidence: claustrum-bootstrap.ts:1105-1108 (config seal), :1135 (audit pg readiness), :1176-1187 (roster drift throw)
  - consequence: Any test composition that boots the real bootstrap must satisfy Postgres-audit readiness and roster integrity; CONFIG_SEAL_DIGESTS gives an existing mechanism to fingerprint/pin pack config per environment — potentially reusable for the test-mode attestation.
- **WhatsApp channel is conditionally absent: WhatsAppChannel only pushed when TWILIO_ACCOUNT_SID/AUTH_TOKEN/FROM are all set, else logger.warn and the conductor runs web-only; WebChannel requires WEB_GATEWAY_SIGNING_KEY via requireSecret (fails closed when unset).**
  - evidence: claustrum-bootstrap.ts:1210-1230 (Twilio gating), :1240 requireSecret("WEB_GATEWAY_SIGNING_KEY")
  - consequence: Harness env must provision WEB_GATEWAY_SIGNING_KEY (boot fails without it) and can deliberately omit TWILIO_* to keep test runs web-only; WhatsApp-channel journeys need either Twilio fakes or env-gated skip.
- **Fresh CLI surface from the day-before-HEAD commit: 'ibx dev urls' + infraEndpoints() registry in packages/cli/src/services.ts (ports source of truth), commit c7cb988 (2026-06-10).**
  - evidence: packages/cli/src/services.ts:143 'export function infraEndpoints(): InfraEndpoint[]'; git show --stat c7cb988 (dev.ts +57, services.ts +29, .env.example +1)
  - consequence: The harness should resolve SUT base URLs/ports via infraEndpoints()/ibx dev urls instead of hardcoding — also a ready-made hook for the production-hostname denylist check.
- **sessionTokenKey is exported from resolve-and-assemble.ts as the single source of truth for the per-session LLM token counter key (write side imports it from the read side).**
  - evidence: apps/api/src/claustrum/resolve-and-assemble.ts:35-37 export function sessionTokenKey; claustrum-bootstrap.ts:155 imports it; :878-879 comment 'single source of truth ... so the key can never drift'
  - consequence: Token-budget journey fixtures can deterministically pre-load or reset a session's consumed-token counter through this exported key rather than reverse-engineering the rk() pattern.


# ===== packsTools =====

## [CONFIRMED] Exactly 17 LLM-callable mutating tools registered in apps/api/src/tools/register-ibatexas-tool-packs.ts; capability === intentKind enforced by toolRosterDrift() at boot; toolRosterDrift does NOT check planner-advertised ⊆ registered (deliberately not asserted).
- evidence: register-ibatexas-tool-packs.ts:224-376 IBATEXAS_TOOLS — grep count of makeTool/makeReservationTool = 17 (orders 10, reservations 4, onboarding 2, payments 1; section comments at :225,:309,:343,:361). toolRosterDrift at :423-445 checks cap!==kind and pack-ownership; :409-410 'deliberately does NOT assert the reverse (pack-owned ⊆ registered)' + TODO(WS4) :411. Boot fail-closed: claustrum-bootstrap.ts:1176-1187 (throws on drift); bootstrapClaustrum invoked apps/api/src/index.ts:93.
- consequence: Plan's Phase 0 item 'extend toolRosterDrift() to diff planner-advertised vs registered' is valid and the helper is pure/reusable (takes any tool list + intent-kind array), but note it diffs against pack.intents, not planner allowedIntents — the planner-advertised set is runtime-computed per session state.

## [CONFIRMED] payment.method.switch + payment.retry advertised by paymentsCapabilityPlanner (packages/pack-payments/src/capabilities.ts ~84-92) with no registered tool → tool_unresolved on EXECUTE; TODO(WS4).
- evidence: packages/pack-payments/src/capabilities.ts:84-88 allowedIntents = ['payment.pix.regenerate','payment.method.switch','payment.retry']; only payment.pix.regenerate registered (register-ibatexas-tool-packs.ts:367-375). tool_unresolved is a real dispatch failure code: claustrum/packages/core/src/execution/dispatch.ts:93,155,184,323,346. TODO(WS4) at register-ibatexas-tool-packs.ts:411-417.
- consequence: Phase 0 'fix or de-advertise' item stands, and the waived-pending-WS4 coverage-gate seed entries are correct — but the count of dangling planner-advertised kinds is 4, not 2 (see discovery: reservation.checkin/complete).

## [CONFIRMED] Stale INERT comments in register-ibatexas-tool-packs.ts (~32-38) and ibatexas-planner.ts (~43) contradicting live wiring (bootstrapClaustrum invoked at apps/api/src/index.ts:93).
- evidence: register-ibatexas-tool-packs.ts:32-38 '⚠️ INERT — registerIbatexasToolPacks() has no caller in the live graph (bootstrapClaustrum is not invoked at server start)' — false: index.ts:93 `await bootstrapClaustrum()`; claustrum-bootstrap.ts:1177 `registerIbatexasToolPacks(toolRegistry)`. ibatexas-planner.ts:43 'INERT until wired into createConductor at the composition root' — false: claustrum-bootstrap.ts:1252 `planner: createIbatexasPlanner({`.
- consequence: Phase 0 'delete stale INERT comments' is correct, low-risk. Note claustrum-bootstrap.ts:120 also says 'INERT until bootstrapClaustrum() is called' — that one is conditionally true, not stale; don't delete it.

## [CONFIRMED] customer-intent-gateway.ts checks actor.principal === 'user' and taint === 'UNTRUSTED' at runtime, 400 forgery_attempt + system-actor REFUSE audit on violation.
- evidence: apps/api/src/routes/__shared__/customer-intent-gateway.ts:253-260 detectForgery (principal !== 'user' / taint !== 'UNTRUSTED'); :415-443 returns statusCode 400 with code FORGERY_REJECTION_CODE ('forgery_attempt', :92) and decision kind REFUSE; emitForgeryAudit re-stamps principal 'system', taint 'SYSTEM', kind 'REFUSE' (:291-322, audit kind suffix '.forgery_rejected' :304).
- consequence: Confirms the plan's Amendment 2 rationale: testing agents must go through the public HTTP surface or they bypass real, load-bearing forgery defenses. Negative journeys can exactly assert 400 + forgery_attempt + a system-actor REFUSE audit record.

## [CONFIRMED] OrderProjection.version (optimistic concurrency) + OrderEventLog (append-only, unique idempotencyKey) exist in packages/domain/prisma/schema.prisma; canTransition/canTransitionPayment matrices in packages/types.
- evidence: schema.prisma:349 model OrderProjection; :368 'version Int @default(1) // optimistic concurrency'; :411 '/// Append-only event log for ALL order domain events'; :421 model OrderEventLog; :425 'idempotencyKey String @unique'. packages/types/src/order-status.ts:28 canTransition; packages/types/src/payment-status.ts:89 canTransitionPayment; exported index.ts:110,140.
- consequence: ProjectionBarrier primitive (awaitProjection polling OrderProjection.version / OrderEventLog) is buildable as planned. Caveat for oracle kit: OrderEventLog append-only has a documented LGPD erasure exception — payload may be overwritten to {anonymized:true} (schema.prisma:414-420); replay consumers must tolerate it.

## [BROKEN] MEDUSA SEED-STABILITY: Medusa entity ids (products, variants, carts) are deterministic across ibx db reset + reseed runs (v1 assertion, never verified).
- evidence: apps/commerce/src/seed.ts:125-150,180 — createProducts/createProductCategories/createPriceSets never pass an id; Medusa generates them. @medusajs/utils@2.13.5 dist/common/generate-entity-id.js: 'const id = (0, ulid_1.ulid)(); return `${prefix}${id}`' — ULID = timestamp + 80 random bits, fresh every run. ibx db reset: packages/cli/src/commands/db.ts:251 'DROP DATABASE IF EXISTS ... WITH (FORCE)' then re-migrate + reseed → all prod_/variant_ ids regenerate. Carts are runtime-created via the same mechanism. Stable keys are handles only: seed-data.ts fixes 42 product handles; packages/domain/src/seed-orders.ts:337-395 resolves Medusa ids BY HANDLE at seed time (buildProductLookup) precisely because ids drift.
- consequence: Recorded conversations referencing raw entity ids can NEVER replay against a re-seeded DB. Journey harness must resolve entities by handle/variant-title (the seed-orders.ts pattern) or maintain a runId-scoped id-remap; the plan must drop any assumption of id-stable replay and treat handles as the canonical join key.

## [CONFIRMED] ibx stripe complete --cart exists as the dev force-complete path for PIX carts; refuses in production-like env.
- evidence: packages/cli/src/commands/stripe.ts:184-191 — .command('complete') with --cart <cartId> and --all; ':189 if (process.env.NODE_ENV === "production") { console.error(... "Refusing to run in production — this is a dev rescue tool.")'; completes via POST /store/carts/:id/complete (:203). Listed in CLI help index.ts:247.
- consequence: Phase-1 scenarios can reach completed-cart states in dev without a Stripe webhook via ibx stripe complete --cart. Caveat: the guard is only NODE_ENV==='production' (no broader prod-like detection), and it bypasses the webhook→payment-status pipeline, so it does not produce a realistic 'paid' payment lifecycle — Phase-2 webhook fixture still needed.

## DISCOVERY
- **Two MORE dangling planner-advertised kinds the plan doesn't mention: reservation.checkin and reservation.complete are pushed into reservationsCapabilityPlanner allowedIntents for staff sessions but have NO registered tool (roster has only create/modify/cancel/waitlist.join).**
  - evidence: packages/pack-reservations/src/capabilities.ts:116-117 'if (isStaffSession) { allowedIntents.push("reservation.checkin", "reservation.complete") }'; grep for those kinds in register-ibatexas-tool-packs.ts returns nothing.
  - consequence: Phase 0 'fix or de-advertise the two dangling payment capabilities' undercounts: there are 4 dangling kinds (2 payments + 2 reservations-staff). The extended toolRosterDrift diff and the coverage-gate waiver seed list must include all four.
- **Inverse drift exists too: ibatexas.order.submitReview.v1 (order.review.submit) is registered but NEVER planner-advertised — ordersCapabilityPlanner allowedIntents omits it, so chat can never propose it.**
  - evidence: register-ibatexas-tool-packs.ts:300-307 registers it; packages/pack-orders/src/capabilities.ts:118-133 allowedIntents lists 9 kinds, no order.review.submit (only the tool-name mapping at :65,:87); it IS in ordersPack.intents (pack-orders/src/index.ts) so boot passes.
  - consequence: Journey coverage gate keyed on 'every registered tool covered by a chat journey' would mark tool #10 structurally uncoverable. Gate needs a third category (registered-but-unadvertised) or the planner gap fixed; the roster-drift extension should diff BOTH directions against planner-advertised sets.
- **@ibatexas/intent-kinds package exports KNOWN_INTENT_KINDS — a ReadonlySet<string> of 63 kinds (22 orders + 8 reservations + 4 whatsapp + 3 pix + 17 payments + 8 onboarding + 1 loyalty), including system-only kinds never LLM-proposable.**
  - evidence: packages/intent-kinds/src/index.ts:206-214 (KNOWN_INTENT_KINDS), :202 (LOYALTY_INTENT_KINDS); count comment :181-188. Consumed by apps/api/src/plugins/kernel-bootstrap.ts and packages/cli/src/commands/kernel.ts.
  - consequence: The journey roster gate's 'expects.intentKind ∈ KNOWN_INTENT_KINDS / listIbatexasToolPacks()' conflates three different sets: KNOWN_INTENT_KINDS (63, incl. webhook/system kinds journeys legitimately expect), pack.intents union, and the 17 registered tools. Gate spec must name which set validates which assertion type.
- **An existing boot-time coverage gate already exists: assertPackCoverage() in apps/api/src/plugins/kernel-bootstrap.ts refuses to boot when registered intent kinds lack pack policy coverage.**
  - evidence: apps/api/src/plugins/kernel-bootstrap.ts:264 'export function assertPackCoverage(', invoked at :366 with PACK_REGISTERED_INTENT_KINDS; comment :16.
  - consequence: The plan's journey coverage gate has a precedent pattern + naming convention in-repo to mirror (and a place to register the journey gate alongside); don't re-invent the waiver/refuse-to-boot mechanics.
- **IBATEXAS_POLICY_PACKS is module-private (not exported) in claustrum-bootstrap.ts; the existing roster test reconstructs the pack-intent union by importing the five packs directly.**
  - evidence: claustrum-bootstrap.ts:696 'const IBATEXAS_POLICY_PACKS' (no export); apps/api/src/__tests__/tool-roster-integrity.test.ts:29-38 builds PACK_INTENT_UNION from ordersPack/paymentsPack/reservationsPack/customerOnboardingPack/whatsappPack imports.
  - consequence: The journey roster gate cannot import the composed pack list; it must either replicate the five-pack import (drift surface) or the plan should add a small export of the pack list/intent union from a shared module — a concrete Phase 0/1 task.
- **`ibx chat scenarios` still appears in CLI help but the subcommand was deleted in the WS8 cutover; no conversation-E2E harness exists at all.**
  - evidence: packages/cli/src/index.ts:204 lists 'chat scenarios — Run E2E conversation test scenarios'; packages/cli/src/commands/chat.ts:345-351 'the former chat scenarios subcommand and its runScenarios runner were removed... No replacement exists, so the command had no subject.'
  - consequence: Confirms plan gap #3 and frees the `ibx journey` namespace; also a stale-help cleanup item. The deleted runner's fixture format should NOT be resurrected (it imported legacy llm-provider internals).
- **Existing deterministic data engines the plan partially credits: ibx scenario YAMLs live at packages/cli/scenarios/*.yml (8 files: base-customers, base-products, customer, homepage-realistic, homepage, intelligence, recommendations, search) — none cover pending_payment or paid order states; plus ibx simulate (lib/simulator.ts) generates seeded-PRNG deterministic customers/orders/reviews via Prisma upserts.**
  - evidence: ls packages/cli/scenarios/ → 8 .yml files; packages/cli/src/lib/simulator.ts:1-11 'Deterministic: same seed → same customers, orders, reviews, timestamps... Write everything to DB via Prisma upserts (idempotent)'.
  - consequence: Paid-order scenario YAML is genuinely missing (plan's Phase-2 webhook fixture stands), but base-customer/product state setup should reuse the existing scenario engine + schema (packages/cli/src/lib/scenario-{engine,schema}.ts) rather than new harness seeding code.
- **Existing order-history fixture with deterministic synthetic order ids: packages/domain/src/seed-orders.ts (ibx db seed:orders) seeds 12 CustomerOrderItem orders with fixed ids 'seed-order-hist-01..12' + 8 reservations across all statuses (completed/confirmed/pending/cancelled/no_show/seated), resolving Medusa product/variant ids by handle at runtime.**
  - evidence: packages/domain/src/seed-orders.ts:117-243 ORDER_TEMPLATES with orderId 'seed-order-hist-NN'; :251 reservation statuses union; :349-415 seedOrderItems resolves via buildProductLookup(handle→id); package.json:25 db:seed:orders; CLI db.ts:920.
  - consequence: Journeys needing past-order context (reorder, review, 'my last order') have a ready deterministic fixture; these are CustomerOrderItem history rows, NOT OrderProjection lifecycle states — paid/pending_payment OrderProjection fixtures still don't exist.
- **An OrderProjection factory helper already exists: ibx db backfill (runBackfillOrderProjections) builds OrderProjection rows from Medusa orders via toOrderProjectionData() exported from @ibatexas/domain, including OrderStatusHistory backfill.**
  - evidence: packages/cli/src/commands/db.ts:649-706 (toOrderProjectionData import :657, orderProjection.upsert :706) and :790-857 (status-history backfill, 'Historical backfill from OrderProjection' :851).
  - consequence: The Phase-2 paid-order fixture should reuse toOrderProjectionData rather than hand-rolling projection rows; it's the canonical Medusa-order→projection mapping.
- **No fake-Twilio seam exists at the HTTP level: routes/auth.ts always calls the real Twilio Verify client (verifications.create / verificationChecks.create) and config.ts hard-requires TWILIO_ACCOUNT_SID/AUTH_TOKEN/VERIFY_SID; Twilio is only mocked in vitest module mocks.**
  - evidence: apps/api/src/routes/auth.ts:127,139,150,157 (real client calls, no env bypass; grep for mock/stub/bypass in auth.ts hits only unrelated comments); apps/api/src/config.ts:29-31 required zod fields.
  - consequence: Plan's Phase-1 'authFixture — test-OTP via the existing fake-Twilio seam' references a seam that does not exist; building it (env-gated stub verify provider or test-only OTP route) is unbudgeted Phase-0/1 work and a prerequisite for any authenticated journey.
- **tests/e2e is nearly empty: fixtures/ contains only .gitkeep; specs are api-golden-path.spec.ts, web-golden-path.spec.ts, smoke.spec.ts. ibx stripe trigger shells out to the real `stripe` CLI binary (no self-contained webhook fixture).**
  - evidence: ls -la tests/e2e/fixtures/ → only .gitkeep; ls tests/e2e → 3 specs + fixtures; packages/cli/src/commands/stripe.ts:169 execa('stripe', ['trigger', eventType]).
  - consequence: Confirms greenfield for the journey harness; the Phase-2 'Stripe/PIX webhook fixture' cannot lean on ibx stripe trigger in CI without the Stripe CLI + keys — a synthetic signed-webhook fixture posting to the API is the CI-safe route.


# ===== cliTests =====

## [CONFIRMED] ibx api chat implements POST /api/chat/messages + SSE parsing + --session reuse BUT ignores returned sessionSecret/sessionToken and never sends x-session-secret/x-session-token — guest session reuse 403s today
- evidence: packages/cli/src/commands/api.ts:116-133 postChatMessage sends only Content-Type and reads only {messageId}; :171-192 streamChatResponse fetch with no headers; :459 --session option. Server: apps/api/src/routes/chat.ts:195-215 guest secret minted first POST, 403 'Segredo de sessão inválido' on mismatch; :327-331 response includes sessionSecret/sessionToken; :388-398 SSE GET also rejects ('Acesso negado.') when secret exists and x-session-secret missing.
- consequence: Stronger than stated: not only --session reuse — even the FIRST guest run breaks, because the POST mints a secret and the headerless SSE GET is then rejected. The ChatClient cannot be a thin lift of this code; it must thread sessionSecret/sessionToken through both POST and GET.

## [CONFIRMED] ibx CLI top-level command count is 30 (24 grouped + 6 root), not 31; kernel/scenario/matrix/simulate present; no existing 'journey' command
- evidence: packages/cli/src/index.ts:345-369 groupedCommands array has exactly 24 entries (incl. kernel:365, scenario:351, matrix:354, simulate:355); :380 rootRegistrations = [bootstrap, db, env, git, intelligence, tunnel] = 6, each registering exactly one top-level command (verified per-file). grep -rn 'journey' over CLI src: zero hits; repo-wide only tests/e2e/web-golden-path.spec.ts.
- consequence: Plan's '31 commands' should read 30. 'journey' namespace is free. Note CLAUDE.md says '24 commands' — both docs are wrong in different directions; trust index.ts.

## [CONFIRMED] ibx kernel replay reads intent_audit via readAuditWindow, re-adjudicates with current-checkout packs, classifies drift — but exit code 1 only on ERRORS (drift does NOT fail), no --json, adjudicates against EMPTY state, prints stub + exit 0 when IBX_AUDIT_POSTGRES_ENABLED unset, @adjudicate/pack-payments-pix npm-pinned
- evidence: packages/cli/src/commands/kernel.ts:255,291 readAuditWindow over intent_audit SQL; :413-449 drift classification (DECISION_KIND/BASIS/PAYLOAD); :403-407 adjudicate(envelope, {} as never, pack.policy) — empty state; :210-242 stub + process.exitCode=0 when flag unset; :1313-1320 replay options are only --since/--intent-kind/--limit/--dry-run (no --json); :335-345 only thrown exceptions set exitCode=1 — printReplayReport(:504-543) sets nothing, drift AND the report.errors bucket both exit 0. packages/cli/package.json:43 "@adjudicate/pack-payments-pix": "^0.2.1"; pnpm-workspace.yaml:5 confirms @adjudicate/* from npm registry.
- consequence: One refinement: even the report's error bucket (pack missing/parse) exits 0 — only DB/connection exceptions fail. Promoting replay to a policy-diff CI gate needs: drift+error→nonzero exit, --json output, and adjudicate-repo edits are invisible until a version bump + pnpm install (CI must install the PR-built tarball or use overrides).

## [CONFIRMED] ibx scenario engine: zod ScenarioFileSchema, closed StepNameSchema of 9 data-state steps, hardcoded phase order cleanup→setup→simulate→tags→rebuilds→verify, verify keys hardcoded (Prisma counts/Redis/Typesense/GET-only api:), no HTTP/chat act concept; runPipeline fully generic
- evidence: packages/cli/src/lib/scenario-schema.ts:8-18 StepNameSchema z.enum of exactly 9 steps; :62-78 ScenarioFileSchema. scenario-engine.ts:614-620 executeScenarioSteps runs cleanupPhase→setupPhase→simulatePhase→tagsPhase→rebuildsPhase→verifyPhase in fixed order; :415-458 runSingleCheck hardcoded key dispatcher (7 Prisma counts, Typesense checkProducts :287-298, Redis ranking/global-score :386-411, api: GET-only fetch :368-384 with no method option). No chat/HTTP-act step exists. pipeline.ts:9-14,112-156 PipelineTask{name,label,run} + runPipeline fully generic (used by test seed/integration too).
- consequence: Confirms the plan's call to give journeys a sibling schema + reuse runPipeline rather than widen StepNameSchema. The verify dispatcher is the seed of the oracle kit's deterministic validators but is warning-severity for api: keys and engine-internal — extraction, not import, is needed.

## [CONFIRMED] Vitest file count ~298 (excluding node_modules), plus 3 Playwright specs in tests/e2e
- evidence: find (pruning node_modules) -name '*.test.ts' -o -name '*.test.tsx' → 298. Only .spec.ts files: tests/e2e/{web-golden-path,smoke,api-golden-path}.spec.ts = 3. Combined 301.
- consequence: Plan's '294 vitest files' figure in section 3 is slightly stale (298 actual); the '~298 + 3 Playwright' framing is exact at HEAD 01d2d0a.

## [CONFIRMED] packages/domain (16 test files) + packages/types (7) have NO test script in package.json — turbo test / CI never runs them; types lacks vitest devDep
- evidence: packages/domain/package.json scripts: build/lint/db:* only, no 'test' (vitest IS in devDeps). packages/types/package.json scripts: build/lint only; devDeps only typescript (no vitest). find: domain=16, types=7 *.test.ts files. Root package.json:9 "test": "turbo test"; .github/workflows/ci.yml:30 'pnpm test -- -- --coverage'; turbo.json:23-27 test task runs per-package 'test' scripts — absent script = silently skipped.
- consequence: 23 test files never execute in CI, including domain's no-direct-prisma-bypass.test.ts territory. Phase-0 hygiene fix is two one-line script additions + one devDep; cheap and high-value, plan correctly lists it.

## [CONFIRMED] scripts/check-bypass.sh step 3 filters @ibatexas/llm-provider (deleted) — pnpm exits 0 on no-match so the leg passes silently despite set -euo pipefail
- evidence: scripts/check-bypass.sh:67-69 'pnpm --filter @ibatexas/llm-provider exec vitest run src/__tests__/no-execute-tool-direct.test.ts'; ls packages/ shows no llm-provider. Empirical: `pnpm --filter @ibatexas/llm-provider exec echo would-run` prints 'No projects matched the filters in "/Users/thaisrodolpho/projects/ibatexas"' and 'exit code: 0' — so set -euo pipefail does not trip.
- consequence: A third of the bypass gate is dead and reports success. Fix: delete the leg (its assertion is meaningless post-cutover) or move the executeToolDirect check into the api-side bypass-detection suite; also consider pnpm's --fail-if-no-match guard pattern for the remaining legs.

## [CONFIRMED] Playwright wired into NO CI workflow; tests/e2e/fixtures empty
- evidence: grep -rln playwright .github/ → zero hits across all 9 workflows (branch-naming, ci, cleanup-branches, codeql, deploy-staging, deploy, override-drift, secret-scan, upgrade-radar). ls -la tests/e2e/fixtures → only .gitkeep (0 bytes). playwright.config.ts exists at repo root; @playwright/test ^1.58.2 in root package.json:18.
- consequence: Plan's Phase-2 'plain Playwright specs in CI' starts from zero CI wiring but a working local runner exists (`ibx test e2e-run`, see discovery). The 3 existing specs have never gated anything.

## DISCOVERY
- **ibx chat dump <sessionId> --json (--source redis|postgres) already extracts full machine-readable transcripts; ibx chat list enumerates active sessions with channel inference; ibx chat clean deletes session state**
  - evidence: packages/cli/src/commands/chat.ts:135-214 (runDump with JSON.stringify output paths at :166 and :201), :37-108 (runList), :359-378 (command registration)
  - consequence: The plan's `ibx journey from-audit` scaffolder and transcript-harvesting can reuse this today — transcript extraction is solved; only the audit-slice → expects mapping is new work.
- **ibx obs turn <turnId> traces one full turn by correlationId, and obs decisions/payments/funnel watch kernel decisions — all backed by VictoriaLogs, with log intentHash documented as joining 1:1 to the intent_audit row**
  - evidence: packages/cli/src/commands/obs.ts:1-9 (header: 'intentHash joins 1:1 to the durable intent_audit row; correlationId groups one turn'), :68-87 (turn), :27-65 (decisions)
  - consequence: Ready oracle shortcut for journey failure triage, but it depends on VictoriaLogs being up — the nightly test composition must include it or these debug paths silently return 'no lines'. Postgres intent_audit remains the assertable source; obs is the human-debug view.
- **An existing JSONL trace convention: lib/events.ts emits typed ScenarioEvents as JSONL to stderr when IBX_EVENTS=json ('ibx scenario homepage 2> events.jsonl'), plus an onEvent listener API**
  - evidence: packages/cli/src/lib/events.ts:7-21 (ScenarioEventType union), :58-72 (emit with IBX_EVENTS=json stderr path), :42-48 (onEvent)
  - consequence: The plan's 'JSONL trace per run' should extend this event union (journey.start, act.*, llm.call, evidence.*) rather than invent a parallel format — CI consumers and the emit/listener plumbing already exist.
- **Step-input caching exists: .ibx/cache/steps/<step>.json keyed on sha256 of declared seed-file inputs, skipping unchanged seed steps**
  - evidence: packages/cli/src/lib/step-cache.ts:13 (CACHE_DIR), :26-36 (STEP_INPUT_FILES map), :77-108 (isStepCached/cacheStep)
  - consequence: Journey runner inherits free seed-step skipping via stepsToTasks, but STEP_INPUT_FILES is a closed Record<StepName,...> — any new step name forces a map entry, another reason journeys should not extend StepNameSchema.
- **The scenario lock is a single GLOBAL Redis lock, and its implementation contradicts its own comment: claims 'SET NX EX' but does GET-then-SET without NX (TOCTOU race); release is PID-checked plain DEL, not the Lua-conditional pattern CLAUDE.md rule #10 mandates**
  - evidence: packages/cli/src/lib/lock.ts:3 (comment 'Uses SET NX EX 300'), :28-50 (redis.get then redis.set(key, ..., {EX}) — no NX flag), :53-72 (release via del); LOCK_KEY 'ibx:scenario:lock' is scenario-name-agnostic
  - consequence: If journeys reuse acquireScenarioLock, all journeys serialize behind one global key (fine for nightly-sequential, blocks any parallelism) and the race means two runners can both 'acquire'. Journey runner should take per-journey/per-runId keys with real SET NX.
- **guardDestructive — the only existing production-safety rail — checks only NODE_ENV/APP_ENV === 'production' and exits**
  - evidence: packages/cli/src/lib/pipeline.ts:38-46
  - consequence: Exists but is far weaker than the plan's Phase-1-blocker environment handshake (test-mode fingerprint + hostname denylist). The plan correctly treats the handshake as new work; do not let guardDestructive be mistaken for it.
- **ibx test e2e-run already wraps Playwright (filter/--headed/--ui, flag passthrough), and ibx test e2e is the destructive clean+full-seed pipeline; ibx test seed/integration build the seed pipeline on runPipeline**
  - evidence: packages/cli/src/commands/test.ts:360-391 (e2e-run → execa npx playwright test), :323-358 (e2e destructive), :267-281 (seed via buildSeedPipeline + runPipeline)
  - consequence: Phase-2 'Playwright in CI' is a workflow-file change plus `ibx test seed` invocation — the local entrypoints exist. The 'seed once per CI job' step the plan specifies is literally `ibx test seed`.
- **ibx stripe trigger [event] fires test webhooks (via the Stripe CLI binary) and ibx stripe complete force-completes orphaned PIX/card carts as a dev rescue (NODE_ENV=production refused)**
  - evidence: packages/cli/src/commands/stripe.ts:161-178 (trigger → execa stripe trigger), :184-200 (complete, prod refusal)
  - consequence: The plan's Phase-2 'Stripe webhook fixture for paid states' has partial building blocks — but `stripe trigger` needs the external stripe binary + listen session, so a CI-grade fixture (locally-signed webhook POST) is still new work; `complete` shows the cart-finalization seam to imitate.
- **ibx auth create-staff creates Staff rows directly via Prisma (OWNER/MANAGER/ATTENDANT) and auth flush/status manage OTP rate-limit state; no JWT minting exists anywhere in the CLI**
  - evidence: packages/cli/src/commands/auth.ts:169-219 (create-staff via prisma.staff.create), :114,145 (flush/status)
  - consequence: Seeding staff identities for the authFixture is solved; the offline JWT-minting half of the plan's auth approach is confirmed greenfield — nothing in the repo signs customer or staff tokens outside the API.
- **ibx kernel defer and ibx kernel resume <sessionId> subcommands exist alongside status/replay/migrate/pack-bom/analyze/seal**
  - evidence: packages/cli/src/commands/kernel.ts:1352 (.command("defer")), :1358 (.command("resume <sessionId>")), :1305-1421 (full subcommand roster)
  - consequence: A CLI park/resume surface already exists for deferred envelopes — the Phase-3 approvals glue and any confirmation-resume journey should inspect/reuse this resume path before building a new one.
- **Governance gates with --json + digest baselines already exist: ibx kernel status --json, kernel pack-bom --json --verify-file against governance/pack-bom-baseline.json, kernel analyze --json --sarif, kernel seal, plus ibx policy export/diff for the rule-provenance manifest**
  - evidence: packages/cli/src/commands/kernel.ts:1307-1308, 1395-1415, 1259-1267 (digest-lock note); packages/cli/src/commands/policy.ts:162,177
  - consequence: The journey roster/coverage gates have an exact in-repo idiom to copy (checked-in baseline + --verify-file diff + nonzero exit). Replay is the outlier lacking --json — the pattern to retrofit is already next to it in the same file.
- **CLAUDE.md's CLI reference line is stale ('Full CLI reference (24 commands)') versus 30 actual top-level commands**
  - evidence: /Users/thaisrodolpho/projects/ibatexas/CLAUDE.md table row 'Full CLI reference (24 commands)' vs packages/cli/src/index.ts:345-382 (24 grouped + 6 root)
  - consequence: Adds to the plan's Phase-0 doc-hygiene list; any agent reading CLAUDE.md undercounts the surface it is told to extend.


# ===== infraCi =====

## [CONFIRMED] NATS runs with NO server-side auth in ALL deployments: docker-compose.yml (~64-77), docker-compose.prod.yml (~148-168), infra/terraform production nats — command is just --jetstream --store_dir -m 8222; no accounts/users/permissions
- evidence: docker-compose.yml:67 command ["--jetstream","--store_dir","/data","-m","8222"]; docker-compose.prod.yml:153 identical command (nats service at :148-168); infra/terraform/environments/production/nats.tf task definition containerDefinitions command = ["--jetstream","--store_dir","/data","-m","8222"] (image nats:2.11-alpine, line ~24-27). FOURTH surface the claim missed: infra/terraform/environments/dev/compose.yml.tpl:134-139 nats command ["-js"] — also no auth, no monitoring port. No accounts/users/permissions/config file in any of the four.
- consequence: Server-side NATS auth must be added to four surfaces, not three: dev compose, prod compose, terraform production task def, and the dev-EC2 compose template. Any plan item should enumerate all four.

## [CONFIRMED] Client creds plumbing exists (NATS_CREDS_PATH/NKEY env + prod fail-closed boot check) but servers cannot validate
- evidence: packages/nats-client/src/index.ts:62 reads NATS_CREDS_PATH, :67 reads NATS_NKEY_SEED (exact var name is NATS_NKEY_SEED, not NATS_NKEY); fail-closed block at index.ts:148-162: if NODE_ENV===production && no authenticator && no tls → console.error + throw. .env.example:161-165 documents NATS_CREDS_PATH, NATS_NKEY_SEED, NATS_TLS_CA, NATS_TLS_REQUIRED. Test exists: packages/nats-client/src/__tests__/nats-auth-fail-closed.test.ts.
- consequence: Client side is done and fail-closed; only server-side provisioning (nsc/accounts or token) is the gap. Exact env var is NATS_NKEY_SEED — plan should use the precise name.

## [STALE] docs/adjudicate-migration/remediation/NATS-AUTH-REQUIREMENTS.md referenced but absent
- evidence: Referenced path is absent (`ls docs/adjudicate-migration` → no such dir) and is cited in nats-client/src/index.ts:157-158 and .env.example:158-159. BUT the doc EXISTS at docs/security/NATS-AUTH-REQUIREMENTS.md — it carries a 2026-05-24 'PARTIALLY SUPERSEDED' banner stating the nsc/.creds/nkey/TLS operational steps remain valid live operator guidance.
- consequence: Do not plan to write a new NATS-auth runbook — one exists at docs/security/. Plan item shrinks to: fix the two stale path references and execute the existing runbook's server-side steps.

## [CONFIRMED] CI (.github/workflows/): vitest+coverage with Docker probe, check-bypass.sh, kernel pack-bom/analyze gates, Sonar all in CI
- evidence: 9 workflows total: branch-naming, ci, cleanup-branches, codeql, deploy-staging, deploy, override-drift, secret-scan, upgrade-radar. ci.yml has ONE job `check` (line 10): docker info probe :28-29, `pnpm test -- -- --coverage` :30, `./scripts/check-bypass.sh` :37, `pnpm audit --audit-level=high` :39, build :40, `kernel pack-bom --verify-file packages/cli/governance/pack-bom-baseline.json` :46, `kernel analyze` :48, SonarCloud scan :49-52. Other jobs: deploy.yml {build,migrate,deploy,health-check}; deploy-staging.yml {build,migrate,deploy,health-check}; codeql {analyze}; override-drift {check-overrides}; secret-scan {gitleaks}; branch-naming {check-branch-name}; cleanup-branches {delete-branch}; upgrade-radar {radar}.
- consequence: Plan's CI baseline is accurate; new gates would slot into the single `check` job or a new job in ci.yml.

## [CONFIRMED] NO nightly workflow, NO Playwright job, NO workflow that boots the full stack
- evidence: Only two schedules exist, both WEEKLY: codeql.yml:8-10 cron "17 8 * * 1", upgrade-radar.yml:4-5 cron "0 9 * * 1". grep playwright across .github/workflows/* → zero hits. Deploy workflows run remotely (deploy-staging.yml:131-142 SSM send-command running /usr/local/bin/ibatexas-deploy on EC2; deploy.yml ECS) — nothing in CI ever runs docker compose up or process-compose for the app stack.
- consequence: A nightly full-stack workflow and a Playwright CI job are genuinely greenfield; nothing to retrofit, only the existing single-job ci.yml pattern to extend.

## [CONFIRMED] Full-stack local env services and ports: Postgres 5433, Redis 6379, NATS 4222, Typesense 8108, Medusa 9000, api 3001, web 3000, admin 3002; readiness probes exist
- evidence: docker-compose.yml:11 "127.0.0.1:${POSTGRES_PORT:-5433}:5432" (port is env-overridable), :30 redis 6379 (with --requirepass ${REDIS_PASSWORD:-localdev} :28), :51 typesense 8108, :69-70 nats 4222/8222 — all four have healthchecks. process-compose.yaml: infra one-shot `docker compose up -d --wait` :14 with exec readiness probe :17-23; commerce http_get :9000/health (:52-55); api http_get :3001/health (:76-79); web :3000/ (:107-110); admin :3002/ (:131-134); plus build-packages, web-clean/admin-clean, tunnel (ngrok), stripe listen. No docker-compose.test.yml exists (`ls` → No such file). Prod compose runs api on PORT 9000 (docker-compose.prod.yml:19), not 3001.
- consequence: A test composition can reuse the per-service healthchecks and the /health-based readiness probes verbatim; it must supply POSTGRES_PORT, REDIS_PASSWORD (Redis is password-protected even locally), TYPESENSE_API_KEY, and explicit JWT secrets, and note api port differs between dev (3001) and prod compose (9000).

## [CONFIRMED] JWT secrets contract: JWT_SECRET + STAFF_JWT_SECRET in .env.example; aud:'token' / aud:'staff_token'; test-env secret conventions exist
- evidence: .env.example:275 JWT_SECRET (min 32 chars), :279 STAFF_JWT_SECRET (MUST differ). apps/api/src/server.ts:70-92: requireSecret("JWT_SECRET") with verify.allowedAud:"token"; requireSecret("STAFF_JWT_SECRET") namespace "staff" allowedAud:"staff_token"; throws at boot if the two secrets are equal. Issuance: routes/auth.ts:181 aud:"token", :203 aud:"staff_token"; middleware/auth.ts:62 COOKIE_TO_EXPECTED_AUD binds cookie name to aud. Test convention: vi.stubEnv("JWT_SECRET","test-jwt-secret-key") / ("STAFF_JWT_SECRET","test-staff-jwt-secret-key") in apps/api/src/__tests__/staff-auth-routes.test.ts:128-129. utils/require-secret.ts:8-24: minLength 32 enforced when value present; NODE_ENV===test && missing → random 32-byte secret generated per process.
- consequence: Multi-process test stack MUST set explicit >=32-char, mutually distinct JWT_SECRET/STAFF_JWT_SECRET — relying on the test fallback gives each process a different random secret, breaking cross-service token verification.

## [CONFIRMED] apps/api exposes /health and it could carry a test-mode fingerprint
- evidence: apps/api/src/routes/health.ts:119-165: GET /health (rateLimit:false, silent logging) returns {status: healthy|degraded|unhealthy, version (from apps/api package.json via createRequire, line 12), timestamp, checks:{redis,postgres,nats,typesense}, dlq?, outbox?}; 503 only when redis or postgres fail; nats/typesense/DLQ/outbox backlog → degraded. No env/test-mode field today. Medusa exposes its own :9000/health returning "OK" (packages/cli/src/services.ts:48-49 healthExpect).
- consequence: Adding e.g. an `env: APP_ENV` field to the HealthResponse is a one-line, low-risk fingerprint carrier; the endpoint already doubles as the readiness probe in process-compose and playwright webServer.

## [CONFIRMED] BullMQ usage inventory (queue.ts, defer-timeout-sweeper, pix-expiry jobs) — jobId-based dedup available for trigger-side dedup
- evidence: apps/api/src/jobs/queue.ts:22-31 createQueue(name) with hardcoded prefix "ibx" (:17) and defaultJobOptions removeOnComplete/Fail; createWorker :39-68 concurrency 1 + swallowing error listener. defer-timeout-sweeper.ts:666 createQueue("defer-timeout-sweeper"), :697 upsertJobScheduler("defer-timeout-sweeper-repeat") + startup recovery scan (:433-439, :679). pix-expiry-checker.ts:202/215 same pattern ("pix-expiry-checker"/"pix-expiry-repeat"). jobId dedup is ALREADY USED in-repo: stripe-webhook-processor.ts:86 `jobId: event.id` with comment ':15 jobId = event.id → BullMQ-level dedup across re-deliveries'. ~14 job files under apps/api/src/jobs/.
- consequence: Trigger-side dedup via queue.add(name, data, {jobId}) is proven in-repo (stripe processor) and works on the existing createQueue instances; no new infra needed.

## [CONFIRMED] Redis usage: rk() prefixing with APP_ENV namespacing — per-run namespaces would work without code change
- evidence: packages/tools/src/redis/key.ts:14 `const ENV_PREFIX = process.env.APP_ENV ?? "development"` (captured once at module load, no value whitelist despite comment), :22 rk(key) → `${ENV_PREFIX}:${key}`; :10-12 fail-fast only when NODE_ENV=production && !APP_ENV. nats-client outbox keys also use APP_ENV (index.ts:302), as does health.ts:105/111. CAVEAT: BullMQ keys are NOT covered — queue.ts:17 hardcodes prefix "ibx" independent of APP_ENV, so per-run APP_ENV isolates rk()-routed keys but NOT BullMQ queues; full isolation needs a per-run REDIS_URL DB index (queue.ts:11-15 takes {url}) or a one-line prefix change.
- consequence: Per-run namespace = set APP_ENV=<run-id> before process boot (works, any string accepted). Plan must additionally isolate BullMQ via per-run REDIS_URL database number or make PREFIX APP_ENV-aware — otherwise concurrent runs share job queues.

## DISCOVERY
- **`ibx test` command group already implements the seeding/reset lifecycle a test composition needs: `test seed` (full pipeline products→reindex→domain→homepage→delivery→orders→reviews→intel), `test integration` (idempotent — skips product seed if present), `test e2e` (destructive cleanAllData + full reseed, confirmation-gated), `test e2e-run` (wraps `npx playwright test`), `test status`**
  - evidence: packages/cli/src/commands/test.ts:263-392 (seed :267, integration :285, e2e :323, e2e-run :360, status :393); seed steps also invoked by ibx bootstrap (commands/bootstrap.ts:150-163) and ibx db (db.ts:60-140)
  - consequence: The plan's test-composition seeding should shell out to `ibx test e2e --force` / `ibx test integration` rather than inventing a new seed path; canonicalization concerns (Medusa ids not seed-stable) remain but the pipeline itself exists.
- **guardDestructive() blocks destructive CLI ops (db reset, test e2e) only when NODE_ENV or APP_ENV === 'production'**
  - evidence: packages/cli/src/lib/pipeline.ts:38-46; invoked at db.ts:223 and test.ts:328
  - consequence: A test composition running with APP_ENV=test (or any non-production value) passes the guard — destructive reseed is permitted by design, no override flag needed.
- **Two real-container test harnesses exist with documented policy: redis-testcontainer.ts (Redis 7 per test file, ephemeral port, IBX_SKIP_REAL_REDIS=1 local opt-out, 'CI must run real Redis' fail-default) and h3-postgres-container.ts (postgres:15-alpine + `prisma migrate deploy`, repoints the lazy `@ibatexas/domain` prisma singleton via DATABASE_URL, ~10-20s migrate amortized per file, truncateDomainTables() per-test reset)**
  - evidence: apps/api/src/__tests__/helpers/redis-testcontainer.ts:1-50; apps/api/src/__tests__/audit-2026-05-24/h3-postgres-container.ts:1-40; apps/api/package.json:76 testcontainers ^12.0.0
  - consequence: Reusable patterns for any new integration layer: lazy-singleton env repointing + truncate helper. Note drift: harness uses postgres:15-alpine 'matching production' but docker-compose.yml runs postgres:17-alpine — align when touched.
- **SonarCloud config has a fixed sources whitelist and excludes packages/cli entirely from coverage; lcov collected from apps/*/coverage and packages/*/coverage**
  - evidence: sonar-project.properties: sonar.sources enumerates 12 src dirs; sonar.coverage.exclusions includes packages/cli/**, packages/types/**, **/seed-*.ts; sonar.javascript.lcov.reportPaths=apps/*/coverage/lcov.info,packages/*/coverage/lcov.info
  - consequence: Any new package (e.g. a journeys/harness package) is invisible to Sonar until added to sonar.sources; putting harness code under packages/cli keeps it out of coverage gates by existing convention.
- **.env loading model: apps/api never loads dotenv (no import anywhere in apps/api/src) — env is injected by process-compose (see the BKL-249 correction below), docker compose env_file, or the shell; only the ibx CLI loads dotenv with precedence shell > root .env > cli .env. playwright.config.ts webServer launches `pnpm --filter @ibatexas/{web,api} dev` directly, inheriting only shell env**
  - evidence: grep dotenv apps/api/src → 0 hits; process-compose.yaml (top-level env_file, since removed — see correction); packages/cli/src/index.ts:6,40; playwright.config.ts webServer block (api url ${API_URL}/health, reuseExistingServer: !process.env.CI)
  - CORRECTION (BKL-249, 2026-07-26): the mechanism named above was wrong. process-compose SILENTLY IGNORES a top-level `env_file:` key, so it never injected anything; the dev `.env` reached processes via process-compose's DEFAULT env-file list ([.env], the `-e` flag), which binds with no yaml key at all. The dead key additionally made `process-compose --dry-run` reject the file ("Unknown field 'env_file' in project file"), so it was removed and the profiles are now gated by packages/cli/src/__tests__/process-compose-validate.test.ts. The consequence below still holds unchanged.
  - consequence: A docker-compose.test.yml composition must inject env explicitly per service (env_file or environment:); running Playwright in CI without pre-exported env will boot an api missing required secrets — the harness must source the test env before invoking playwright.
- **LLM token accounting in Redis already exists: per-session counter at rk(`llm:tokens:${channel}:${customerId}`) — write side in claustrum emitTurn, read side fail-open in resolve-and-assemble, plus an existing `ibx rate` CLI inspector over rk('llm:tokens:*')**
  - evidence: apps/api/src/claustrum/resolve-and-assemble.ts:35-56 (sessionTokenKey, readSessionTokensConsumed); compose-policy-packs.ts:29 comment (write via emitTurn onto TurnRecord); packages/cli/src/commands/rate.ts:43-100
  - consequence: Per-agent/per-run token budgets can extend this exact key shape and the `ibx rate` inspector rather than introducing a new accounting mechanism.
- **infraEndpoints() registry (new at HEAD commit c7cb988, 'ibx dev urls') is the declared source-of-truth for infra addresses, resolving DATABASE_URL/REDIS_URL/TYPESENSE_HOST+PORT/NATS_URL with the same defaults as docker-compose.yml (5433/6379/8108/4222)**
  - evidence: packages/cli/src/services.ts:140-161; git log: c7cb988 'feat(cli): ibx dev urls + infraEndpoints registry'; SERVICES registry in same file holds app ports + healthUrl/healthExpect per service
  - consequence: Test-composition tooling should resolve ports via infraEndpoints()/SERVICES instead of hardcoding; it is also what `ibx svc status` probes — free stack-readiness check.
- **docs/agents/ (untracked, created 2026-06-11 22:25) already contains architectures.md, maps.md, research.md, plan-v1.md, plan-v1-critique.md, plan-v1-verification.md — the plan's 'durability note' action (copy /tmp/wf-results into ibatexas/docs/agents/) is already executed but not committed**
  - evidence: ls -la docs/agents/ → 6 files; git status → '?? docs/agents/'; plan line 171 durability note
  - consequence: Plan v2 should drop the copy task, replace with 'git add + commit docs/agents/' and land v2 alongside.
- **check-bypass.sh step 3 shells `pnpm --filter @ibatexas/llm-provider exec vitest run src/__tests__/no-execute-tool-direct.test.ts` but packages/ contains no llm-provider (deleted in claustrum cutover) — pnpm filter matches nothing and the step is a silent no-op**
  - evidence: scripts/check-bypass.sh:62-67; `ls packages/` → audit-sink, cli, domain, eslint-config, intent-kinds, nats-client, pack-*, pii, tools, types, ui (no llm-provider)
  - consequence: Corroborates plan's hygiene item with exact lines: a third of the bypass gate is dead; fix is to delete the block or repoint at the claustrum-side equivalent test.
- **Staging deploys run a host-side script `/usr/local/bin/ibatexas-deploy` via SSM on a tagged EC2 instance (Role=ibatexas-<env>-host) with a 15-min poll loop; migrations are deliberately MANUAL (medusa db:migrate hangs from GitHub runners against the Supabase pooler — documented workaround)**
  - evidence: .github/workflows/deploy-staging.yml:95-176 (SSM send-command, migration-hang comment :95-101)
  - consequence: Any plan assuming CI can run Medusa migrations against remote envs is wrong today; an ephemeral in-CI stack (local Postgres) sidesteps the pooler issue entirely — a point in favor of the docker-compose.test.yml approach.
- **Dev docker-compose Redis requires a password even locally (--requirepass ${REDIS_PASSWORD:-localdev}), per AUDIT-FIX INFRA-12**
  - evidence: docker-compose.yml:27-35
  - consequence: Test composition REDIS_URL must embed the password (redis://:localdev@...) or set REDIS_PASSWORD; testcontainer-based suites are unaffected (they boot their own unauthenticated Redis).
- **/health already reports DLQ and outbox queue depths (rk('dlq:<event>') lists and ${APP_ENV}:outbox:<event> lists), flipping status to degraded on backlog >100**
  - evidence: apps/api/src/routes/health.ts:97-117,129-143
  - consequence: A nightly/e2e harness gets free post-run assertions ('no DLQ entries, no outbox backlog') by polling /health — no new instrumentation needed.
