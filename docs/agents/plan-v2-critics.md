

# ===== CRITIC: feasibility =====

## [MUST-FIX] T1a-11 names a wrong base and is secretly L, not M. 'Clone dev compose' yields infra only — dev docker-compose.yml contains exactly postgres/redis/typesense/nats; the app stack (api :3001, Medusa commerce :9000, web) runs only via process-compose. No compose file anywhere runs Medusa: docker-compose.prod.yml builds api/web/admin but has NO commerce service. The task's own verify (`curl -s localhost:3001/health`) cannot pass against a cloned dev compose, and `ibx test seed` requires Medusa up. Containerizing api+commerce (Dockerfiles exist but the commerce compose service, in-container Medusa migrations, env/secrets plumbing, CI image builds are all greenfield) is multi-day work.
- where: §5 Phase 1a, T1a-11
- evidence: ibatexas/docker-compose.yml:3-65 (only postgres/redis/typesense/nats services); ibatexas/docker-compose.prod.yml:8-60 + grep 'commerce' → no commerce service; process-compose.yaml:46-47 `commerce: pnpm --filter @ibatexas/commerce dev` is the only Medusa runner
- fix: Rewrite T1a-11 as either (a) split into T1a-11a 'infra test compose (clone dev compose + explicit env)' + T1a-11b 'app services: api + commerce compose services from apps/api/Dockerfile + apps/commerce/Dockerfile (commerce service is new), in-container migrate+seed' (11b sized L), or (b) keep infra in compose and run apps via a process-compose test profile with the test env file — and change the verify command to match whichever is chosen.

## [MUST-FIX] JOURNEY-001 'guest-place+cancel (the exit journey)' is infeasible as a guest chat journey: the orders capability planner advertises order.checkout.create and order.cancel ONLY when isAuthenticated, and the bootstrap derives isAuthenticated from memory.customerId with guest:/anon: prefixes explicitly mapped to customerId:null; the chat route assigns guests customerId `guest:${sessionId}`. A guest can build a cart via chat but can never place or cancel an order — the Phase-1a exit criterion rides on a journey the planner will not drive.
- where: §5 T1a-12 (JOURNEY-001), Phase 1a exit criterion, T1a-13
- evidence: packages/pack-orders/src/capabilities.ts:118-133 (`if (isAuthenticated) allowedIntents.push("order.checkout.create", "order.cancel", ...)`); apps/api/src/claustrum-bootstrap.ts:751-766 (guest-prefix → customerId:null) ; apps/api/src/routes/chat.ts:268 (`customerId = request.customerId ?? \`guest:${sessionId}\``)
- fix: Re-scope JOURNEY-001 to an authenticated customer (authFixture customer JWT presented as the `token` cookie on chat POST + SSE so request.customerId is real), and add cookie-auth threading to T1a-5's ChatClient contract; if a guest journey is wanted, scope it to cart-stage kinds plus an assertion that checkout is not proposable (negative expectation), and move place+cancel to the authenticated journey.

## [MUST-FIX] T3-8's acceptance criterion is impossible per the plan's own ledger: JOURNEY-003 is authored blocked_by [handoff-port-noop, chat-confirmation-resume] (T1a-12), T3-8 closes only handoff-port-noop, and nothing in any phase closes chat-confirmation-resume (WebChannel.matchToParked returns null unconditionally; ledger claim 7 says these journeys stay blocked). 'Verify: ibx journey run JOURNEY-003 passes' cannot be satisfied.
- where: §5 Phase 3, T3-8 (and T1a-12 JOURNEY-003)
- evidence: claustrum/packages/channel-web/src/web-channel.ts:66 (matchToParked returns null unconditionally); plan §2 row 7 'Chat-confirm journeys stay blocked_by: chat-confirmation-resume'; plan T1a-12 '003 ... blocked_by:[handoff-port-noop, chat-confirmation-resume]'
- fix: Either give T3-8 its own acceptance (a handoff-specific journey/test asserting the queued notification artifact, not JOURNEY-003), or schedule a web matchToParked/confirmation-resume task (upstream or via the WebChannelStub injection seam for tests) before claiming JOURNEY-003 unblocks; update JOURNEY-003's blocked_by lifecycle accordingly.

## [MUST-FIX] P0-7 mischaracterizes reservation.checkin/reservation.complete and directs a wrong change. They are not live dangling advertisements: the live chat planner pins staffId:null (claustrum-bootstrap.ts documents 'staff-only reservation kinds remain non-proposable via the chat planner (they are staff-route only)'), so the staff branch never fires in production; the real execution path is admin HTTP routes that build envelopes directly and adjudicate via reservation.service.transitionFromEnvelope — no registered tool involved, so 'tools ship' is not what unblocks them. De-advertising the pack's staff branch deletes a deliberate, tested seam (pack test asserts staff exposure) while changing nothing observable in the live planner. DR-5's 'waived-pending-registration' category is also wrong: these kinds are drivable TODAY via the journey schema's own deterministic staff-http legs (POST /api/admin/reservations/:id/checkin|complete).
- where: §5 P0-7 and §4 DR-5 (waiver seeding)
- evidence: apps/api/src/claustrum-bootstrap.ts:758-766 ('staffId stays null ... reservation.checkin/.complete remain non-proposable via the chat planner (they are staff-route only)'); apps/api/src/routes/admin/reservations.ts:159-245 (checkin/complete routes building envelopes directly, requireManagerRole); packages/pack-reservations/src/__tests__/reservations-pack.test.ts:531-541 ('planner exposes staff transitions only when staffId is present')
- fix: Shrink P0-7's de-advertising to the two payment kinds only; treat reservation.checkin/complete as staff-http-covered cells (journeys via admin routes + staff JWT, no waiver); have the extended toolRosterDrift evaluate capability planners under named contexts (authed-customer, staff) and either whitelist the documented staff-chat follow-up or assert the live-bootstrap context (staffId:null) — and update the existing pack test if the branch is ever removed.

## [SHOULD-FIX] T3-2's deriveNonce threading is feasible but the per-turn carrier is unstated, and a cold implementer will hit a wall: createIbatexasPlanner(deps) is bootstrap-static and the nonce is minted inside propose(state); CognitiveState has no ctx field — the only structured per-turn carrier reaching the planner is state.perception.externalId (ChannelMessage.externalId is threaded into Perception by handle-turn). So yes, CognitiveState is the right carrier, but only via perception.externalId set by T3-1's SystemChannel.perceive(); 'structured working memory' (workingMemory is a plain string) is not a reliable nonce source.
- where: §5 Phase 3, T3-2 (and T3-1)
- evidence: apps/api/src/claustrum/ibatexas-planner.ts:204 (static deps), :261-267 (nonce: randomUUID() inside propose); claustrum/packages/core/src/ports/planner.ts:25-34 (CognitiveState = perception/memory/retrieval/workingMemory?/tenantId/locale/conversationId/turnId — no ctx); claustrum/packages/core/src/ports/channel.ts ChannelMessage.externalId + handle-turn.ts:58-59 (externalId → perception)
- fix: Amend T3-1 to require perceive() set ChannelMessage.externalId = `${sourceSubject}:${eventId}`, and amend T3-2 to define deriveNonce as a static planner dep `(state) => state.perception.externalId`-derived (fallback randomUUID for non-trigger turns), noting multi-envelope plans need a per-envelope suffix.

## [SHOULD-FIX] T3-4 says 'kernel guard ESCALATEs over-budget' while telling the implementer to compose 'like sessionTokenBudgetGuard' — but createTokenBudgetGuard's action union is "REFUSE" | "DEFER" only; ESCALATE requires the separate createEscalateGuard/createThresholdGuard factory. As written the named pattern cannot produce the named decision.
- where: §5 Phase 3, T3-4
- evidence: adjudicate/packages/primitives/src/guards.ts:740 (`export type TokenBudgetAction = "REFUSE" | "DEFER"`), :354-365 (createEscalateGuard — 'Mints a guard that returns ESCALATE when a threshold is crossed')
- fix: Name the seam: scope/sessionId-prefix violations via a custom REFUSE guard, over-budget ESCALATE via createEscalateGuard (threshold = agent budget) reading the llm:tokens-shaped counter from state — not via createTokenBudgetGuard's action.

## [SHOULD-FIX] T2-6 is secretly larger than M. Running bootstrapClaustrum in vitest requires: real Postgres with the audit schema (assertAuditPostgresReady), pgvector + claustrum_memory tables (memory + grounding providers), real Redis, and WEB_GATEWAY_SIGNING_KEY; the grounding provider calls modelProvider for embeddings (modelId text-embedding-3-small), so the content-keyed scripted provider must implement the full ModelProvider port including embeddings; and the reset hook must handle more than `_conductor` (per-call pgPool, audit-sink DI registered via bootstrapAuditSinkDI, global metrics sink) or repeated bootstraps leak/cross-contaminate.
- where: §5 Phase 2, T2-6
- evidence: apps/api/src/claustrum-bootstrap.ts:168 (let _conductor), :1132-1135 (pgPool + assertAuditPostgresReady), :1199-1206 (createPgVectorGroundingProvider({pool, modelProvider, modelId: 'text-embedding-3-small'})), :1240 requireSecret(WEB_GATEWAY_SIGNING_KEY); :1155-1160 bootstrapAuditSinkDI; :1122-1125 metrics-sink global
- fix: Split T2-6 into (a) bootstrap options param + reset hook + the pg(vector)+redis test harness (reuse h3-postgres-container/redis-testcontainer patterns, document what the reset must close/reset), and (b) content-keyed scripted provider (complete + stream + embed) + CC-006-format fixtures; size each M.

## [SHOULD-FIX] T1a-4/T1a-5 omit the token transport: customer JWTs are verified from the httpOnly `token` cookie (fastifyJwt cookie config) and staff JWTs strictly from the `staff_token` cookie (no Authorization-header path for staff); revocation check (jti) requires Redis up. A cold implementer minting bearer tokens will get 401s; the authenticated chat journeys (see JOURNEY-001 finding) additionally need the cookie on chat POST + SSE GET.
- where: §5 Phase 1a, T1a-4 and T1a-5
- evidence: apps/api/src/server.ts:71-75 (`cookie: { cookieName: "token" }`); apps/api/src/middleware/auth.ts:139-141 (`request.cookies?.["staff_token"]`), :75-79 (revocation fail-closed on Redis)
- fix: State in T1a-4 that tokens are delivered as Cookie headers (`token=` / `staff_token=`), and add to T1a-5 that the ChatClient accepts an optional auth cookie threaded on both POST and SSE GET.

## [SHOULD-FIX] Workspace dependency cycle baked into T1a-1/T1a-8: packages/cli must import @ibatexas/journeys (to register `ibx journey`), while T1a-1 tells journeys to reuse runPipeline and T1a-8 tells it to 'extend lib/events.ts union' — both private modules of packages/cli, which has a bin but no main/exports surface. journeys→cli + cli→journeys is a pnpm workspace cycle.
- where: §5 Phase 1a, T1a-1 and T1a-8
- evidence: packages/cli/package.json (name @ibatexas/cli, `bin` only, no main/exports); packages/cli/src/lib/pipeline.ts and lib/events.ts are cli-internal
- fix: Decide the home explicitly: keep runPipeline/event-emission usage in the cli-side registration layer only, or move events.ts (+ pipeline types) into a shared package (e.g. @ibatexas/tools or the new journeys package owns the extended event union and cli depends on it one-way).

## [SHOULD-FIX] P0-8 never says where the shared module lives. Exporting IBATEXAS_POLICY_PACKS 'from a shared module' while pointing only at claustrum-bootstrap.ts:696 invites a cold implementer to export from apps/api — which packages/cli (T1a-2's lint gate) cannot import; §9 already records this exact failure mode as 'killed' but P0-8's text doesn't encode the fix.
- where: §5 P0-1..P0-11, P0-8
- evidence: apps/api/src/claustrum-bootstrap.ts:696 (`const IBATEXAS_POLICY_PACKS` module-private in an app, not a package); packs are workspace packages (@ibatexas/pack-orders et al.) importable from packages/*
- fix: P0-8 must name the target: a workspace package importable by both apps/api and packages/cli (e.g. extend @ibatexas/intent-kinds or a new @ibatexas/packs-composed) that composes the five packs; apps/api re-imports it so there is exactly one composition site.

## [SHOULD-FIX] P0-2's containment parenthetical doesn't match P0-3's actual new leg: P0-2 says the audit read module is 'never importable from the future testkit — enforced by P0-3's new leg', but P0-3's leg checks the opposite direction (apps/* and packages/* never import @ibatexas/journeys). Nothing proposed enforces 'testkit must not import apps/api internals'.
- where: §5 P0-2 / P0-3
- evidence: Plan P0-2: '(never importable from the future testkit — enforced by P0-3's new leg)' vs P0-3: 'add a leg: apps/* and packages/* (non-test) never import @ibatexas/journeys'
- fix: Either add the second direction to P0-3 (journeys/testkit sources contain no imports from apps/api) or drop the unsupported claim from P0-2.

## [ACCEPTED-RISK] T1b-4's verify `gh workflow run journeys-nightly && gh run watch` is not executable verbatim: `gh workflow run` requires the workflow to declare workflow_dispatch (a schedule-only nightly won't dispatch) and `gh run watch` without a run id is interactive. Human-runnable with small adjustments; harmless if the workflow adds workflow_dispatch.
- where: §5 Phase 1b, T1b-4
- evidence: gh run watch help: 'Watch a run until it completes' — without args it prompts for run selection; gh workflow run errors on workflows lacking workflow_dispatch
- fix: Add `on: workflow_dispatch` alongside the schedule and pin the verify to `gh run watch $(gh run list --workflow journeys-nightly -L1 --json databaseId -q '.[0].databaseId')` or `gh run watch --exit-status <id>`.

## [ACCEPTED-RISK] Cosmetic/verified-OK items: Phase-3 header lists T3-2 twice as two prerequisites; T1a-3's baseline path `governance/journey-coverage-baseline.json` is ambiguous vs the existing convention (packages/cli/governance/pack-bom-baseline.json). Checks that PASSED: P0-1 is genuinely a one-call change (buildAdjudicator deps.ledger optional, forwarded at the adjudicateAndAudit call for both fresh and receipt/resume paths); kernel pack-bom's idiom is exactly `--verify-file <path>` + `--json` + exit 1 so T1a-2/T1a-3 flags match; commander 13 accepts the `--k <n>` long flag (empirically tested); @adjudicate/observability exists for T1b-6; jobs/register-workers.ts exists for T3-2; claustrum root has a `test` script for T3-0's verify; chooseImplementation seam confirmed for T3-6; the DAG is acyclic with no task consuming a later artifact.
- where: §5 Phase 3 header; T1a-3; P0-1; T1a-2/T1a-3; T1a-13; T1b-6; T3-0; T3-6
- evidence: apps/api/src/claustrum-bootstrap.ts:196-199 + :415-420 (ledger dep forwarded); packages/cli/src/commands/kernel.ts:1392-1401 (--json/--verify-file); commander@13 parse test of `--k 2 --json` → OK; adjudicate/packages/ contains observability/; claustrum/package.json `"test": "pnpm -r test"`; claustrum/packages/core/src/tools/registry.ts:75
- fix: Dedupe the T3-2 mention; spell the baseline path as packages/cli/governance/journey-coverage-baseline.json (or wherever the journeys package keeps governance) in T1a-3 and the 1a exit criterion.

## verdictOnSection9
The Critic-1 paragraph is plausible but materially undersells what a feasibility pass at HEAD finds, and one of its claims is incomplete. What it gets right and which I verified: the P0-8/lint-time pack-list fix is real (apps/api runtime export genuinely unusable from the CLI gate); the T2-2 split exists; JOURNEY-008's forgery observable (400 + forgery_attempt + system REFUSE audit) is exactly assertable per customer-intent-gateway.ts; the journeys-as-new-package resolution is recorded. Incomplete: the 'bootstrapClaustrum is a zero-param singleton (T2-6 needs the reset hook, not just the options param)' note stops too early — the reset hook must also handle the per-call pgPool, audit-sink DI, and the global metrics sink, and the scripted provider must serve embeddings because the pgvector grounding provider calls modelProvider; T2-6 as one M task is not honest. Missing entirely from the paragraph (all found by this pass, all in §5 task text a cold implementer would execute): (1) JOURNEY-001 'guest-place+cancel' cannot be driven — checkout/cancel are auth-gated in the planner and chat guests get guest:-prefixed customerIds, so the Phase-1a flagship/exit journey is infeasible as titled; (2) T1a-11 'clone dev compose' names a wrong base — no compose file anywhere runs Medusa commerce and dev compose is infra-only, so the task's own verify cannot pass and the task is L; (3) T3-8's acceptance (JOURNEY-003 passes) contradicts the plan's own ledger because chat-confirmation-resume is never closed; (4) P0-7's de-advertising of reservation.checkin/complete misreads the code — the live planner never advertises them (staffId pinned null) and the real path is direct-envelope admin routes, so the directed change and DR-5's waived-pending-registration category are wrong; (5) T3-4 names an impossible pattern (TokenBudgetAction is REFUSE|DEFER; ESCALATE needs createEscalateGuard); (6) T3-2's deriveNonce has no stated per-turn carrier — feasible only via Perception.externalId set by T3-1's perceive(), which the plan must say. The paragraph should be rewritten to record these six (or the tasks amended so they're moot), and it can keep — now as verified rather than predicted — the items it already lists. Checks the orchestrator flagged that PASSED cleanly: P0-1 (deps.ledger alone suffices; bridge forwards it on both fresh and resume paths), T1a-2/T1a-3 (--verify-file/--json idiom matches kernel pack-bom exactly), and --k (commander 13 parses it).


# ===== CRITIC: operations =====

## [MUST-FIX] The test composition cannot run journeys: T1a-11 says 'clone dev compose', but the dev compose contains ONLY infra (postgres, redis, typesense, nats) — no api, no Medusa (commerce), no web. Even docker-compose.prod.yml has api/web/admin but NO commerce service, so there is no existing containerization of Medusa to clone. Apps are booted by process-compose in dev, which no nightly task invokes. T1a-11's own verify line (`curl -s localhost:3001/health`) and the Phase-1a exit criterion (`docker compose -f docker-compose.test.yml up --wait && ibx test seed && ibx journey run JOURNEY-001`) require a running api+Medusa that nothing in the plan starts. Fresh-DB migrations (ibx bootstrap layers) are also absent from the T1b-4 step list ('compose up → ibx test seed → journey suite').
- where: T1a-11, T1b-4, Phase 1a exit criterion
- evidence: /Users/thaisrodolpho/projects/ibatexas/docker-compose.yml services = postgres(:3)/redis(:24)/typesense(:44)/nats(:64) only; docker-compose.prod.yml services = api(:11)/web(:35)/admin(:55)/postgres/redis/typesense/nats — no commerce; process-compose.yaml boots commerce :9000 and api :3001 (ledger infraCi). Plan T1a-11: 'clone dev compose … Verify: docker compose -f docker-compose.test.yml up --wait && ibx svc status && curl -s localhost:3001/health'
- fix: Rewrite T1a-11 to name the app-boot mechanism explicitly: either (a) add containerized api + commerce + (optionally web) services to docker-compose.test.yml — noting Medusa containerization is NEW work with no prod-compose precedent, plus an image-build step in the workflow — or (b) have the nightly run `docker compose up` for infra then process-compose/pnpm-based app processes (with pnpm install + build + `ibx bootstrap` migrations as counted steps). Re-baseline the ≤10-min boot+seed budget against whichever path is chosen, including CI pnpm install/build of packages/cli so `ibx` exists.

## [MUST-FIX] ANTHROPIC_API_KEY is missing from the plan's secrets story. T1a-11 enumerates the test composition's explicit env (JWT secrets, REDIS_PASSWORD, TYPESENSE_API_KEY, APP_ENV, fingerprint, ANTHROPIC_MODEL) but omits ANTHROPIC_API_KEY, which apps/api config validation hard-requires — the composition fails at boot without it. T1b-4 (nightly workflow) never mentions provisioning it as a GH Actions secret, and the driver (T1a-8) needs its own key. T1a-10's pre-flight asserts the model pin but not key presence.
- where: T1a-11, T1b-4, T1a-10
- evidence: /Users/thaisrodolpho/projects/ibatexas/apps/api/src/config.ts:70 `ANTHROPIC_API_KEY: z.string().min(1, "ANTHROPIC_API_KEY is required")`; claustrum-bootstrap.ts:1138 `apiKey: process.env.ANTHROPIC_API_KEY ?? ""`; plan T1a-11 env list and T1b-4 contain no mention of ANTHROPIC_API_KEY
- fix: Add ANTHROPIC_API_KEY to T1a-11's explicit env list (api service + driver process); T1b-4 documents it as a repo/environment secret (recommend a dedicated, spend-capped key for the nightly, distinct from prod); T1a-10 pre-flight asserts key presence alongside the model pin.

## [MUST-FIX] §7 names an impossible data source for dollar cost: 'Read actuals from the Redis llm:tokens counter (exported sessionTokenKey)'. That counter is written as incrBy(key, inputTokens+outputTokens) — a single combined total with no input/output split. With Sonnet 4.6 priced $3/MTok in vs $15/MTok out (5x spread), the counter cannot be converted to dollars, so neither the per-run cost line (T1a-13 acceptance) nor the $50/night abort can be computed from it. Additionally, NO task implements the $50/night suite-level abort: T1b-4 says only 'budget abort env (§7)', and §7's mechanisms (driver meter + per-run token ceiling) are per-attempt, not per-night aggregation.
- where: §7 Budgets, T1b-4, T1a-13
- evidence: /Users/thaisrodolpho/projects/ibatexas/apps/api/src/claustrum-bootstrap.ts:1020-1029 `const total = (record.inputTokens ?? 0) + (record.outputTokens ?? 0); … .incrBy(sessionKey, total)`; plan §7: 'Read actuals from the Redis llm:tokens counter … Hard abort at $50/night (driver meter + per-run token ceiling env)'
- fix: Compute dollars from the JSONL trace instead: T1a-8's `llm.call` events must record per-call inputTokens/outputTokens (available on completion + plan.usage), and a checked-in price table converts them. Add an explicit sub-task (in T1b-4 or a new T1b-8) for the suite runner: aggregate cumulative cost across attempts after each journey, abort the suite (red run, remaining journeys reported as 'aborted-by-budget') when cumulative cost ≥ cap. Keep the Redis counter as the SUT-side budget-guard input only, not the cost ledger.

## [SHOULD-FIX] §7's wall-clock arithmetic is self-contradictory: 'start ~10 journeys … ≈ 22 attempts' × 'journeys sequential ≈2–5 min each' = 44–110 min of journey time, plus ≤10 min boot+seed — the 45-min cap (named as the parallelization tripwire) is exceeded on the very first nightly even at the optimistic end. The full 25-journey suite (~49 attempts) is 98–245 min. The cap, the suite size, or the per-attempt estimate must change; as written the 'revisit trigger' fires on night one, which makes it meaningless as a tripwire.
- where: §7 Budgets (Wall-clock), T1b-4, 'Deliberately not built' table (per-run isolated DBs row)
- evidence: Plan §7: 'start ~10 journeys (money flows k=4, exploratory k=1) ≈ 22 attempts' and 'journeys sequential ≈2–5 min each → 45-min cap (the existing revisit trigger for parallelization)'
- fix: Either raise the nightly cap to ~2h for the starting suite (nightly cadence tolerates it) and keep 45 min as a per-journey/per-phase budget, or shrink night-one scope (e.g. k=2 money flows ≈ 14 attempts) to fit 45 min, and state the projected wall-clock for the 25-journey suite so the parallelization trigger is set at a number the suite won't hit on day one.

## [SHOULD-FIX] §7's token estimates are ~5x above what the actual prompt assembly produces today. The planner sends: a ~25-line pt-BR system prompt (~400 tokens), ONE user message containing only state.perception.text (no conversation history), and a tool surface of express_intent (enum of allowed intents) plus ~6-15 read tools with one-line descriptions — roughly 1.0–1.6k input tokens/turn, not 5–8k. The responder is a one-line system prompt + perception text (~60-150 tokens in), not 3–5k. An 8-turn journey is ~10–15k in / ~3–6k out ≈ $0.08–0.15 SUT-side at Sonnet 4.6 pricing ($3/$15 per MTok), vs the plan's $0.38–0.55. The error is conservative (budgets won't be blown) but the §7 numbers are presented as derived from real prompt sizes and they are not; also 'SUT side dominates' is doubtful — the driver loop, which accumulates full conversation context across turns, is likely comparable to or larger than the SUT cost.
- where: §7 Budgets (token cost per journey-attempt)
- evidence: /Users/thaisrodolpho/projects/ibatexas/apps/api/src/claustrum/ibatexas-planner.ts:233-239 `messages: [{ role: "user", content: state.perception.text }]` with system at :71-88; claustrum-bootstrap.ts:812-819 responder: one-line system + `input.cognition.perception.text` only; pack read-tool sets e.g. pack-orders capabilities.ts:46-53 (6 read tools)
- fix: Mark §7's figures as deliberate over-provisioned ceilings, and add a T1a-13 acceptance output: record the MEASURED per-attempt token/cost split (driver vs SUT, in vs out) from the first green JOURNEY-001 runs and re-baseline §7 from it. Note prompt sizes will grow when richer prompt synthesis / multi-hop enrichment lands (both documented follow-ups in source), so the re-baseline must be repeated when those ship.

## [SHOULD-FIX] T1a-11's isolation story is internally inconsistent and incomplete. It adds BullMQ isolation via a dedicated Redis DB index 'for concurrent local runs', but the cloned dev compose binds fixed host ports (127.0.0.1:5433/6379/8108/4222), so a test composition cannot run concurrently with the dev stack at all without overriding every port — and if the composition instead gets dedicated ephemeral containers (the GH-Actions reality), the DB-index measure is redundant while the genuinely shared surfaces in the local-shared scenario (Medusa + domain Postgres databases, Typesense collections, NATS subjects) are not covered. Also rk()'s ENV_PREFIX is captured once at module load, so APP_ENV must be injected before process boot — worth stating as an acceptance check.
- where: T1a-11
- evidence: /Users/thaisrodolpho/projects/ibatexas/docker-compose.yml:11 '127.0.0.1:${POSTGRES_PORT:-5433}:5432' (fixed host ports per service); packages/tools/src/redis/key.ts:14 ENV_PREFIX captured at module load (ledger infraCi); apps/api/src/jobs/queue.ts:17 hardcoded prefix 'ibx'; plan T1a-11 'BullMQ isolation via dedicated Redis DB index … APP_ENV does not cover it'
- fix: T1a-11 must declare the isolation model explicitly: 'one ephemeral composition per run, all infra containers dedicated, host ports overridden via env (POSTGRES_PORT etc.) and resolved through infraEndpoints()' — then port-collision is the only local-concurrency concern and the Redis DB-index becomes defense-in-depth. If shared local infra is ever intended, the isolation checklist must extend to Postgres database names, Typesense collection names, and NATS subjects, or that mode must be forbidden in the doc.

## [SHOULD-FIX] The flake policy has no failure-ownership half. T1b-4 defines retry-once=yellow and quarantine-after-2-consecutive-flakes (printed in artifact), but says nothing about: (a) a journey that fails HARD (non-flakily) two nights running — who is notified, by what channel, and what blocks until triage; (b) de-quarantine — no rule ever returns a quarantined journey to active, so quarantine is a one-way coverage leak that the coverage gate (T1a-3, covered→uncovered fails) may not even see if quarantined journeys still count as covering their cells; (c) any notification mechanism at all for a red nightly (no Slack/webhook/issue-creation step in the workflow).
- where: T1b-4, §8 risk 2, Phase 1b exit criterion
- evidence: Plan T1b-4: 'flake ledger (retry-once = yellow; quarantine after 2 consecutive flakes — quarantined journeys listed in the artifact, never silently dropped)' — no owner, no paging, no de-quarantine, no hard-failure escalation path; 1b exit criterion accepts 'green/explained' with no definition of who explains
- fix: Add to T1b-4: red-run notification (GH issue auto-filed or Slack webhook — the same incoming-webhook channel T3-7 plans can be reused), a named owner field in the flake ledger entry, a de-quarantine rule (e.g. green twice consecutively on a manual re-run → active), and a statement of whether quarantined journeys' coverage cells count as covered (recommend: they count as waived-quarantined and surface in the coverage report).

## [SHOULD-FIX] Two trigger-agent failure modes named in T3-2/T3-0 have mitigations but NO specified test coverage: (a) feedback loops — 'agent-caused-event suppression (skip events whose causal actor sessionId has the agent prefix)' has no test in T3-2's verify line (which covers only redelivery dedup and cooldown), yet a suppression bug is a self-amplifying loop that the $-budget guard catches only after burning tokens; (b) trigger-vs-chat serialization — T3-0's verify is only 'pnpm -C claustrum test green; ibatexas boots on the new pin'; no test anywhere asserts that a trigger turn and a live chat turn for the same customer actually serialize under the new lock-key strategy (risk #5's tripwire is purely observational).
- where: T3-2, T3-0, §8 risk 5
- evidence: Plan T3-2 Verify: 'redelivered job does not double-fire (test: two deliveries → one EXECUTE + one suppressed/skip); cooldown test' — no loop-suppression test; T3-0 Verify: 'pnpm -C claustrum test green; ibatexas boots on the new pin' — no serialization test; conductor.ts:101-122 lock key ignores sessionKey (ledger claustrum row 2)
- fix: Extend T3-2's acceptance with a loop test: synthetic event whose causal actor sessionId carries the agent prefix → zero capsules opened, suppression journaled. Extend T3-0's (or T3-2's) acceptance with a two-process serialization test: concurrent trigger turn + chat turn for one customer under PostgresAdvisorySessionLock + the new lock-key strategy → audit rows strictly serialized (no interleaved turn timestamps).

## [SHOULD-FIX] Embedding-model config is a second bogus-default of the same class P0-6 fixes for ANTHROPIC_MODEL, and it silently affects nightly noise/cost: the grounding provider is constructed with modelId = EMBEDDING_MODEL_ID ?? 'text-embedding-3-small' — an OpenAI model id — fed to the AnthropicProvider. P0-6's verify grep only targets the literal 'claude-opus-4-5-20250101', so this default survives Phase 0. Depending on the pgvector provider's behavior this is either a per-turn failing call (latency + log noise in every journey) or grounding silently disabled; either way §7's cost model never mentions grounding/embedding calls and T1a-11's env list doesn't pin EMBEDDING_MODEL_ID.
- where: P0-6, §7, T1a-11
- evidence: /Users/thaisrodolpho/projects/ibatexas/apps/api/src/claustrum-bootstrap.ts:1199-1205 `createPgVectorGroundingProvider({ pool: pgPool, modelProvider, modelId: process.env.EMBEDDING_MODEL_ID ?? "text-embedding-3-small", … })`; plan P0-6 verify: `grep -rn "claude-opus-4-5-20250101"` only
- fix: Widen P0-6 to audit EMBEDDING_MODEL_ID: determine whether grounding actually issues model calls per turn; replace the OpenAI default with fail-fast or a valid configuration; add the variable to T1a-11's env list; state in §7 whether grounding contributes per-turn cost (and have T1a-13's measured cost report confirm).

## [ACCEPTED-RISK] Nightly environment leakage (orphaned containers, volumes, disk growth) is unaddressed: T1b-4 specifies no teardown step and never states whether the workflow runs on GitHub-hosted runners (ephemeral VMs make leakage moot) or self-hosted (where repeated `compose up` with named volumes accumulates Postgres/Typesense data and stopped containers across nights). Acceptable as a risk only if the runner choice is stated.
- where: T1b-4
- evidence: Plan T1b-4 step list ends at 'artifacts … flake ledger' with no `docker compose down -v` / cleanup step and no runner specification; ledger infraCi: no existing workflow boots the stack (nothing to inherit cleanup from)
- fix: One line in T1b-4: 'runs on GitHub-hosted runners (ephemeral — no cleanup needed)' OR add a final `if: always(): docker compose -f docker-compose.test.yml down -v --remove-orphans` step plus a disk-usage check if self-hosted is ever used. Also state artifact retention days for the JSONL/sim_runs exports.

## verdictOnSection9
Section 9's Critic-2 (operations) paragraph is directionally right but verified the wrong layer and must be rewritten. What it got right: it caught Typesense-reindex/Medusa-boot time, forced the BullMQ Redis-DB-index isolation, the printed quarantine list, and abort-is-a-red-run. What it missed or got wrong: (1) its headline kill — 'wall-clock estimate ignored Medusa boot (~1-2 min)' — priced Medusa boot into a composition that contains no Medusa, api, or web service at all; the dev compose T1a-11 clones is infra-only and even prod compose has no commerce service, so the ≤10-min boot+seed budget it 'raised' rests on an app-boot mechanism that does not exist and is not tasked (my must-fix #1). (2) Its 'Accepted: sequential journeys until the 45-min tripwire' endorses arithmetic that fails on night one: §7's own 22 attempts × 2–5 min = 44–110 min, exceeding the cap before boot+seed is added. (3) It let 'abort-on-budget is a red run' stand without an owning task or a workable data source — §7 points cost actuals at the Redis llm:tokens counter, which stores a combined in+out total that cannot be converted to dollars at a $3/$15 per-MTok split. (4) It is silent on CI secrets entirely — ANTHROPIC_API_KEY is absent from T1a-11's explicit env list yet hard-required by apps/api config, so the composition fails at boot as written. (5) It is silent on failure ownership: nothing pages or files anything when a journey fails non-flakily two nights, and quarantine has no exit rule. (6) It did not test the cost math against the real prompt assembly — the planner/responder send single-turn perception text with short system prompts, making §7's per-attempt figure ~5x high (conservative, but presented as grounded). The rewritten paragraph should state: killed = the boot/wall-clock model (app services unbooted, cap arithmetic inconsistent); forced = ANTHROPIC_API_KEY + EMBEDDING_MODEL_ID into the env contract, a suite-level dollar-abort task computing from JSONL llm.call in/out events, red-run notification + de-quarantine rules, and an isolation-model declaration (dedicated ephemeral containers with overridden host ports); accepted = GH-hosted-runner teardown-by-ephemerality and the over-provisioned cost ceiling pending T1a-13 measurement.


# ===== CRITIC: governance =====

## [MUST-FIX] Stage-0 shadow EXECUTE rows land in the REAL intent_audit table and are ingested as production signal by the kernel-replay CI gate and the impact graph. T3-6's only stated isolation is 'session-namespace isolation so sandboxed EXECUTE audit rows never surface in real-customer memory via the (now-implemented) read paths' — that covers ONLY the P0-2 customerId/sessionId-scoped read paths. The replay gate query is a time-windowed scan with no session/principal filter, so once Stage-0 goes live (Phase 3) the pre-existing replay gate (established Phase 1b) will re-adjudicate shadow EXECUTE decisions (deliberately exploratory) and classify drift over them, and T2-4's impact graph will count phantom executions that never touched real state.
- where: T3-6 (Phase 3); T1b-3 / T2-4; Risk #6
- evidence: plan line 286 ('session-namespace isolation … never surface in real-customer memory via the … read paths'); ibatexas packages/cli/src/commands/kernel.ts ~274 `SELECT * FROM intent_audit WHERE recorded_at >= $1 AND recorded_at < $2` (+ optional intent_kind only — no session_id/principal filter); plan T2-4 line 269 'impact (intent_audit aggregation …)'; intent_audit carries session_id + principal columns (adjudicate packages/audit-postgres/src/postgres-sink.ts:43,161,221) so the rows ARE distinguishable but nothing filters them
- fix: T3-6 must add a shadow-row exclusion at every site that treats intent_audit as a time-windowed production signal: filter the kernel-replay query (T1b-3) and the impact aggregation (T2-4) by `principal`/`session_id`-prefix to exclude the shadow-agent namespace (or mark shadow rows with a dedicated principal/marker column). State explicitly that 'session-namespace isolation' covers memory recall only, and that replay + impact + any production drift baseline get the exclusion filter as part of T3-6's acceptance.

## [MUST-FIX] The Stage-0 suppression mechanism rests entirely on ToolRegistry.chooseImplementation routing the shadow tenant to a sandbox executor, but the plan never pins the per-tenant discriminator or a fail-closed default. The registry's default chooseImplementation is last-registered-wins; registering sandbox tools alongside real tools under the same capability means a mis-set or absent ctx flag can resolve EITHER a real customer to the sandbox (their action silently no-ops) OR — catastrophically — the shadow agent to the REAL executor, in which case the 'shadow' agent mutates live customer orders and the entire safety property collapses. Stage-0 runs in the production conductor over real PIX failures, so this is a production code path.
- where: T3-6 (Phase 3)
- evidence: plan line 286 'sandbox tool executor resolved per-tenant via ToolRegistry.chooseImplementation (the only invariant-compatible suppression seam)'; claustrum packages/core/src/tools/registry.ts:114-119 default chooseImplementation = `candidates[candidates.length - 1]` (last-registered wins); no plan task specifies the ctx discriminator, the fail-closed default, or a conformance test
- fix: Specify the exact ctx field that flags a shadow capsule, and require chooseImplementation to be fail-closed in BOTH directions: a real-tenant ctx can NEVER resolve a sandbox tool, and a shadow-tenant ctx can NEVER resolve a real tool (default to refuse/no-op, never to the real mutator). Add a conformance check (extends CC-002 / options.checks) asserting both, and prefer registering sandbox tools in a dedicated shadow registry instance rather than co-mingled under the same capability.

## [MUST-FIX] The journey schema introduces a `system-fixture` act kind available to journey authors from Phase 1a, with no definition of what it does or how it is contained. If it injects state directly it is a side effect around the kernel; if it publishes a system event it launders taint to SYSTEM via buildSystemEnvelope. This directly contradicts §9's claim that the T3-9 trigger-injection helper is 'the only publish-capable test path' and the plan's invariant that all side effects route through adjudicate. A journey act is exactly the 'journey act calling the helper' the lens warns about.
- where: T1a-1 (act kinds) vs T3-9 / §9 Critic 3
- evidence: plan line 186 'acts[] (sequential; kind: chat|http|system-fixture; …)'; plan line 289 T3-9 'fire the trigger (the one allowlisted publish helper)'; plan line 348 §9 'the allowlisted trigger-injection helper in T3-9 is named as the only publish-capable test path'; buildSystemEnvelope stamps taint:'SYSTEM', principal:'system' (apps/api/src/subscribers/__shared__/system-actor-envelope.ts:92-98) — clearing the SYSTEM-only floor
- fix: Define `system-fixture` precisely and bound it: either (a) it only writes inert DB fixtures via a deny-listed-in-prod helper that NEVER publishes NATS or stamps SYSTEM taint, or (b) it is the single allowlisted publish helper and that helper is the ONLY producer of system envelopes, gated by the test fingerprint. Reconcile T1a-1 with T3-9 so there is exactly one auditable forge path, and give it the same containment rigor as the JWT helper (named grep gate forbidding app + journey-driver imports).

## [MUST-FIX] T3-9's 'one allowlisted publish helper' — the primary control against NATS taint-laundering — is specified far more loosely than the JWT helper. The JWT helper gets a named grep gate ('no app imports'); the publish helper only 'carries its own check-bypass leg' with no statement of what that leg asserts or what prevents the test-driver agent / a journey act from importing it. Until T3-10 there is zero NATS server-side auth, so any code that can call publish forges a SYSTEM-tainted envelope undetectably.
- where: T3-9 / DR-2 / §9 Critic 3; Risk #4
- evidence: plan line 289 'the one allowlisted publish helper'; plan line 348 '…carries its own check-bypass leg; JWT-minting helper confined to the testkit package with a grep gate (no app imports)' — the publish helper gets no equivalent grep gate spec; DR-2 line 103 and Risk #4 line 334 confirm publish on capture creds is 'undetectable server-side today'
- fix: Specify the publish helper's containment with the same precision as the JWT gate: a named grep leg asserting it is importable ONLY from the T3-9 acceptance test and never from the journey-driver act tools (T1a-8 acts are {chat,http} — enumerate them as a closed set and assert the publish helper is not among them), nor from any apps/* or packages/* non-test code, plus a runtime guard requiring the test fingerprint env var before it will publish.

## [MUST-FIX] T3-4's agent-scope guard — the sole control bounding a managed agent to its declared intent kinds — is specified as 'composed like sessionTokenBudgetGuard', which places it in the BUSINESS phase, the wrong altitude for an authorization check. The kernel order is state→taint→auth→business, and the adopter prepends confirmOnAutoResolveGuard ahead of pack guards in business. If a managed agent proposes an out-of-scope but valid money kind (e.g. PIX agent emits order.cancel), confirmOnAutoResolveGuard matches FIRST (order.cancel ∈ its kind set) and returns REQUEST_CONFIRMATION, short-circuiting before the scope guard can REFUSE — the out-of-scope intent is parked for confirmation instead of hard-refused.
- where: T3-4 (Phase 3)
- evidence: plan line 284 'composed like sessionTokenBudgetGuard (compose-policy-packs.ts pattern)'; adjudicate kernel order state→taint→auth→business (packages/core/src/kernel/adjudicate.ts:9-19); adopter guards prepended to BUSINESS phase (ibatexas compose-policy-packs.ts:114); confirmOnAutoResolveGuard matches order.cancel/payment.pix.regenerate/reservation.cancel (compose-policy-packs.ts:65-69) and would short-circuit ahead of a later business-phase scope guard
- fix: Place createAgentScopeGuard in the AUTH phase (it is an identity/scope check), or guarantee it runs before ANY other guard that can emit a non-REFUSE decision for an out-of-scope kind. Add a conformance fixture proving an out-of-scope kind from a registered agent yields REFUSE (not REQUEST_CONFIRMATION/REWRITE/DEFER) even when confirmOnAutoResolve would otherwise match. Also reconcile the action mismatch: T3-4 says 'kernel guard ESCALATEs over-budget' while sessionTokenBudgetGuard uses REFUSE (compose-policy-packs.ts:49).

## [SHOULD-FIX] Offline JWT minting (T1a-4) is contained solely by the operational assumption that the test JWT_SECRET differs from production's. The minting helper is not bound to the test-fingerprint env var, and the fingerprint handshake (T1a-10) gates only the harness, not the minting primitive. If an operator ever sets the test compose's JWT_SECRET equal to production's (or points minted tokens at a reachable prod), the tokens are accepted — the contract test 'prod-secret env rejects both' validates the mechanism but not the deployment invariant.
- where: T1a-4 / T1a-10 / T1a-11
- evidence: plan line 201-202 'tokens valid only where the secret matches … prod-secret env rejects both'; plan line 236 T1a-11 sets distinct ≥32-char secrets; plan line 231 fingerprint asserted by the HARNESS pre-flight only; nothing binds authFixture minting to the fingerprint
- fix: Gate the authFixture minting helper itself on the presence of the test-fingerprint env var (refuse to mint without it), so a minted token cannot be produced in or aimed at an environment lacking the test marker even if secrets were misconfigured. Document the secret-distinctness as an explicit invariant of T1a-11, not an implicit one.

## [SHOULD-FIX] T2-1 lists two mechanisms for the paid-state fixture — a locally-signed synthetic Stripe webhook POST AND `toOrderProjectionData()` for 'projection fixtures' — without separating their roles. toOrderProjectionData writes the OrderProjection directly via Prisma upsert, bypassing the kernel entirely. If the paid state the journey ASSERTS is produced by a direct projection upsert rather than the real signed-webhook → buildSystemEnvelope → adjudicate → projection pipeline, the journey asserts a forged fixture and the 'payment-status matrix transition asserts' verify is vacuous.
- where: T2-1 (Phase 2)
- evidence: plan line 266 'locally-signed synthetic Stripe webhook POST …; toOrderProjectionData() reused for projection fixtures'; ledger lines 289-291 (toOrderProjectionData → orderProjection.upsert direct, bypasses webhook→payment pipeline); real signed route exists and is the kernel-routed path (apps/api/src/routes/stripe-webhook.ts:716-742, constructEvent + livemode bind)
- fix: State explicitly that the paid-state TRANSITION under test must be driven through the real signature-verified webhook route (signed with the test-plane STRIPE_WEBHOOK_SECRET) so it flows through buildSystemEnvelope → adjudicate; restrict toOrderProjectionData to pre-seeding HISTORICAL context (reorder/review journeys), never to forging the asserted paid projection.

## [SHOULD-FIX] The plan asserts all side effects route through adjudicate, but the seeding/fixture lifecycle (ibx test seed, seed-orders.ts, toOrderProjectionData, db backfill) writes Medusa + Prisma state directly, around the kernel. This is acceptable as test setup, but the plan never draws the boundary explicitly, so there is no stated rule preventing a journey's `system-fixture`/fixture acts from using these direct writers to establish the very state a `verify` then asserts.
- where: T1a-13 / T2-1 / T1a-1 fixture acts
- evidence: ledger lines 287-291 (seed-orders.ts + toOrderProjectionData write via Prisma upsert directly); plan line 246 invariant execution; plan line 186 `system-fixture` act kind undefined
- fix: Add an explicit governance rule: direct DB/seed writers may establish PRECONDITIONS only, and any state that appears in a journey's `expects`/`verify` must have been produced by a kernel-routed path (HTTP chat/http act → adjudicate, or the signed webhook). Make the reconciliation gate (T1b-1) or lint enforce that asserted intent kinds have a matching audited envelope, not a direct fixture write.

## verdictOnSection9
The §9 Critic 3 paragraph is directionally correct but materially UNDERSTATES the governance findings and contains one refuted claim. Verified/correct: the host-side dispatch-suppression kill (no seam between submit and act; sandbox-executor-only is the right call — confirmed by claustrum dispatch.ts/handle-turn CC-002 semantics), the smalltalk/empty-plan CC-002 exception, and the DR-6 projection-grade-approver framing, and the JWT-helper grep gate. REFUTED: 'the allowlisted trigger-injection helper in T3-9 is the only publish-capable test path' is false as written — T1a-1 introduces a journey-author-reachable `system-fixture` act kind whose containment is undefined, so a journey act can plausibly inject/forge system events; the paragraph must reconcile this. MISSING (the biggest gaps): (1) shadow EXECUTE rows land in the real intent_audit table and are ingested by the kernel-replay CI gate and the impact graph as production signal — T3-6's session-namespace isolation only covers memory read paths, not time-windowed scans, and the replay SQL has no session/principal filter; (2) chooseImplementation has no fail-closed pin/conformance test guaranteeing real tenants never resolve the sandbox and the shadow agent never resolves the real executor; (3) the publish helper's 'check-bypass leg' is unspecified versus the JWT helper's named grep gate; (4) the T3-4 agent-scope guard is placed in the business phase (wrong altitude) where confirmOnAutoResolveGuard can short-circuit it. The paragraph should be rewritten to (a) drop or qualify the 'only publish-capable path' claim, (b) add the intent_audit-as-production-signal hole as the headline governance finding with the replay/impact filter fix, (c) add the chooseImplementation fail-closed requirement, and (d) note the scope-guard altitude.
