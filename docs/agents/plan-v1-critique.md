
# HIGH

- **Three-way auth contradiction. Oracle kit (line 87) says authFixture does 'test-OTP via the existing fake-Twilio seam'; Section 3 gap 4 (line 46) says 'Twilio Verify OTP has no test seam at the HTTP level'; Safety rails (line 103) and Section 5 adoption (line 156) decide 'no test route in the binary at all — offline JWT minting'. Source confirms gap 4: apps/api/src/routes/auth.ts constructs the real Twilio client inline from env vars (TWILIO_ACCOUNT_SID/AUTH_TOKEN, lines 26-35) — no fake-Twilio seam exists. The oracle-kit bullet references infrastructure that does not exist and was explicitly superseded.**
  - where: Section 4 Phase 1 'Oracle kit' authFixture bullet (line 87) vs Safety rails (line 103) vs Section 3 gap 4 (line 46) vs Section 5 first adopted bullet (line 156)
  - fix: Offline JWT minting is operative (stated twice as decided, including as a Phase-1 blocker). Rewrite the authFixture bullet: 'mints customer JWTs offline with the test env JWT_SECRET for seeded customers; staff-JWT minting gated tighter; Twilio HMAC computed locally for WhatsApp webhook driving'. Delete every fake-Twilio reference.

- **The entire decided visualization workstream is missing. Preference: read-only React Flow viewer over GENERATED JSON (ibx graph export + run traces), 4 derived graphs (capability/journey/run/impact), drift-gated. The plan name-drops 'journey graph, kernel fixtures, viewer JSON' once as generated projections (noun-test paragraph, line 80) but contains no `ibx graph export` command, no viewer deliverable, no derived-graph generation, no drift gate, and no phase assignment for any of it.**
  - where: Absent from all phases; only oblique mention at Section 4 Step 0 noun-test paragraph (line 80)
  - fix: Add an explicit workstream (likely Phase 2, after JSONL traces and the registry exist): `ibx graph export` emitting the 4 derived graphs as JSON, the standalone QA React Flow viewer consuming it plus run traces, and a CI drift gate (regenerate-and-diff) so the graphs can never be hand-maintained.

- **Console-scope conflict with the decided preference 'adjudicate console stays OPS-ONLY; QA gets its own viewer'. Section 5 adopts: 'console extended (≤3 routes: /agents, /coverage, /runs + save-audit-record-as-journey button), never a new app'. /coverage, /runs, and save-as-journey are QA-plane features placed in the ops console, and 'never a new app' directly forecloses the QA viewer the preference mandates.**
  - where: Section 5, 'agentRosterDrift … console extended (≤3 routes …)' bullet (line 163)
  - fix: Keep /agents (and approvals pages) in the adjudicate console; move /coverage, /runs, and save-as-journey into the QA viewer (which doubles as the React Flow graph viewer from the visualization preference — one new QA app, not zero). Strike 'never a new app'.

- **Stage 0 'dispatch structurally suppressed' has no mechanism and conflicts with the kernel/runtime invariant EXECUTE ⇒ exactly one tool invocation. If the adjudicator returns EXECUTE and the host swallows dispatch, the audit row asserts an execution that never happened — corrupting the very journal the plan designates as 'the eval dataset', and breaking conformance. Stage 1 names its mechanism (shadow guard converts EXECUTE → REQUEST_CONFIRMATION); Stage 0 names none.**
  - where: Section 4 Phase 3, Rollout Stage 0 (line 127) vs Stage 1 (line 128)
  - fix: Define Stage 0 mechanically: either a shadow guard that converts EXECUTE to a distinct auditable outcome (e.g., REFUSE with reason shadow-mode, queryable for the eval dataset), or a shadow composition with a no-op tool registry whose dispatch result is explicitly marked suppressed in the journal. State which, and state how audit truthfulness and the EXECUTE invariant are preserved.

- **'Subscribe-only NATS credentials' presented as settled, but no NATS authorization exists in this stack: docker-compose.yml runs nats:2.11-alpine with command [--jetstream, --store_dir, /data, -m 8222] — no accounts, users, or permissions. Delivering subscribe-only creds means introducing NATS authz config and migrating every existing client (API, workers, audit sink) to authenticated connections — an unstated, unscheduled deliverable with production blast radius, buried inside a Phase-1 oracle-kit bullet.**
  - where: Section 4 Phase 1 Oracle kit, NatsCapture bullet (line 85); verified against ibatexas/docker-compose.yml lines 64-72
  - fix: Either schedule NATS authz provisioning as a named Phase 0/1 deliverable (config + client migration + the one allowlisted publish helper Phase 3 line 133 assumes), or downgrade the rail honestly: NatsCapture client constructed publish-incapable by the harness with a lint/bypass-check rule, with the forgery risk stated as residual.

- **Haiku-pinning fidelity gap, worse than suspected: production default is claude-opus-4-5 (claustrum-bootstrap.ts:813 and :1254, `ANTHROPIC_MODEL ?? "claude-opus-4-5-20250101"`) — pinning the SUT to Haiku-class certifies a planner two capability tiers below production. Tool selection, confirmation round-trips, and refusal behavior are exactly what journeys assert, and all are model-sensitive. The nightly flake ledger will conflate Haiku capability noise with real journey defects, and the Phase 1 exit criterion certifies a system that never ships. The plan frames the pin purely as cost ('SUT-side tokens dominate cost') with no fidelity caveat — internally inconsistent with its own 'state it honestly' posture.**
  - where: Section 4 Phase 1 'Cost & flake posture' first bullet (line 106); exit criterion line 113
  - fix: Nightly certification suite runs the production model — at the plan's own $0.35–0.60/journey Haiku figure, Opus at ~10-20x for 20-50 journeys nightly is roughly $10-60/night, still trivial against the value; reserve Haiku-class for on-demand/dev iteration runs marked non-certifying. Pre-flight asserts model == certification target (not just == pinned cheap model) and records the model id in the JSONL trace. At minimum: weekly full-prod-model pass with divergence diffed against Haiku nights.

- **Deterministic nonces for trigger envelopes are under-specified and internally tension-laden. Phase 0 (line 58) prescribes 'hash of triggerId+intentKind', but Section 5 (line 167) establishes the planner mints a fresh nonce per attempt and trigger-originated envelopes flow through that same planner (Phase 3: SystemChannel → Conductor → planner). No seam is named for where the deterministic nonce is minted or how it overrides the planner's mint. Also: hash(triggerId+intentKind) collides if one trigger legitimately yields multiple same-kind envelopes (multiple entities), and over-dedupes legitimate later re-fires unless ledger-dedup TTL semantics are stated.**
  - where: Section 4 Phase 0 first bullet (line 58) vs Section 5 'Corrected' first bullet (line 167) and Phase 3 SystemChannel (line 123)
  - fix: Specify the seam: trigger bridge stamps triggerId into channel/turn context; envelope-builder (or planner wrapper) accepts an optional nonce-derivation input and derives nonce = hash(triggerId, intentKind, entityId). State the ledger dedup window/TTL. Note explicitly that this requires a small claustrum-or-ibatexas-side seam and budget it in Phase 0 (which is currently sized in 'days').

- **Phase 1 effort unrealistic at 3-4 weeks. The phase now contains: Journey Registry Step 0 (zod schema + roster gate + coverage gate + a reconciliation engine with EXACT/IN_ORDER/ANY_ORDER trajectory matching against intent_audit sessionId namespaces), 8-10 authored journeys including negatives, a 5-component oracle kit, the driver, structurally-enforced skill split (Postgres read-only role provisioning), safety rails (env handshake requires a NEW test-mode fingerprint health endpoint — an API change — plus a 'test composition' that exists nowhere), offline-JWT + Twilio-HMAC fixtures, nightly CI environment for the full stack (Postgres+Redis+NATS+Typesense+Medusa+API — no workflow exists; Section 5 line 164 confirms Playwright is wired to no CI), JSONL artifacts, retry/flake policy, AND promoting ibx kernel replay to a policy-diff PR gate. Realistically 6-8+ weeks for one engineer.**
  - where: Section 4 Phase 1 header (line 63) through exit criterion (line 113)
  - fix: Split: Phase 1a (registry schema + roster/coverage gates + ChatClient lifted from ibx api chat + authFixture + ProjectionBarrier + driver + ONE green journey, with manual nightly invocation) and Phase 1b (reconciliation gate, NatsCapture, AuditTrailMatcher full matchers, kernel-replay PR gate, CI nightly automation). Or extend to 6-8 weeks. Keep the exit criterion on 1a.

# MEDIUM

- **Phase 3 approvals glue at ~1 week is optimistic against its own deliverable list: parked claustrum SessionPort envelope ↔ approval-engine request ↔ bridge resume() re-adjudication, spanning two repos (adjudicate console approvals pages + ibatexas bridge), plus supersedes lineage, ConfirmationReceipt staff principal, INV-AGENT-CONFIRM-LINEAGE invariant, notification channel config, and agent-provenance rendering. Section 3 gap 5 itself establishes no resume path exists for parked envelopes outside the HTTP receipt store — this is new plumbing, not glue.**
  - where: Section 4 Phase 3 Stage 1 (line 128) and resume-lineage bullet (line 132)
  - fix: Re-estimate at 2-3 weeks within the weeks 9-13 window (which also holds SystemChannel, ChannelKind upstream edit, scope guard, loop breakers, kill switch, HandoffPort), or descope Stage 1 initially to webhook-channel approvals without console UI and stage the console pages.

- **Terminology drift: 'scenario' still used where 'journey' is meant, after the plan itself reserves 'scenario' for the ibx data-state engine and kernel fixtures. Instances: skill-split pass/fail ('scenario YAML carries required_invariants', 'scenario-bound args', line 94); the entire 'Scenario rules learned from the code' section ('pre-flight rejects scenarios', 'Phase-1 scenarios stop at pending_payment', 'Paid-order scenarios', 'scenarios create all run-scoped data', 'per-scenario destructive re-seeds', lines 96-99); '$0.35–0.60/scenario' (line 106); 'a scenario flaking two nights in a row' (line 108); Phase 2 '6–8 scenarios' and 'the 1–2 cheapest scenarios' (line 116); not-built table 'parallel scenarios' (line 145). Each now actively denotes the wrong artifact.**
  - where: Lines 94, 96-99, 106, 108, 116, 145
  - fix: Global rename pass: journey YAML, journey rules, $/journey, journey flaking, 6-8 journeys, parallel journeys. Allow bare 'scenario' only with an ibx- or kernel- prefix.

- **Stale examples surviving the OrderShepherd → PIX-remediation switch: (a) identity example 'agent:order-shepherd@3:order:{entityId}' (line 124); (b) acceptance test 'seed a stale order, fire the trigger' (line 133) — stale order is the shepherd scenario; the PIX agent's trigger is payment.status_changed failed/expired; (c) Stage 2 ramp 'staff notes → WhatsApp nudge → cancel proposals' (line 129) reads as shepherd intent kinds and is inconsistent with the stated PIX ladder 'pix.regenerate auto; refunds always confirm' (line 120).**
  - where: Section 4 Phase 3, lines 124, 129, 133
  - fix: Rewrite all three for PIX: 'agent:pix-remediation@1:order:{id}'; 'seed an order with a failed/expired PIX payment, fire payment.status_changed'; ramp 'pix.regenerate → customer payment nudge → refund proposals'. Keep order-shepherd examples only in an explicit agent-#2 aside.

- **Section 5 adopts eight items assigned to NO phase (orphans): (1) agentRosterDrift() boot/CI gate + agent registry in AI-BOM; (2) console route extensions (also preference-conflicted, see separate finding); (3) sim_runs/sim_results Postgres tables joined to intent_audit via sessionId; (4) add-only SEMCONV keys; (5) scripted-completion pipeline regression tests — explicitly called 'an engineering deliverable with the canonicalization cost stated' yet scheduled nowhere; (6) ChannelKind upstream widening + version bump — a BLOCKING prerequisite of Phase 3's SystemChannel, absent from Phase 3's deliverable list; (7) docs durability copy of /tmp/wf-results/* (dies on reboot — urgent, unowned); (8) lifting ibx api chat into the ChatClient (Phase 1's '2–3 days' budget never updated despite Section 5 saying it 'shrinks substantially').**
  - where: Section 5 'Adopted from the fleet blueprint' bullets (lines 156-164), 'Corrected' bullet 2 (line 168), Durability note (line 171)
  - fix: Assign each: sim tables + SEMCONV keys → Phase 1 (testkit needs run identity on day one); ChannelKind widening + agentRosterDrift + registry-in-AI-BOM → Phase 3 deliverable list; scripted-completion regression tests → Phase 2 or explicitly deferred with a revisit trigger; docs copy → today/Phase 0; update the ChatClient budget line.

- **Phase 0 has two sources of truth: Section 5's hygiene list is labeled '(Phase 0 additions)' (untested packages domain/types, dead check-bypass.sh step 3, unwired Playwright, stale CLAUDE.md/AI_CONTEXT.md) but Section 4's canonical Phase 0 checklist does not contain them.**
  - where: Section 4 Phase 0 (lines 57-61) vs Section 5 hygiene bullet (line 164)
  - fix: Fold the hygiene items into Section 4's Phase 0 list and leave Section 5 as provenance commentary only.

- **Coverage gate is weaker than the decided metric. Preference: coverage = intentKind × Decision-outcome matrix from intent_audit. The plan's gate 2 enforces only 'every intentKind covered by ≥1 journey'; the Decision dimension is tracked as view (a) (line 74) but never gated — so cancel can be 'covered' by one happy EXECUTE journey while its REFUSE cell stays empty with no waiver required, despite line 75 declaring negative journeys first-class.**
  - where: Section 4 Step 0, gate 2 (line 72) vs two-views paragraph (line 74) and negative-journeys paragraph (line 75)
  - fix: Gate over matrix cells: each intentKind × expected-Decision pair the packs can produce must be covered by a journey's expects or carry a cell-level checked-in waiver. This makes the negative-journey commitment enforceable rather than aspirational and matches the decided metric exactly.

- **Rule-of-two trigger mismatch. Preference: extract upward only when DOMAIN #2 arrives. Phase 4 and the not-built table fire extraction when 'a managed agent + testkit both consume it (Phase 3/4)' — two consumers inside one domain (ibatexas), which triggers @claustrum/skills / agent-host / runner extraction a full stage earlier than the decided rule.**
  - where: Section 4 Phase 4 (line 136) and not-built table row 1 (line 141)
  - fix: Restate the Phase 4 trigger as second business domain. Agent+testkit dual consumption within ibatexas licenses only the additive in-repo manifest wrapper, not package extraction.

- **Sequencing contradiction on the dangling payment capabilities. Phase 0: 'Fix or de-advertise payment.method.switch / payment.retry'. Phase 1: coverage gate seeds them as 'waived-pending-WS4'. If Phase 0 de-advertises, they exit the roster and the waiver is dead text; if Phase 0 fixes, they need journeys, not waivers. Phase 2 then schedules a 'payment retry' journey although WS4 is never defined or scheduled anywhere in the document (it also surfaces unexplained at line 177).**
  - where: Phase 0 bullet 3 (line 60) vs Phase 1 coverage-gate seeding (line 72) vs Phase 2 list (line 116) vs Section 6 item 4 (line 177)
  - fix: Pick one path: de-advertise in Phase 0 and drop both the waiver and the Phase 2 payment-retry journey (re-add behind blocked_by when the capability ships), OR define WS4 with a phase and keep the waiver + journey behind blocked_by: WS4. Add a one-line WS4 definition either way.

- **Stale rejection rationale for transcript replay. Section 4 rejects it as 'verified infeasible: no model-provider boot seam in claustrum-bootstrap.ts (AnthropicProvider constructed inline)' — but Section 5 corrects this: 'that file is ibatexas-side, so adding an optional modelProvider override is an in-repo change', and declares a feasible scripted-completion variant valuable. The not-built table's 'Never as designed' partially papers over it, but Section 4's load-bearing reason is contradicted later in the same document.**
  - where: Section 4 'Explicitly rejected' (line 111) vs Section 5 'Corrected' bullet 2 (line 168) vs not-built table row 2 (line 142)
  - fix: Rewrite the Section 4 rejection to cite only the still-true blockers (content-blind InMemoryModelProvider, non-seed-stable Medusa ids, dynamic prompt fragments) and cross-reference the rescoped scripted-completion deliverable and its phase.

- **Section 3 gap 3 ('No programmatic chat client for tests') is overstated relative to Section 5's verified finding that 'ibx api chat already implements the chat+SSE protocol' (POST /api/chat/messages, SSE parse, session reuse). The Phase 1 ChatClient bullet still budgets 2-3 days of 'fiddly SSE work' as if from scratch.**
  - where: Section 3 gap 3 (line 45) vs Section 5 adopted bullet 2 (line 157) vs Phase 1 ChatClient bullet (line 86)
  - fix: Restate gap 3 as 'chat client exists in the CLI, not yet packaged as a testkit ChatClient with turn-completion barrier and contract test'; reduce the budget line accordingly.

- **Nightly CI environment provisioning is presupposed, never specified. Cadence is 'nightly + on-demand (PR label)'; seeding is 'once per CI job'; the environment handshake requires a 'test composition' emitting a test-mode fingerprint — yet no docker-compose.test.yml exists, no CI workflow is named, no boot/seed time is budgeted for the full stack (Postgres+Redis+NATS+Typesense+Medusa+API), and no secret handling (ANTHROPIC_API_KEY, test JWT_SECRET) is mentioned. Section 5 itself notes Playwright is wired into zero CI workflows today, so there is no precedent pipeline to extend.**
  - where: Section 4 'Cost & flake posture' cadence bullet (line 107), seeding rule (line 99), environment handshake (line 102)
  - fix: Add a named Phase 1 deliverable: docker-compose.test.yml (the 'test composition', with fingerprint env), a nightly GitHub Actions workflow with boot+seed+suite steps and secrets, and a measured boot-time figure feeding the existing 45-min revisit trigger.

# LOW

- **Missing exit criteria: only Phase 1 has one (line 113). Phase 0 has no verification (e.g., 'a redelivered trigger does not double-fire, proven by a test'); Phase 2 has none; Phase 3 has none despite its own justification being 'the outcome is directly measurable (recovered orders)' — no target or measurement deliverable is defined; Phase 4 has triggers but no completion criteria.**
  - where: Sections 4 Phase 0 (lines 57-61), Phase 2 (line 116), Phase 3 (lines 118-133)
  - fix: One falsifiable exit criterion per phase. Phase 3's should include a recovered-orders measurement (the sim_runs/sim_results tables are the natural home, which also helps schedule that orphan).

- **Orphan noun: the noun test (line 80) declares 'AgentDefinition' the ops plane's single authored noun, but AgentDefinition is never defined or mentioned again — Phase 3 has only the implicit 'registered agent' in the scope guard and the unscheduled agent registry in AI-BOM.**
  - where: Section 4 Step 0 noun-test paragraph (line 80) vs Phase 3 (lines 118-133)
  - fix: Define AgentDefinition in Phase 3: shape (id, version, declared intentKinds, trigger, autonomy stage, kill-switch key), storage location, and its relation to agentRosterDrift and the AI-BOM registry.

- **No owners anywhere in the document: safety-rail maintenance (production-hostname denylist), flake re-authoring ('leaves the suite until re-authored' — by whom?), waiver approval for the coverage gate, the docs durability copy, and incident response for the kill switch all lack a DRI. Phases lack named owners entirely.**
  - where: Whole document; specifically lines 102-103, 108, 72, 171, 130
  - fix: Add a per-phase DRI line and name owners for the four standing duties (denylist, flake ledger, waivers, kill switch).

- **Phase 4 'scenario-runner extraction' is ambiguous after the renaming: unclear whether it means extracting the ibx scenario engine (runPipeline/StepRegistry — which Section 5 line 169 says journeys share) or the journey runner.**
  - where: Section 4 Phase 4 (line 136)
  - fix: Rename explicitly: 'shared runPipeline/StepRegistry engine extraction (serving both ibx scenario and ibx journey)' or 'journey-runner extraction' — whichever is intended.
