; Link sibling @adjudicate/* and @claustrum/* (declared as semver ranges) to the
; live ../adjudicate and ../claustrum working trees so kernel + runtime fixes
; compose into the app. See pnpm-workspace.yaml + pnpm.overrides in package.json.
link-workspace-packages=true
prefer-workspace-packages=true
