{
  "name": "ibatexas",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "db:migrate": "turbo db:migrate",
    "db:seed": "turbo db:seed",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "audit:overrides": "tsx scripts/audit-overrides.ts",
    "check:overrides": "tsx scripts/check-overrides-change.ts",
    "upgrade:radar": "tsx scripts/upgrade-radar.ts"
  },
  "devDependencies": {
    "@ibatexas/journeys": "workspace:*",
    "@playwright/test": "^1.58.2",
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^3.2.6",
    "prettier": "^3.0.0",
    "tsx": "^4.21.0",
    "turbo": "^2.8.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.6"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "packageManager": "pnpm@10.32.1",
  "pnpm": {
    "overrideNotes": {
      "@adjudicate/core": "Force a SINGLE @adjudicate/core version (1.9.0) across the workspace. The published @adjudicate/* deps pin core to EXACTLY 1.5.0; the SDD claims-runtime (1.6.0) + W6 valueBinding/falsifier/renderableCanonical (1.7.0) + Track-A egress-brand kernel (1.8.0) + BKL-069 IntentActor.role carrier (1.9.0) are purely additive (1.9.0 ⊇ 1.5.0 — no existing export changed/removed; `role` is an optional field on IntentActor, canonical-drop-safe so intentHash is unaffected, proven by golden vectors v12/v13), so this override safely coerces those transitive 1.5.0 pins up. @claustrum/core (0.6.0 primary on dev + 0.4.0 still transitive) peer-deps @adjudicate/core ^1.8.0, satisfied by 1.9.0 as a single shared instance (no duplicate core). Published on npm (registry.npmjs.org/@adjudicate/core/-/core-1.9.0.tgz). Remove/relax only if the @adjudicate/* deps move to a compatible RANGE.",
      "minimatch, rollup, glob": "vitest/vite toolchain — review on vite 6 upgrade",
      "express>path-to-regexp": "medusa>express transitive fix — GHSA-37ch-88jc-xwx2 ReDoS. Scoped (not global) because @medusajs/framework declares path-to-regexp ^8.2.0 and uses the v6+ named-export API; forcing 0.1.13 globally crashes commerce at startup with 'pathToRegexp is not a function'. next@16 also declares 6.3.0 and would be silently downgraded. Remove when medusa drops express 4.",
      "lodash": "medusa/prisma transitive fix — GHSA-r5fr-rjxr-66jc code injection via _.template",
      "picomatch": "eslint-config-next/vitest transitive fix — GHSA-c2c7-rcm5-vvqj ReDoS",
      "@types/react, @types/react-dom": "medusa pulls React 18 types — force single version to fix JSX portability errors",
      "vite@7": "vitest/qa-viewer>vite v7 transitive fix — GHSA-v2wj-q39q-566r + GHSA-p9ff-h696-f583 + GHSA-fx2h-pf6j-xcff (server.fs.deny bypass on Windows alternate paths, patched 7.3.5). Bumped 7.3.2->7.3.5. Scoped to v7 so medusa's vite v5 is untouched. Remove when vitest pulls vite >= 7.3.5.",
      "form-data": "apps/api>twilio>axios>form-data transitive fix — GHSA-hmw2-7cc7-3qxx CRLF injection via unescaped multipart field names/filenames. Patched in 4.0.6+. Remove when twilio's axios pulls form-data >=4.0.6.",
      "@rushstack/node-core-library>ajv": "@medusajs/cli>...>@rushstack/node-core-library>ajv transitive fix — GHSA-2g4f-4pwh-qvx6 ReDoS via $data option. Scoped (not global) because @eslint/eslintrc uses ajv v6 API and breaks if forced to v8 ('NOT SUPPORTED: option missingRefs' / 'Cannot set defaultMeta'). Remove when rushstack updates.",
      "esbuild": "@medusajs/framework>vite v5>esbuild + .>tsx>esbuild transitive fix — GHSA-67mh-4wv8-2f99 dev-server CORS (fixed in 0.25.0). Pinned to ^0.25.0, NOT 0.28.x: esbuild 0.28 lowers destructuring more strictly and crashes Medusa's admin-vite-plugin dep optimizer with thousands of 'Transforming destructuring to the configured target environment is not supported yet' errors against the @react-aria/@ariakit ESM (admin /app/login spins forever). The only advisory 0.28 added over 0.25 was GHSA-gv7w-rqvm-qjhr (Deno binary-integrity verification, N/A to this Node repo), so 0.25.x is fully sufficient. Remove when medusa upgrades vite to v6+.",
      "@grpc/grpc-js": "apps/api>testcontainers>dockerode>@grpc/grpc-js transitive fix — GHSA-5375-pq7m-f5r2 (CVE-2026-48068) + GHSA-99f4-grh7-6pcq (CVE-2026-48069) malformed-request server crash. Test-only dependency (Testcontainers / dockerode). Patched in 1.14.4+.",
      "fast-jwt": "apps/api>@fastify/jwt transitive fix — GHSA-rp9m-7r4c-75qg cache confusion + GHSA-gmvf-9v4p-v8jc (CVE-2026-44351) CRITICAL JWT auth bypass on empty HMAC secret. Patched in 6.2.4+. Remove when @fastify/jwt pulls fast-jwt >=6.2.4.",
      "axios": "apps/api>twilio transitive fix — CVE-2026-42033/42035/42043/44486-44496 cluster (prototype pollution, proxy-auth credential leak, MITM, ReDoS, NO_PROXY bypass) + GHSA-gcfj-64vw-6mp9 Node HTTP adapter inherited-proxy leak (vuln >=1.15.2 <1.18.0). Patched in 1.18.0+. Remove when twilio pulls axios >=1.18.0.",
      "brace-expansion@5": "@vitest/coverage-v8>test-exclude>glob>minimatch + @sentry/nextjs + eslint toolchain transitive fix — GHSA-3jxr-9vmj-r5cp DoS via exponential-time expansion of consecutive non-expanding {} groups, plus GHSA-mh99-v99m-4gvg DoS via unbounded expansion length causing an out-of-memory process crash (vuln <=5.0.7 — the 5.0.7 pin this override previously carried is itself vulnerable). Dev/build-tooling only. Scoped to v5 so any future 1.x/2.x copy keeps its own API. Patched in 5.0.8. Remove when minimatch/glob pull brace-expansion >=5.0.8.",
      "js-yaml@3": "apps/commerce>@medusajs/cli>pg-god>cli-ux transitive fix — GHSA-52cp-r559-cp3m YAML merge-key chains force quadratic CPU (3.x range: vuln >=3.0.0 <3.15.0). Dev/CLI-tooling only. Scoped to v3 (installed 3.14.2) via the 3.15.0 security backport so cli-ux's v3 API is preserved. Remove when medusa's CLI chain drops js-yaml 3.x.",
      "js-yaml@4": "apps/admin>eslint>@eslint/eslintrc transitive fix — GHSA-52cp-r559-cp3m YAML merge-key chains force quadratic CPU (4.x range: vuln >=4.0.0 <4.3.0). Dev/lint-tooling only. Scoped to v4 (installed 4.1.1) so the js-yaml@3 copy is untouched. Patched in 4.3.0. Remove when eslint pulls js-yaml >=4.3.0.",
      "protobufjs": "apps/api>@sentry/node transitive fix — GHSA-xq3m-2v4x-88gg arbitrary code execution + CVE-2026-44289/44290/44291/44293 code injection + DoS. Patched in 8.0.2+. @sentry/node's OTLP encoder serializes only fixed internal schemas (never user-supplied .proto), so the major bump is purely hardening.",
      "fastify": "apps/api direct dep fix — GHSA-247c-9743-5963 body schema validation bypass via leading-space Content-Type. Patched in 5.8.5+.",
      "next": "apps/admin + apps/web direct dep fix — GHSA-q4gf-8mx6-v5v3 DoS via Server Components + the CVE-2026-44573..45109 middleware/proxy-bypass, SSRF and DoS cluster (patched 16.2.6), plus the 2026-07-25 cluster: GHSA-6gpp-xcg3-4w24 middleware/proxy bypass in App Router on Turbopack + single locale, GHSA-m99w-x7hq-7vfj DoS in App Router Server Actions, GHSA-89xv-2m56-2m9x SSRF in Server Actions on custom servers, GHSA-p9j2-gv94-2wf4 SSRF in rewrites via attacker-controlled destination hostname (all vuln >=16.0.0 <16.2.11). PRODUCTION runtime — both Next apps are internet-facing. Patched in 16.2.11+; floor raised 16.2.6 -> 16.2.11, in-range for both apps' declared ^16.2.0 so this is a patch-level move inside the installed major.",
      "@opentelemetry/sdk-node, @opentelemetry/exporter-prometheus": "apps/api transitive fix — GHSA-q7rr-3cgh-j5r3 (CVE-2026-44902) Prometheus exporter process crash via malformed HTTP. Patched in 0.217.0+.",
      "tmp": "transitive fix — GHSA-ph9p-34f9-6g65 (CVE-2026-44705) path traversal via unsanitized prefix/postfix. Patched in 0.2.6+.",
      "fast-xml-builder": "transitive fix — GHSA-5wm8-gmm8-39j9 (CVE-2026-44665) attribute-value injection. Patched in 1.1.7+.",
      "undici": "apps/api>testcontainers>undici transitive fix — GHSA-vmh5-mc38-953g + GHSA-vxpw-j846-p89q + GHSA-hm92-r4w5-c3mj (cross-origin request routing via SOCKS5 proxy pool reuse). Test-infra only (Testcontainers HTTP). Patched in 7.28.0+; forward patch within v7 (installed 7.25.0). Remove when testcontainers pulls undici >=7.28.0.",
      "hono": "packages/domain>prisma>@prisma/dev>hono transitive fix — GHSA-88fw-hqm2-52qc CORS middleware reflects any Origin with credentials when origin defaults to wildcard. Dev-only (Prisma dev server). Patched in 4.12.25+; forward minor within v4 (installed 4.12.8). Remove when @prisma/dev pulls hono >=4.12.25.",
      "multer": "apps/commerce>@medusajs/medusa>multer transitive fix — GHSA-72gw-mp4g-v24j DoS via deeply nested multipart field names. Patched in 2.2.0+; medusa declares multer ^2.0.2, so >=2.2.0 is in-range (installed 2.1.1) — a safe patch within v2, NOT a major bump.",
      "fast-uri@3": "apps/api>@fastify/swagger>json-schema-resolver + apps/api>fastify>@fastify/ajv-compiler>ajv + packages/domain>prisma>@prisma/dev>ajv transitive fix — GHSA-q3j6-qgpj-74h6 (CVE-2026-6321) path traversal + GHSA-v39h-62p7-jpjc (CVE-2026-6322) host confusion (patched 3.1.2), plus GHSA-4c8g-83qw-93j6 host confusion via failed IDN canonicalization (vuln >=3.0.0 <3.1.3) and GHSA-v2hh-gcrm-f6hx host confusion via literal backslash authority delimiter — whose 3.x range is >=3.0.0 <=3.1.3, so the previous >=3.1.2 floor was itself vulnerable. PRODUCTION runtime (fastify's request-schema/URI validation on apps/api). Patched in 3.1.4+; floor raised 3.1.2 -> 3.1.4, a patch-level move inside v3. Scoped to v3 so the v4 copies keep their own API.",
      "fast-uri@4": "Floor guard for GHSA-v2hh-gcrm-f6hx (4.x range: vuln >=4.0.0 <=4.1.0, patched 4.1.1). NOTE: after this change no fast-uri 4.x copy exists in the tree at all. The 4.1.0 copy the audit flagged under apps/admin>@sentry/nextjs>@sentry/webpack-plugin>webpack>schema-utils>ajv was self-inflicted — the previous UNBOUNDED `fast-uri: >=3.1.2` override let pnpm satisfy ajv's declared ^3.0.1 with 4.1.0, crossing a major boundary. Every real consumer (ajv 8.18/8.20, json-schema-resolver, @fastify/ajv-compiler) declares ^3.x, so bounding the v3 override to <4 removes the 4.x copy outright. This entry is kept as a no-op guard so that if a consumer ever legitimately adopts fast-uri 4.x it lands on the patched line rather than reintroducing the advisory.",
      "postcss": ".>@vitest/coverage-v8>vitest>vite + apps/admin + apps/web (next/tailwind CSS pipeline) transitive fix — GHSA-6g55-p6wh-862q arbitrary file read + information disclosure via attacker-controlled sourceMappingURL in CSS comments (vuln <=8.5.11) + GHSA-r28c-9q8g-f849 path traversal in previous-source-map auto-loading leading to arbitrary .map file disclosure (vuln <=8.5.17). Build/test-tooling only — postcss runs at CSS build time over first-party stylesheets, never on user input at runtime. Patched in 8.5.18+; bounded to <9 so both installed copies (8.4.31 and 8.5.8) move forward inside major 8 and keep their plugin API.",
      "immutable": "apps/commerce>@medusajs/*>@medusajs/utils>@graphql-codegen/typescript>@graphql-tools/relay-operation-optimizer>@ardatan/relay-compiler transitive fix — GHSA-v56q-mh7h-f735 `List` 32-bit trie overflow causing unrecoverable DoS + GHSA-xvcm-6775-5m9r hash-collision algorithmic-complexity DoS in Immutable.Map/Set (both vuln >=5.0.0-beta.1 <5.1.8). Dev/CLI-tooling only (Medusa's GraphQL codegen path, never a request-serving code path). Patched in 5.1.8+; bounded to <6, a patch-level move inside v5 (installed 5.1.5). Remove when medusa's codegen chain pulls immutable >=5.1.8.",
      "@opentelemetry/propagator-jaeger": "apps/commerce>@medusajs/framework>@medusajs/deps>@opentelemetry/sdk-node transitive fix — GHSA-45rx-2jwx-cxfr DoS in `JaegerPropagator` via an unhandled exception on a malformed header (vuln <2.9.0). Reaches only apps/commerce through Medusa's bundled OTel SDK; we never enable Jaeger propagation, so no malformed header reaches the propagator. Patched in 2.9.0+; bounded to <3, a minor-level move inside v2 (installed 2.7.1). Remove when @medusajs/deps pulls @opentelemetry/sdk-node with propagator-jaeger >=2.9.0.",
      "find-my-way": "apps/api>fastify>find-my-way (+ via fastify-type-provider-zod) transitive fix — GHSA-c96f-x56v-gq3h DDoS with HTTP2 (vuln <=9.6.0). PRODUCTION runtime — this is fastify's router on the public API. A fastify floor bump does NOT clear it: fastify 5.10.0 still declares find-my-way ^9.6.0, which permits the vulnerable 9.6.0, so the fix has to be pinned here. Patched in 9.6.1+; bounded to <10 so fastify keeps its v9 router API (installed 9.5.0). Remove when fastify raises its own find-my-way floor to >=9.6.1.",
      "@fastify/static": "apps/api>@fastify/swagger-ui>@fastify/static transitive fix — GHSA-83w8-p2f5-377r route guard bypass via path traversal (vuln <=10.1.0). Serves the Swagger UI asset bundle on apps/api. FORCED MAJOR (9 -> 10): the advisory has no 9.x backport (patched is >=10.1.1) and no released @fastify/swagger-ui declares ^10 — 5.2.5 declares @fastify/static ^9.0.0 and even the latest 6.1.0 declares ^9.1.2 — so upgrading swagger-ui cannot clear it and the override is the only path. 9->10 changed only content-disposition ^1->^2 and fastify-plugin ^5->^6 (both internal); the register/decorateReply surface swagger-ui uses is unchanged, and fastify-plugin 6 targets the fastify v5 we already run. Verified by a green turbo build + full workspace test run. Remove when @fastify/swagger-ui declares @fastify/static ^10.",
      "sharp": "apps/admin + apps/web >next>sharp (also via @sentry/nextjs>next) transitive fix — GHSA-f88m-g3jw-g9cj inherited libvips vulnerabilities CVE-2026-33327 / CVE-2026-33328 / CVE-2026-35590 / CVE-2026-35591 (vuln <0.35.0). sharp is Next's optional image-optimization codec, so this is production runtime wherever next/image serves a remote or user-supplied source. FORCED MINOR across a 0.x boundary: next@16.2.11 declares sharp ^0.34.5 optionally, and the patch only exists at >=0.35.0, so the bump has to be forced past next's declared range. sharp 0.35 keeps the 0.34 JS API (the release moved to prebuilt libvips 8.18 binaries); bounded to <0.36 so it stays on the audited line. Verified by a green turbo build + full workspace test run. Remove when next declares sharp ^0.35."
    },
    "overrides": {
      "@adjudicate/core": "1.9.0",
      "minimatch": "10.2.4",
      "rollup": "4.59.0",
      "glob": "13.0.6",
      "express>path-to-regexp": "0.1.13",
      "lodash": "4.18.1",
      "picomatch@<3": "2.3.2",
      "picomatch@>=4": "4.0.4",
      "@types/react": "19.2.14",
      "@types/react-dom": "19.2.3",
      "vite@7": "7.3.5",
      "form-data": ">=4.0.6",
      "@rushstack/node-core-library>ajv": "8.18.0",
      "esbuild": "^0.25.0",
      "@grpc/grpc-js": ">=1.14.4",
      "fast-jwt": ">=6.2.4",
      "axios": ">=1.18.0",
      "brace-expansion@5": "5.0.8",
      "js-yaml@3": "3.15.0",
      "js-yaml@4": "4.3.0",
      "protobufjs": ">=8.0.2",
      "fastify": ">=5.8.5",
      "next": ">=16.2.11",
      "@opentelemetry/sdk-node": ">=0.217.0",
      "@opentelemetry/exporter-prometheus": ">=0.217.0",
      "tmp": ">=0.2.6",
      "fast-xml-builder": ">=1.1.7",
      "undici": ">=7.28.0 <8",
      "hono": ">=4.12.25 <5",
      "multer": ">=2.2.0 <3",
      "fast-uri@3": ">=3.1.4 <4",
      "fast-uri@4": ">=4.1.1 <5",
      "postcss": ">=8.5.18 <9",
      "immutable": ">=5.1.8 <6",
      "@opentelemetry/propagator-jaeger": ">=2.9.0 <3",
      "find-my-way": ">=9.6.1 <10",
      "@fastify/static": ">=10.1.1 <11",
      "sharp": ">=0.35.0 <0.36"
    },
    "auditConfig": {
      "ignoreCves": [
        "CVE-2026-34950",
        "CVE-2026-35042",
        "CVE-2026-44680"
      ],
      "ignoreGhsas": [
        "GHSA-gwhv-j974-6fxm",
        "GHSA-qpfv-44f3-qqx6",
        "GHSA-fx2h-pf6j-xcff",
        "GHSA-gv7w-rqvm-qjhr"
      ],
      "ignoreCveNotes": {
        "CVE-2026-35042": "fast-jwt <=6.1.0 GHSA-hm7r-c7qw-ghp6 — accepts unknown `crit` header extensions (RFC 7515 violation). No upstream patch released yet. Mitigation: we never set or trust `crit` headers in our JWT flow (Twilio Verify OTP only). Re-check when nearform/fast-jwt ships a fix.",
        "CVE-2026-44680": "@mikro-orm/knex SQL injection (GHSA-cfw5-68c4-ffqp, patched >=6.6.14) — same situation + mitigation as the @mikro-orm/core advisories below: pulled exclusively by @medusajs/deps, which pins the MikroORM 6.4.x line; forcing 6.6.x crashes Medusa's service bootstrap (the 6.4→6.6 EventManager subscribers Array->Set change). Mitigation: nothing in this repo runs user-controlled queries through MikroORM — Medusa is the only consumer and all its query paths are parameterized. FE-D08 (2026-07-16) re-attempted the 6.6.14 override: `pnpm install`, apps/commerce's vitest suite, and `medusa build` all passed clean, but none of those exercise the live DI-container service-bootstrap path where the documented crash occurs, and reaching that needs a running `medusa start`/`develop` (forbidden by this repo's CLAUDE.md — no starting dev servers to verify changes). Reverted rather than ship an unverified risk. Remove when Medusa upgrades MikroORM, or when someone can verify the live-boot path.",
        "GHSA-4w7w-66w2-5vf9": "Documentation only — not enforced (pnpm ignoreCves only matches CVE-* IDs). vite <=6.4.1 path traversal in optimized deps .map handling. Pulled by @medusajs/framework which has a hard peer dep on `vite ^5.4.21`, so we cannot override to 6.4.2+ without breaking medusa's admin-vite-plugin. Dev-server-only vuln, not exploitable in production builds. Stays as a moderate in audit output (below the --audit-level=high CI threshold). Remove when medusa upgrades to vite v6+.",
        "GHSA-gwhv-j974-6fxm": "Documentation only — not enforced (pnpm ignoreCves only matches CVE-* IDs). @mikro-orm/core SQL injection. Pulled exclusively by @medusajs/deps@2.13.5 which pins @mikro-orm/core to 6.4.16. Cannot override to a patched 6.6.x line because @medusajs/utils was built against MikroORM 6.4's EventManager (subscribers is an Array with .some()); 6.6+ switched subscribers to a Set, so registerInternalServiceEventSubscriber crashes on the first service bootstrap. Mitigation: nothing in this repo executes user-controlled queries through MikroORM — Medusa is the only consumer and all its query paths are parameterized. Re-check when Medusa upgrades to MikroORM 6.6+ or 7.x.",
        "GHSA-qpfv-44f3-qqx6": "Documentation only — same situation and mitigation as GHSA-gwhv-j974-6fxm. Second SQL injection advisory against the same @mikro-orm/core 6.4.x line pulled via @medusajs/deps@2.13.5. Paired tracking — remove both together when Medusa bumps MikroORM.",
        "GHSA-gv7w-rqvm-qjhr": "esbuild <0.28.1 — Deno binary-integrity verification bypass, N/A to this Node repo (we never run the esbuild Deno binary). Re-introduced deliberately by pinning esbuild to ^0.25.0: esbuild 0.28 lowers destructuring more strictly and crashes Medusa's admin-vite-plugin dep optimizer against the @react-aria/@ariakit ESM (admin /app/login spins forever), so 0.28 is not usable here. 0.25.x already fixes the only OTHER esbuild advisory (GHSA-67mh dev-server CORS). Remove when medusa upgrades vite to v6+ and esbuild 0.28 becomes usable.",
        "GHSA-fx2h-pf6j-xcff": "vite `server.fs.deny` bypass on Windows alternate paths. The vitest + qa-viewer instances (vite v7) ARE fixed via the `vite@7: 7.3.5` override above. The ONLY remaining instance is `@medusajs/framework>vite` (v5.x), which has no patched release in the 5.x line (patched is >=6.4.3) and cannot be overridden because medusa hard-pins `vite ^5.4.21` (forcing v6 breaks medusa's admin-vite-plugin — see the GHSA-4w7w note). Dev-server-only vuln (we run `vitest run` + production builds, never a long-lived vite dev server) and Windows-only; not exploitable here. Remove when medusa upgrades to vite v6+."
      }
    },
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "@swc/core",
      "core-js",
      "esbuild",
      "msgpackr-extract",
      "prisma",
      "protobufjs",
      "sharp",
      "@parcel/watcher",
      "@medusajs/telemetry",
      "unrs-resolver"
    ]
  }
}
